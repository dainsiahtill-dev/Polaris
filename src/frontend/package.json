{
  "name": "polaris-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Frontend workspace metadata. Dependency versions are managed at the repository root package.json.",
  "scripts": {
    "build": "vite build",
    "dev": "vite"
  }
}