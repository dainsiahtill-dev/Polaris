/**
 * Backward compatibility re-export for runtime transport stream API.
 */

export { runtimeSocketManager } from './runtimeSocketManager';
export type {
  ChannelSubscription,
  ConnectionState,
  ConnectionStateListener,
  MessageListener,
  RuntimeRole,
  RuntimeSocketManager,
} from './runtimeSocketManager';
