/**
 * Task Trace Type Definitions (Legacy Re-export)
 *
 * 此文件为向后兼容保留，新代码请直接从 '@/app/types/taskTrace' 导入
 */

export type {
  TaskTraceEvent,
  TaskTraceMap,
  TaskTraceMapObject,
  TaskTraceStatus,
  TaskTraceKind,
} from '@/app/types/taskTrace';
