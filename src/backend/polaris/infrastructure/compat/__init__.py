"""Compatibility surfaces that are intentionally outside KernelOne.

KernelOne must stay focused on clean runtime primitives. Transitional facades
for older application/cell consumers live here so the compatibility burden does
not leak back into the kernel.
"""
