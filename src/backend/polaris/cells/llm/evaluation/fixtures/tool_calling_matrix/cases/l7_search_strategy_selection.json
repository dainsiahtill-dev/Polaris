{
  "case_id": "l7_search_strategy_selection",
  "level": "L7",
  "role": "director",
  "title": "L7 Search Strategy Selection",
  "description": "测试 Agent 能否根据任务选择合适的搜索策略（glob vs grep vs search_code）。",
  "workspace_fixture": "base_tooling_workspace",
  "prompt": "找到所有包含 'import' 关键词的 Python 文件，然后读取其中一个查看导入内容。",
  "tags": ["l7", "search", "strategy", "grep", "tool-selection"],
  "weight": 1.0,
  "critical": true,
  "judge": {
    "score_threshold": 0.7,
    "stream": {
      "required_tools": ["read_file"],
      "required_any_tools": [
        ["search_code", "repo_rg", "grep"]
      ],
      "ordered_tool_groups": [
        ["search_code", "repo_rg", "grep"],
        ["read_file"]
      ],
      "forbidden_tools": ["execute_command", "search_replace", "edit_file"],
      "min_tool_calls": 2,
      "max_tool_calls": 4,
      "required_output_substrings": ["import"]
    },
    "non_stream": {
      "required_tools": ["read_file"],
      "required_any_tools": [
        ["search_code", "repo_rg", "grep"]
      ],
      "forbidden_tools": ["execute_command", "search_replace", "edit_file"],
      "min_tool_calls": 2,
      "max_tool_calls": 4
    },
    "parity": {
      "required": true,
      "compare_mode": "set"
    }
  }
}