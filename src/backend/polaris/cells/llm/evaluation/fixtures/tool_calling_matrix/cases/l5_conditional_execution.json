{
  "case_id": "l5_conditional_execution",
  "level": "L5",
  "role": "director",
  "title": "L5 Conditional Execution",
  "description": "测试 Agent 能否根据条件判断执行不同操作路径。",
  "workspace_fixture": "base_tooling_workspace",
  "prompt": "检查 tests/test_api_real.py 是否存在。如果存在，读取它的内容；如果不存在，列出 tests 目录的内容。",
  "tags": ["l5", "conditional", "branching", "file_exists"],
  "weight": 1.0,
  "critical": true,
  "judge": {
    "score_threshold": 0.75,
    "stream": {
      "required_tools": ["file_exists"],
      "required_any_tools": [
        ["read_file"],
        ["list_directory"]
      ],
      "ordered_tool_groups": [
        ["file_exists"],
        ["read_file", "list_directory"]
      ],
      "forbidden_tools": ["execute_command", "search_replace", "edit_file"],
      "min_tool_calls": 2,
      "max_tool_calls": 4
    },
    "non_stream": {
      "required_tools": ["file_exists"],
      "required_any_tools": [
        ["read_file"],
        ["list_directory"]
      ],
      "forbidden_tools": ["execute_command", "search_replace", "edit_file"],
      "min_tool_calls": 2,
      "max_tool_calls": 4
    },
    "parity": {
      "required": true,
      "compare_mode": "set"
    }
  }
}