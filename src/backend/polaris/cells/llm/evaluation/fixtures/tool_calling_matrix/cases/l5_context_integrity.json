{
  "case_id": "l5_context_integrity",
  "level": "L5",
  "role": "director",
  "title": "L5 Context Integrity — No User Message Duplication",
  "description": "验证多轮迭代中用户消息不被重复注入 context，避免历史中出现重复指令。",
  "workspace_fixture": "base_tooling_workspace",
  "prompt": "1. 读取 config.py 2. 读取 server.py 3. 再读取 config.py 确认内容",
  "tags": ["l5", "context_integrity", "dedup", "multi_iteration"],
  "weight": 1.0,
  "critical": true,
  "judge": {
    "score_threshold": 0.80,
    "stream": {
      "required_tools": ["read_file"],
      "min_tool_calls": 3,
      "max_tool_calls": 4,
      "allow_repeated_file": true,
      "require_no_context_bloat": true,
      "forbidden_tools": ["execute_command", "write_file"]
    },
    "non_stream": {
      "required_tools": ["read_file"],
      "min_tool_calls": 3,
      "max_tool_calls": 4,
      "allow_repeated_file": true,
      "require_no_context_bloat": true,
      "forbidden_tools": ["execute_command", "write_file"]
    },
    "parity": {
      "required": true,
      "compare_mode": "ordered"
    }
  }
}
