# Allowlist of cross-cell dependency cycles known to cells.yaml.
# Regenerate with: run_catalog_governance_gate.py --write-cycle-allowlist <path>
# Each entry freezes one strongly connected component (a cycle) so the
# no_new_cross_cell_cycle rule fails only on cycles introduced after this snapshot.
cycles:
  - fingerprint: e978231b2b5a4c13df0456d78ce5ce852fae7749702e133703ceb6addc6a76a7
    members: [architect.design, archive.factory_archive, archive.run_archive, archive.task_snapshot_archive, audit.diagnosis, audit.evidence, audit.verdict, chief_engineer.blueprint, code_intelligence.engine, cognitive.knowledge_distiller, context.catalog, context.engine, director.execution, director.planning, director.task_consumer, director.tasking, docs.court_workflow, events.fact_stream, factory.cognitive_runtime, factory.pipeline, factory.verification_guard, finops.budget_guard, llm.control_plane, llm.dialogue, llm.evaluation, llm.provider_config, llm.provider_runtime, llm.tool_runtime, orchestration.pm_dispatch, orchestration.pm_planning, orchestration.workflow_activity, orchestration.workflow_runtime, policy.permission, policy.workspace_guard, qa.audit_verdict, resident.autonomy, roles.adapters, roles.engine, roles.kernel, roles.profile, roles.runtime, roles.session, runtime.artifact_store, runtime.execution_broker, runtime.projection, runtime.state_owner, runtime.task_market, runtime.task_runtime, storage.layout, workspace.integrity]
