# Polaris Catalog Boundary Debt Ledger

Status: Active
Owner: Polaris backend governance
Created: 2026-07-01
Scope: Cell catalog schema, cross-Cell public boundaries, declared dependencies, and declared side effects.

## Purpose

This ledger tracks the remaining baseline issues reported by
`run_catalog_governance_gate.py --workspace src/backend --mode audit-only`.
It is separate from the legacy/shim convergence ledger: these items are not
old compatibility surfaces, but Cell boundary and manifest contract debt.

The target chain is:

`Cell manifest -> public import boundary -> declared dependency/effect -> catalog gate -> release gate`

## Baseline

After H-37 closed the `runtime_projection` schema blocker, the catalog gate
reported:

| Metric | Count | Notes |
| --- | ---: | --- |
| Total baseline issues | 41 | Existing baseline, `new_issue_count=0`. |
| Blockers | 15 | Cross-Cell internal imports or public-boundary violations. |
| High | 26 | Missing `depends_on` declarations or undeclared side effects. |

## Ledger

| ID | Severity | Gap | Resolution | Status | Verification |
| --- | --- | --- | --- | --- | --- |
| CB-01 | P0 | `roles.adapters` tests imported `director.runtime.internal.repair_kernel` helpers and Rust source-tool constants directly, keeping a cross-Cell internal dependency in test coverage. | `director.runtime.public.repair_kernel_contracts` now exposes only stable read-only constants and pure helper wrappers; `roles.adapters` tests consume that public surface instead of importing repair-kernel internals. | Closed | `ruff`, `py_compile`, `mypy`, targeted pytest slices, and catalog audit-only gate pass; catalog baseline is now 36 issues with 10 blockers and 26 high issues, and `roles.adapters imports director.runtime internal module` is 0. |
| CB-02 | P0 | `roles.adapters.internal.director.adapter` imported `director.tasking.internal` execution profile, execution strategy, and language guidance helpers directly; the public catalog also lacked the declared `roles.adapters -> director.tasking` dependency. | `director.tasking.public.execution_guidance` now owns the stable profile, strategy, override, and language-guidance boundary. The Director adapter consumes that public surface, and both `cell.yaml` and catalog SSoT declare the public module/dependency. | Closed | `ruff`, `py_compile`, `mypy`, targeted adapter/language-guidance pytest slices, and catalog audit-only gate pass; catalog baseline is now 32 issues with 7 blockers and 25 high issues, and both `roles.adapters imports director.tasking internal module` and missing `roles.adapters -> director.tasking` dependency are 0. |
| CB-03 | P0 | `director.execution.public.service` imported `director.tasking.internal` profile and strategy helpers while constructing missing execution-envelope metadata. | `director.execution.public.service` now consumes the `director.tasking.public.execution_guidance` contract introduced by CB-02, preserving execution-envelope behavior without crossing into tasking internals. | Closed | `ruff`, `py_compile`, `mypy`, `director.execution` contract tests, tasking language-guidance tests, and catalog audit-only gate pass; catalog baseline is now 30 issues with 5 blockers and 25 high issues, and `director.execution imports director.tasking internal module` is 0. |
| CB-04 | P0 | `factory.pipeline` imported `roles.kernel.internal.events` from artifact mirroring code and `roles.adapters.internal.director.quality_gate` from characterization tests. | Factory artifact mirroring now uses `roles.kernel.public` event exports. Roles adapters public service exposes stable Director materialization quality helper wrappers, and factory tests consume those public contracts. | Closed | `ruff`, `py_compile`, `mypy`, targeted factory characterization tests, and catalog audit-only gate pass; catalog baseline is now 28 issues with 3 blockers and 25 high issues, and both factory-to-roles internal import messages are 0. |
| CB-05 | P0 | `roles.kernel` tests imported `qa.audit_verdict.internal`, `resident.autonomy.internal`, and `roles.profile.internal` directly. | QA audit verdict now exposes `build_qa_verdict_envelope` through its public service. Roles kernel tests consume QA, Resident, and Profile public contracts instead of cross-Cell internals. | Closed | `ruff`, `py_compile`, `mypy`, targeted roles-kernel QA/Resident/Profile tests, and catalog audit-only gate pass; catalog baseline is now 25 issues with 0 blockers and 25 high issues, and all remaining cross-Cell internal import blocker messages are 0. |
| CD-01 | P1 | `chief_engineer.blueprint.internal.ce_consumer` consumes `control_plane.run_ledger`, `control_plane.verifier_policy`, and `director.tasking` through public contracts, but the CE blueprint Cell declarations did not name those dependencies. | `chief_engineer.blueprint` now declares the control-plane ledger, verifier policy, and Director tasking dependencies in both `cell.yaml` and catalog SSoT. The per-Cell manifest was also brought back in line with the catalog's existing CE runtime dependencies/effects to avoid a second local truth. | Closed | YAML parse checks, `git diff --check`, and catalog audit-only gate pass; catalog baseline is now 22 issues with 0 blockers and 22 high issues, and the three `chief_engineer.blueprint imports ... but does not declare it` messages are 0. |
| CD-02 | P1 | `director.task_consumer.internal.director_consumer` still validates CE handoff through `chief_engineer.blueprint.public` and appends convergence evidence through `control_plane.run_ledger.public`, but the deprecated consumer Cell only declared task-market and roles-adapter dependencies. | `director.task_consumer` now declares the still-active CE handoff and Run Ledger dependencies in both `cell.yaml` and catalog SSoT, while leaving the deprecated-cell removal as a separate future convergence item. | Closed | YAML parse checks, `git diff --check`, and catalog audit-only gate pass; catalog baseline is now 20 issues with 0 blockers and 20 high issues, and the two `director.task_consumer imports ... but does not declare it` messages are 0. |
| CD-03 | P1 | `director.tasking` uses `control_plane.run_ledger.public.stable_hash` for execution profile/envelope identity and `qa.audit_verdict.public.contracts` for repair-routing failure classification, but those dependencies were not declared. | `director.tasking` now declares the Run Ledger and QA verdict Cells in both `cell.yaml` and catalog SSoT, matching the tasking Cell's execution-contract and repair-routing responsibilities. | Closed | YAML parse checks, `git diff --check`, and catalog audit-only gate pass; catalog baseline is now 18 issues with 0 blockers and 18 high issues, and the two `director.tasking imports ... but does not declare it` messages are 0. |
| CD-04 | P1 | `factory.pipeline.internal.factory_stage_executor` reads Director Runtime public repair catalog/coverage contracts, but `factory.pipeline` did not declare `director.runtime` as a dependency. The local `cell.yaml` was also behind the catalog dependency/effect facts. | `factory.pipeline` now declares `director.runtime` in catalog SSoT, and the local manifest dependency/effect list is brought back in line with the existing catalog facts plus the Director Runtime dependency. | Closed | YAML parse checks, `git diff --check`, and catalog audit-only gate pass; catalog baseline is now 17 issues with 0 blockers and 17 high issues, and the `factory.pipeline imports director.runtime but does not declare it` message is 0. |
| CD-05 | P1 | Orchestration QA/evidence code appends Run Ledger events or builds QA verdict failure classifications through public contracts, but `orchestration.pm_dispatch`, `orchestration.workflow_runtime`, and `orchestration.workflow_activity` did not declare those dependencies. | PM dispatch and workflow runtime now declare `control_plane.run_ledger`; workflow activity now declares `qa.audit_verdict`. The change is limited to the dependencies proven by current import evidence. | Closed | YAML parse checks, `git diff --check`, and catalog audit-only gate pass; catalog baseline is now 14 issues with 0 blockers and 14 high issues, and the three orchestration QA/evidence missing dependency messages are 0. |
| CD-06 | P1 | `qa.audit_verdict.internal.qa_consumer` appends QA terminal/requeue gate evidence to `control_plane.run_ledger.public`, but QA verdict did not declare the Run Ledger dependency. | `qa.audit_verdict` now declares `control_plane.run_ledger` in both catalog SSoT and local manifest. The local manifest was also synced with the catalog's existing QA dialogue/runtime dependencies so QA verdict has one dependency truth. | Closed | YAML parse checks, `git diff --check`, and catalog audit-only gate pass; catalog baseline is now 13 issues with 0 blockers and 13 high issues, and the `qa.audit_verdict imports control_plane.run_ledger but does not declare it` message is 0. |
| CD-07 | P1 | `runtime.projection` reads Run Ledger and QA verdict public projections while building runtime/UI status payloads, but those read-model dependencies were not declared. | `runtime.projection` now declares `control_plane.run_ledger` and `qa.audit_verdict` in both local manifest and catalog SSoT, preserving projection as a read-only aggregation layer with explicit upstream contracts. | Closed | YAML parse checks, `git diff --check`, and catalog audit-only gate pass; catalog baseline is now 11 issues with 0 blockers and 11 high issues, and the two `runtime.projection imports ... but does not declare it` messages are 0. |
| CD-08 | P1 | `resident.autonomy` exposes AGI evidence/capability views backed by Director Runtime repair catalog/coverage, Director tasking execution profiles, and runtime artifact resolution, but those upstream Cells were not declared. The local manifest was also behind the catalog's resident dependency truth. | `resident.autonomy` now declares `director.runtime`, `director.tasking`, and `runtime.artifact_store` in both local manifest and catalog SSoT. The dependency is constrained to read/evidence/advisory capabilities; it does not grant AGI direct repair execution, write authority, or Director bypass authority. | Closed | YAML parse checks, `git diff --check`, and catalog audit-only gate pass; catalog baseline is now 8 issues with 0 blockers and 8 high issues, and the three `resident.autonomy imports ... but does not declare it` messages are 0. |
| CD-09 | P1 | `roles.adapters.internal.director.adapter` reads Chief Engineer blueprint persistence and strict Director handoff validation through `chief_engineer.blueprint.public`, but `roles.adapters` did not declare the CE blueprint dependency. | `roles.adapters` now declares `chief_engineer.blueprint` in both local manifest and catalog SSoT, making the PM → Chief Engineer → Director handoff evidence dependency explicit without granting adapters ownership of CE blueprint generation. | Closed | YAML parse checks, `git diff --check`, and catalog audit-only gate pass; catalog baseline is now 7 issues with 0 blockers and 7 high issues, and the `roles.adapters imports chief_engineer.blueprint but does not declare it` message is 0. |
| CD-10 | P1 | `roles.kernel` had three remaining high findings: production task-boundary code appends/evaluates Run Ledger verdicts without a declared dependency; a kernel test imported QA verdict public helpers to assert QA behavior; and the context gateway imported `resident.autonomy` as a fallback despite the provider-injection contract. | `roles.kernel` now declares only the production `control_plane.run_ledger` dependency. QA envelope assertions were removed from the kernel test because QA classification is covered in the QA Cell. Resident AGI capability and decision-trace data now flow through the existing `ContextGatewayConfig` provider from the current role-turn context/audit pack, removing the kernel-to-Resident reverse import. | Closed | `py_compile`, targeted `ruff`, targeted pytest for runtime context-gateway wiring, roles-kernel execution ledger, and context gateway integration, plus catalog audit-only gate pass; catalog baseline is now 4 issues with 0 blockers and 4 high issues, and all three `roles.kernel imports ... but does not declare it` messages are 0. |
