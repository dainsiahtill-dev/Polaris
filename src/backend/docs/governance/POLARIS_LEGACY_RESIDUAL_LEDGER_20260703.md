# Polaris Legacy Residual Ledger - 2026-07-03

Status: Active intake  
Scope: Polaris meta-platform production source, excluding generated packs, tests, fixtures, and historical docs unless they expose a runtime surface.

This ledger records the residual convergence pass after the Execution Control Plane,
Director runtime repair-kernel, task-boundary, QA verdict, and tool-lifecycle hardening
work. It exists because the older convergence ledger is closed intake and cannot be used
as the live work queue for new findings.

## Operating Invariants

- Do not delete by keyword. Every item must be classified as runtime debt, compatibility migration, domain vocabulary, or test-only counterexample before edits.
- Close one item per commit unless a group shares the same contract and test surface.
- A closed item must include a negative scan for the retired symbol/path/field and a focused test or type check.
- Generated descriptor packs are refreshed only in a deliberate descriptor wave; stale generated text alone is not runtime debt.
- Existing dirty changes from other Agents are not included in these commits.

## Closed In This Pass

| ID | Layer | Problem | Closure | Verification |
| --- | --- | --- | --- | --- |
| LR-01 | QA verdict | `KERNELONE_QA_VERDICT_ENGINE_MODE=legacy` disabled the QA verdict engine, allowing a stale config token to bypass the new verdict path. | Removed `legacy` as an off switch; unknown values now fall back to shadow audit. | `rtk pytest src/backend/polaris/cells/qa/audit_verdict/internal/tests/test_qa_consumer.py src/backend/polaris/cells/qa/audit_verdict/internal/tests/test_verdict_engine.py -q`; `rtk ruff check`; `rtk mypy`. |
| LR-02 | Director adapter | Disabled text/PATCH fallback errors used `legacy_*` error codes, polluting evidence even though the path was fail-closed. | Renamed error/protocol codes to `text_tool_protocol_disabled` and `patch_file_protocol_disabled`. | `rtk pytest src/backend/polaris/cells/roles/adapters/tests/test_director_realtime_file_events.py -q`; negative source scan. |
| LR-03 | LLM tool runtime | Role tool integrations still emitted a `legacy_text_tool_protocol_disabled` protocol violation for text-wrapped tool blocks. | Renamed the guard to native-tool-only terminology and aligned the protocol violation with Director adapter errors. | `rtk pytest src/backend/polaris/cells/llm/tool_runtime/tests/test_role_integrations.py src/backend/polaris/tests/architecture/test_tool_calling_canonical_gate.py -q`; negative source scan. |
| LR-04 | Director planning | `director.planning.internal.director_logic.py` retained a simplified write gate and `_dl_*` aliases beside canonical `director_logic_rules`. | Deleted the retired module and removed `_dl_*` package-root exports. | `rtk pytest src/backend/polaris/tests/test_director_logic.py src/backend/polaris/tests/unit/cells/test_director/execution/test_logic.py -q`; negative import scan. |
| LR-05 | CE blueprint | `enable_director_pool` and `director_pool_assignment` names implied CE still managed Director execution when the actual path is ADRStore persistence plus TaskMarket handoff. | Renamed to `enable_adr_blueprint_store` and `director_execution_assignment`. | `rtk pytest src/backend/polaris/cells/chief_engineer/blueprint/internal/tests/test_ce_consumer.py src/backend/polaris/cells/chief_engineer/blueprint/tests/test_ce_consumer_integration.py src/backend/polaris/cells/chief_engineer/blueprint/tests/test_director_pool.py -q`; negative source scan. |
| LR-06 | Storage layout | `save_persisted_settings` still dual-wrote the old home settings file, creating a second settings fact source. | Stopped dual-writing the migration path; retained one-way read migration into canonical global settings. | `rtk pytest src/backend/polaris/cells/storage/layout/tests/test_storage_layout_cell.py src/backend/polaris/tests/test_workspace_settings_sync.py src/backend/polaris/tests/test_llm_phase0_regression.py -q -k "settings"`; negative source scan. |
| LR-07 | Log pipeline | Log pipeline compatibility projection used `LEGACY_CHANNEL_MAPPING`, `normalize_legacy_event`, `to_legacy_projection`, and `legacy_*` model fields, making compatibility input look like a second event fact source. | Renamed the layer to `COMPAT_CHANNEL_MAPPING`, `normalize_compat_event`, `to_compat_projection`, and `compat_*` fields while keeping canonical event writing behavior. | `rtk pytest src/backend/polaris/tests/infrastructure/log_pipeline/test_canonical_event.py -q`; `rtk ruff check`; `rtk mypy`; negative source scan. |
| LR-08 | Workflow embedded bridge | Embedded workflow child payloads and result wrappers still used `mode == "legacy"` as the non-DAG compatibility contract, leaving old terminology in the runtime boundary. | Renamed the compatibility mode to `compat` across the embedded workflow APIs, KernelOne workflow contract parser, PM activity summaries, and focused tests. | `rtk pytest src/backend/polaris/tests/orchestration/test_workflow_engine.py src/backend/polaris/tests/unit/cells/orchestration/workflow_runtime/internal/test_embedded_api.py src/backend/polaris/tests/unit/cells/orchestration/workflow_runtime/internal/test_workflow_client.py src/backend/polaris/kernelone/workflow/tests src/backend/polaris/tests/test_kernelone_safety_regressions.py -q -k "workflow_contract or compat or legacy or from_payload or unwrap_workflow_result or workflow_client"`; `rtk ruff check`; `rtk mypy`; negative source scan. |
| LR-09 | PM dispatch projection | Active PM dispatch metadata still published `legacy_shadow_normalized`, and the cell-local registry used `legacy_id` as an alternate task identity. | Renamed the publish marker to `task_market_contract_normalized` and the external task identity field to `source_task_id` inside pm_dispatch. | `rtk pytest src/backend/polaris/cells/orchestration/pm_dispatch/tests/test_shangshuling_registry.py src/backend/polaris/tests/test_pm_dispatch_shangshuling_registry.py src/backend/polaris/cells/orchestration/pm_dispatch/tests/test_dispatch_pipeline.py src/backend/polaris/tests/unit/cells/orchestration/pm_dispatch/internal/test_dispatch_pipeline.py src/backend/polaris/tests/unit/cells/orchestration/pm_dispatch/internal/test_pm_task_utils.py -q`; `rtk ruff check`; `rtk mypy`; negative source scan. |
| LR-10 | Delivery PM CLI import | Delivery-layer PM import code used `legacy_id`, `legacy_task`, `legacy_sync`, and `sync_from_legacy_tasks` while importing source payloads into canonical PM task rows. | Renamed the import boundary to `source_task_id`, `source_task`, `source_task_sync`, and `sync_from_source_tasks`; updated loop-director and Director node selectors to use source-task terminology. | `rtk pytest src/backend/polaris/tests/unit/delivery/cli/test_pm_cli.py -q`; `rtk ruff check`; `rtk mypy`; negative source scan. |
| LR-11 | Factory resume evidence | Director-only Factory resume helpers used `legacy_task_dirs` / `legacy_taskboard` names while rehydrating source task-directory evidence. | Renamed the helpers and tests to `source_task_dirs` / `source_taskboard` without changing resume selection behavior. | `rtk pytest src/backend/polaris/tests/unit/delivery/http/routers/test_factory_v2.py -q -k "director_resume or source_taskboard"`; `rtk ruff check`; `rtk mypy`; negative source scan. |
| LR-12 | Role runtime task-ref guard | Role runtime contract validation intentionally rejected retired task-market ref shapes through `_is_legacy_task_market_task_ref`, leaving old terminology in the fail-closed guard. | Renamed the helper/test to `retired_task_market_task_ref` while preserving the active `runtime.task_market:task:<task_id>` requirement. | `rtk pytest src/backend/polaris/cells/roles/runtime/public/tests/test_role_runtime_object_contracts.py -q -k "retired_task_ref or task_ref_shape or task_market"`; `rtk ruff check`; `rtk mypy`; negative source scan. |
| LR-13 | Audit diagnosis script projection | `audit.diagnosis` public/internal toolkit exposed `to_legacy_result` and `"legacy"` stats payloads for script-friendly flattened output. | Renamed the public projection to `to_script_projection`, renamed the stats field to `script_projection`, and updated the delivery audit agent import. | `rtk pytest src/backend/polaris/tests/audit/test_audit_service.py::test_to_script_projection_flattens_errors -q`; `rtk ruff check`; `rtk mypy`; negative source scan. Full `test_audit_service.py` currently has unrelated `Audit store factory not registered` failures. |
| LR-14 | CLI retired command host | Active CLI routing still named the fail-closed retired command handler `_route_retired_legacy` and emitted `legacy test window` / `old CLI host` text. | Renamed the handler and user/test wording to retired-command terminology while preserving fail-closed dispatch for retired aliases. | `rtk pytest src/backend/polaris/tests/unit/delivery/cli/test_cli_commands.py -q -k "retired_aliases"`; `rtk ruff check`; `rtk mypy`; negative source scan. |
| LR-15 | CLI backend alias descriptions | CLI parser/help text still described textual/rich console backend aliases and test-window compatibility with legacy terminology, even though runtime treats them as deprecated/retired compatibility paths. | Renamed parser/help descriptions to deprecated alias / retired surface terminology without changing accepted choices or runtime warning behavior. | `rtk pytest src/backend/polaris/tests/unit/delivery/cli/test_cli_commands.py -q -k "parser or test_window or backend"`; `rtk ruff check`; `rtk mypy`; negative source scan. |
| LR-16 | Director v2 CLI state override | `director_v2.py` accepted `--state`, ignored it at runtime, and logged that Director state is derived elsewhere, leaving a misleading second state input. | Removed the ignored `--state` parser option and state parameter from `run_director`; added parser/signature tests so runtime services remain the only Director state source. | `rtk pytest src/backend/polaris/tests/unit/delivery/cli/test_director_cli.py -q -k "director_v2"`; `rtk ruff check`; `rtk mypy`; negative source scan. |
| LR-17 | Retired host test-window command | `polaris_cli.py` still exposed and executed the retired `test-window` command directly through `_run_test_window`, bypassing the canonical router fail-closed path. | Removed the direct `test-window` parser and dispatch branch from the retired host; canonical router still parses retired aliases and rejects them explicitly. | `rtk pytest src/backend/polaris/tests/delivery/cli/test_polaris_cli.py src/backend/polaris/tests/unit/delivery/cli/test_cli_commands.py -q -k "test_window or polaris_parser"`; `rtk ruff check`; `rtk mypy`; negative source scan. |
| LR-18 | Package-level CLI host export | `polaris.delivery.cli.create_parser/main` still lazy-loaded `polaris_cli.py`, making the retired host the package-level API despite `__main__.py` being the canonical CLI host. | Repointed package-level lazy exports to `polaris.delivery.cli.__main__` and added a regression test that package `create_parser()` exposes canonical subcommands. | `rtk pytest src/backend/polaris/tests/unit/delivery/cli/test_cli_commands.py -q -k "package_create_parser or parser_has_required"`; `rtk ruff check`; `rtk mypy`; negative source scan. |
| LR-19 | Retired `polaris_cli.py` direct main | Direct `polaris_cli.main()` still owned its own parser/dispatch path for chat/status/workflow even after package-level exports moved to `__main__.py`. | Changed `polaris_cli.main()` to delegate to canonical `polaris.delivery.cli.__main__.main()`; updated direct-module tests to assert console delegation and retired alias fail-closed behavior through the canonical router. | `rtk pytest src/backend/polaris/tests/test_polaris_cli.py -q`; `rtk ruff check`; `rtk mypy`; negative source scan. |
| LR-20 | Retired `polaris_cli.py` parser/dispatch implementation | `polaris_cli.py` still defined its own chat/status/workflow parser and `_run_*` dispatch helpers even after `main()` became a canonical wrapper. | Reduced `polaris_cli.py` to a no-execution compatibility module: `create_parser()` and `main()` delegate to `__main__.py`, helper functions remain for historical imports, and old `_dispatch` / `_run_*` execution functions were removed. | `rtk pytest src/backend/polaris/tests/delivery/cli/test_polaris_cli.py src/backend/polaris/delivery/cli/tests/test_cli_log_level_option.py src/backend/polaris/tests/unit/delivery/cli/test_cli_commands.py -q -k "polaris_cli or PolarisCli or log_level"`; `rtk ruff check`; `rtk mypy`; negative source scan. |
| LR-21 | Old `cli_router.CliRouter` no-handler success | The old argparse router still parsed `test-window` and returned success when a parsed command had no registered handler, creating a silent acknowledged-but-not-executed path. | Removed `test-window` from the old router parser and changed missing-handler dispatch to fail closed with exit code 1. | `rtk pytest src/backend/polaris/delivery/cli/tests/test_cli_router.py src/backend/polaris/tests/unit/delivery/cli/test_cli_commands.py -q -k "CliRouter or cli_router or router_parser or no_handler or test_window"`; `rtk ruff check`; `rtk mypy`; negative source scan. |
| LR-22 | Console backend alias fallback | The canonical CLI still accepted retired `textual` / `rich` backend aliases and `run_role_console()` silently downgraded unsupported backend values to plain output. | Reduced parser choices to `auto` / `plain` and changed direct console invocation with unsupported backend tokens to fail fast with `ValueError`. | `rtk pytest src/backend/polaris/tests/unit/delivery/cli/test_cli_commands.py src/backend/polaris/delivery/cli/tests/test_cli_router.py src/backend/polaris/delivery/cli/tests/test_terminal_console.py -q -k "backend_aliases or console_subcommand_has_expected_flags or chat_subcommand_has_expected_arguments"`; `rtk ruff check`; `rtk mypy`; negative source scan. |
| LR-23 | Workspace docs migration | Docs-init still generated retired flat docs (`docs/00_overview.md`, `docs/10_requirements.md`, `docs/40_quality.md`) and retired `docs/.polaris.json`, while runtime artifact goals preferred the retired overview before canonical `docs/product/requirements.md`. | Docs-init now generates only canonical `docs/product/*` templates and the HTTP apply allowlist rejects retired flat docs; runtime goal loading prefers product requirements and keeps retired overview only as read-only migration fallback; PM diagnostics label now says `retired_requirements_fallback`. | `rtk pytest src/backend/polaris/cells/workspace/integrity/tests/test_workspace_service.py src/backend/polaris/tests/test_docs_template_quality.py src/backend/polaris/tests/unit/delivery/http/routers/test_docs_v2.py src/backend/polaris/tests/unit/cells/runtime/artifact_store/internal/test_artifacts_docs_goals.py -q -k "docs_templates or docs_init_preview_jetstream or retired_flat or retired_docs_metadata or load_goals"`; `rtk ruff check`; `rtk mypy`; negative source scan. |
| LR-24 | Docs-init HTTP route aliases | Frontend `DocsInitDialog` still called `/docs/init/*` and backend kept non-v2 `/docs/init/dialogue`, `/docs/init/suggest`, `/docs/init/preview`, and `/docs/init/apply` aliases executing the same business logic as v2 routes. | Frontend docs-init dialogue/preview/apply calls now use `/v2/docs/init/*`; backend non-v2 docs-init aliases are removed so retired paths return 404; old integration tests now assert alias routes are not registered. | `rtk pytest src/backend/polaris/tests/integration/delivery/routers/test_docs_router.py src/backend/polaris/tests/unit/delivery/http/routers/test_docs_v2.py src/backend/polaris/tests/unit/delivery/http/routers/test_v2_error_paths.py -q -k "docs_init or retired_docs_init_alias"`; `rtk npm test -- src/frontend/src/app/components/DocsInitDialog.test.tsx src/frontend/src/hooks/useNDJSONStream.test.ts`; `rtk ruff check`; `rtk mypy`; negative source scan. |
| LR-25 | Misc HTTP route aliases | `agents`, `lancedb`, `memos`, and `ollama` routers still registered non-v2 deprecated aliases (`/agents/*`, `/lancedb/status`, `/memos/list`, `/ollama/*`) that executed the same business logic as the canonical v2 routes; frontend service wrappers still called those aliases, and endpoint policy also treated retired `/memos/list` as bootstrap. | Removed the non-v2 aliases, moved frontend service wrappers to `/v2/*`, removed `/memos/list` from bootstrap policy, and converted old integration tests into 404 negative contracts while existing v2 tests remain the behavior authority. | `rtk pytest src/backend/polaris/tests/integration/delivery/routers/test_agents_router.py src/backend/polaris/tests/integration/delivery/routers/test_lancedb_router.py src/backend/polaris/tests/integration/delivery/routers/test_memos_router.py src/backend/polaris/tests/integration/delivery/routers/test_ollama_router.py src/backend/polaris/tests/unit/delivery/http/routers/test_agents_v2.py src/backend/polaris/tests/unit/delivery/http/routers/test_lancedb_v2.py src/backend/polaris/tests/unit/delivery/http/routers/test_memos_v2.py src/backend/polaris/tests/unit/delivery/http/routers/test_ollama_v2.py src/backend/polaris/tests/unit/delivery/http/test_endpoint_policy.py -q`; `rtk ruff check`; `rtk mypy`; `rtk npm test -- src/frontend/src/api.test.ts`; negative source scan. |
| LR-26 | Runtime action aliases | Runtime action routes still exposed `/runtime/clear` and `/runtime/reset-tasks`, and frontend `LogViewer` / `runtimeService.resetTasks()` still called those non-v2 aliases despite canonical `/v2/runtime/clear` and `/v2/runtime/reset/tasks` behavior tests. | Frontend action calls now use `/v2/runtime/*`; backend removed only the retired clear/reset action aliases; integration tests assert those old paths return 404 while v2 runtime tests remain the behavior authority. | `rtk pytest src/backend/polaris/tests/integration/delivery/routers/test_runtime_router.py src/backend/polaris/tests/unit/delivery/http/routers/test_runtime_v2.py src/backend/polaris/tests/test_runtime_maintenance_api.py -q -k "runtime_clear or runtime_reset_tasks or retired_runtime"`; `rtk npm test -- src/frontend/src/services/__tests__/runtimeService.test.ts`; `rtk ruff check`; `rtk mypy`; negative source scan. |
| LR-27 | Arsenal HTTP route aliases | Arsenal kept non-v2 aliases for vision, scheduler, code map/search/index, MCP status, and Director capabilities; `SystemServicesTab` and `ArsenalPanel` still called those retired aliases even though `/arsenal/v2/*` tests already existed. | Frontend Arsenal calls now target `/arsenal/v2/*`; backend removed the non-v2 aliases; old integration router tests now assert aliases return 404 while v2 tests remain the behavior authority and helper tests stay intact. | `rtk pytest src/backend/polaris/tests/integration/delivery/routers/test_arsenal_router.py src/backend/polaris/tests/unit/delivery/http/routers/test_arsenal_v2.py -q`; `rtk npm test -- src/frontend/src/app/components/SystemServicesTab.test.tsx`; `rtk ruff check`; `rtk mypy`; negative source scan. |
| LR-28 | Factory run HTTP route aliases | Factory still registered seven non-v2 `/factory/runs*` aliases that executed the same core logic as canonical `/v2/factory/runs*`; source scan found no active frontend or test caller for the retired aliases. | Removed the retired Factory run aliases; existing v2 Factory router tests remain the behavior authority, and integration tests now assert the old `/factory/runs*` paths return 404. | `rtk pytest src/backend/polaris/tests/integration/delivery/routers/test_factory_router.py -q`; `rtk ruff check`; `rtk mypy`; negative source scan. |
| LR-29 | Logs HTTP route aliases | Logs still registered non-v2 `/logs/query`, `/logs/user-action`, and `/logs/channels` aliases; frontend `logsV2Service` also pointed at a non-existent `/v2/logs/*` namespace instead of the router's canonical `/logs/v2/*` namespace. | Removed the retired Logs aliases; `logsV2Service` now calls `/logs/v2/*`; integration tests cover canonical behavior and assert old `/logs/*` aliases return 404. | `rtk pytest src/backend/polaris/tests/integration/delivery/routers/test_logs_router.py src/backend/polaris/tests/unit/delivery/http/routers/test_logs_v2.py src/backend/polaris/tests/test_log_pipeline_storage_layout.py -q`; `rtk npm test -- src/frontend/src/api.test.ts`; `rtk ruff check`; `rtk mypy`; negative source scan. |
| LR-30 | Memory HTTP route aliases | Memory still registered non-v2 `/memory/state` and `/memory/memories/{id}` aliases; frontend `memoryV2Service` pointed at a non-existent `/v2/memory/*` namespace instead of the router's canonical `/memory/v2/*` namespace. | Removed the retired Memory aliases; `memoryV2Service` now calls `/memory/v2/*`; integration tests cover canonical behavior and assert old `/memory/*` aliases return 404. | `rtk pytest src/backend/polaris/tests/integration/delivery/routers/test_memory_router.py src/backend/polaris/tests/unit/delivery/http/routers/test_memory_v2.py -q`; `rtk npm test -- src/frontend/src/api.test.ts`; `rtk ruff check`; `rtk mypy`; negative source scan. |
| LR-31 | Primary probe deprecation mislabel | `primary.py` documents `/health`, `/ready`, and `/live` as officially supported non-v2 probes, but their route decorators were marked `# DEPRECATED`, causing debt scans to classify supported load-balancer/orchestrator probes as legacy aliases. | Removed the erroneous deprecated comments without changing routes or behavior; v2 system health routes remain separate enhanced authenticated endpoints. | `rtk pytest src/backend/polaris/tests/integration/delivery/routers/test_primary_router.py src/backend/polaris/tests/unit/delivery/http/routers/test_primary_v2.py src/backend/polaris/tests/delivery/http/test_routers.py -q -k "health or ready or live"`; `rtk ruff check`; `rtk mypy`; negative source scan. |
| LR-32 | Provider HTTP route aliases | Providers still registered non-v2 `/llm/providers*` aliases that mirrored canonical `/v2/llm/providers*`; active frontend and workflow callers already used v2, leaving only old router tests on the aliases. | Removed the retired provider aliases; integration tests now exercise `/v2/llm/providers*` behavior and assert old `/llm/providers*` paths return 404. | `rtk pytest src/backend/polaris/tests/integration/delivery/routers/test_providers_router.py src/backend/polaris/tests/unit/delivery/http/routers/test_providers_v2.py src/backend/polaris/tests/integration/delivery/test_workflows.py -q -k "provider or llm"`; `rtk ruff check`; `rtk mypy`; negative source scan. |
| LR-33 | Interview action HTTP route aliases | Interview actions still registered non-v2 `/llm/interview/{ask,save,cancel}` aliases and frontend validation/types still treated those retired paths as supported endpoints, while active product calls already used `/v2/llm/interview/*`. | Removed the retired backend aliases; frontend validation/type contracts and command-log text now use `/v2/llm/interview/*`; integration tests assert old action paths return 404. | `rtk pytest src/backend/polaris/tests/integration/delivery/routers/test_interview_router.py src/backend/polaris/tests/unit/delivery/http/routers/test_interview_v2.py -q`; `rtk npm test -- src/frontend/src/app/types/__tests__/api-types.test.ts src/frontend/src/app/utils/__tests__/apiValidation.test.ts`; `rtk ruff check`; `rtk mypy`; negative source scan. |
| LR-34 | Permissions nested v2 route aliases | The permissions router prefix is already `/v2/permissions`, but it also registered nested `/v2/permissions/v2/*` aliases and frontend/evidence helpers called the nested paths, while canonical `/v2/permissions/*` routes already existed. | Removed the nested backend aliases; removed erroneous deprecated labels from canonical `/v2/permissions/*` handlers; frontend service and Electron evidence probes now call `/v2/permissions/*`; tests assert nested aliases return 404. | `rtk pytest src/backend/polaris/tests/integration/delivery/routers/test_permissions_router.py src/backend/polaris/tests/unit/delivery/http/routers/test_permissions_v2.py -q`; `rtk npm test -- src/frontend/src/api.test.ts`; `rtk ruff check`; `rtk mypy`; negative source scan. |
| LR-35 | LLM test HTTP route aliases | LLM readiness tests still registered non-v2 `/llm/test`, `/llm/test/{id}`, and `/llm/test/{id}/transcript` aliases even though frontend callers and v2 tests already used `/v2/llm/test*`. | Converted old handlers into private helpers behind `/v2/llm/test*`; old non-v2 report/test/transcript paths now return 404. | `rtk pytest src/backend/polaris/tests/integration/delivery/routers/test_llm_tests_router.py src/backend/polaris/tests/unit/delivery/http/routers/test_tests_v2.py -q`; `rtk ruff check`; `rtk mypy`; negative source scan. |
| LR-36 | File read HTTP route alias | File reading still exposed deprecated `/files/read`, and production frontend services/components still called that non-v2 path while `/v2/files/read` already existed. | Removed the deprecated backend alias; production frontend file-read callers now use `/v2/files/read`; behavior tests use v2 and assert `/files/read` returns 404. | `rtk pytest src/backend/polaris/tests/integration/delivery/routers/test_files_router.py src/backend/polaris/tests/unit/delivery/http/routers/test_files_v2.py src/backend/polaris/tests/delivery/http/test_routers.py -q -k "File or files_read or file_read"`; `rtk npm test -- src/frontend/src/api.test.ts src/frontend/src/services/__tests__/fileService.test.ts`; `rtk ruff check`; `rtk mypy`; negative source scan. |
| LR-37 | Runtime bootstrap HTTP route aliases | Runtime storage-layout and migration-status still exposed non-v2 `/runtime/storage-layout` and `/runtime/migration-status` aliases; endpoint policy treated several retired storage-layout aliases as bootstrap-sensitive, and internal stress/audit/Electron callers still used the old storage-layout path. | Removed the retired backend aliases; all production, stress, audit, and Electron callers use `/v2/runtime/storage/layout`; endpoint policy only treats the canonical v2 storage-layout path as bootstrap-sensitive; tests assert old storage/migration aliases return 404. | `rtk pytest src/backend/polaris/tests/integration/delivery/routers/test_runtime_router.py src/backend/polaris/tests/unit/delivery/http/routers/test_runtime_v2.py src/backend/polaris/tests/test_runtime_maintenance_api.py src/backend/polaris/tests/unit/delivery/http/test_endpoint_policy.py -q -k "runtime_storage or migration_status or endpoint_policy or retired_runtime"`; `rtk ruff check`; `rtk mypy`; negative source scan. |
| LR-38 | System control-plane HTTP route aliases | System router still exposed non-v2 `/settings`, `/state/snapshot`, and `/app/shutdown` aliases while canonical `/v2/settings`, `/v2/state/snapshot`, and `/v2/app/shutdown` routes already existed; frontend services, Electron probes, stress preflight, and integration tests still called the old paths. | Removed the retired backend aliases; migrated production/frontend/test callers to v2; endpoint policy no longer treats old system paths as bootstrap-sensitive; router and state snapshot tests assert retired aliases return 404. | `rtk pytest src/backend/polaris/tests/integration/delivery/routers/test_system_router.py src/backend/polaris/tests/unit/delivery/http/routers/test_system_v2.py src/backend/polaris/tests/unit/delivery/http/routers/test_state_snapshot_routes.py src/backend/polaris/tests/delivery/http/test_routers.py src/backend/polaris/tests/test_workspace_settings_sync.py src/backend/polaris/tests/test_runtime_maintenance_api.py src/backend/polaris/tests/unit/delivery/http/test_endpoint_policy.py -q -k "settings or state_snapshot or app_shutdown or system_alias or endpoint_policy or shutdown"`; `rtk npm test -- src/frontend/src/api.test.ts src/frontend/src/app/components/director/__tests__/DirectorStrategyPanel.test.tsx`; `rtk ruff check`; `rtk mypy`; negative route scan. |
| LR-39 | Cognitive runtime nested v2 aliases | `cognitive_runtime` has router prefix `/cognitive-runtime` but also registered child paths like `/v2/cognitive-runtime/resolve-context`, creating actual nested aliases such as `/cognitive-runtime/v2/cognitive-runtime/*`; no active caller used those nested aliases. | Removed the nested v2 alias handlers and dead response-model imports; canonical `/cognitive-runtime/*` routes remain the behavior surface; router tests assert a representative nested alias returns 404. | `rtk pytest src/backend/polaris/tests/integration/delivery/routers/test_cognitive_runtime_router.py -q`; `rtk ruff check`; `rtk mypy`; negative source scan. |
| LR-40 | PM management nested v2 aliases | `pm_management` has router prefix `/pm` but also registered child paths like `/v2/pm/tasks`, creating nested aliases such as `/pm/v2/pm/*`; frontend PM diagnostics and service calls still used nested management status/health/init paths. Direct migration to `/v2/pm/status` would collide with the governed PM runtime status route. | Removed all nested `/pm/v2/pm/*` registrations from the prefixed router; kept document/task/requirement management under canonical global `/v2/pm/*`; moved management status/health/init to explicit `/v2/pm/management/*`; migrated frontend and tests; route tests assert nested aliases are unregistered. | `rtk pytest src/backend/polaris/tests/unit/delivery/http/routers/test_pm_management_v2.py src/backend/polaris/tests/delivery/http/test_routers.py -q -k "pm_management or v2_pm or list_tasks_requires_auth or retired_nested"`; `rtk npm test -- src/frontend/src/services/__tests__/pmService.test.ts src/frontend/src/app/components/pm/PMDiagnosticsPanel.test.tsx`; `rtk ruff check`; `rtk mypy`; negative source scan. |
| LR-41 | CE-managed DirectorPool legacy push scheduler | `chief_engineer.blueprint.internal.director_pool` was explicitly marked legacy/deprecated and modeled CE-direct Director assignment/reassignment, while current execution uses TaskMarket pull leasing and Director task consumer conflict detection. No production caller used the old pool, but tests and traceability smoke still preserved the obsolete PM → CE → DirectorPool → QA story. | Removed the legacy DirectorPool module and dedicated pool tests; traceability smoke now validates PM → CE blueprint → Director evidence → QA verdict without old pool scheduling; fault-injection tests no longer exercise old pool split/reassign behavior; rollback guard factory no longer exposes `director_pool_mode`. | `rtk pytest src/backend/polaris/cells/chief_engineer/blueprint/tests/test_event_bus.py src/backend/polaris/tests/unit/cells/chief_engineer/blueprint/internal/test_event_bus.py src/backend/polaris/cells/chief_engineer/blueprint/tests/test_rollback_guard.py src/backend/polaris/tests/test_traceability_contract_smoke.py src/backend/polaris/tests/agent_stress/test_fault_injection_e2e.py -q`; `rtk ruff check`; `rtk mypy`; negative source scan. |
| LR-42 | LLM config/status HTTP route aliases | `delivery.http.routers.llm` still registered executable non-v2 `/llm/config`, `/llm/config/migrate`, `/llm/status`, and `/llm/runtime-status*` routes, while product callers and v2 tests already used `/v2/llm/*`; endpoint policy still treated `/llm/status` as bootstrap-sensitive. | Removed non-v2 LLM route registrations, made v2 handlers the only public HTTP surface, converted old route tests to v2 behavior plus 404 negative coverage, migrated stress probe/docs to `/v2/llm/status`, and removed `/llm/status` from bootstrap endpoint policy. | `rtk pytest src/backend/polaris/tests/integration/delivery/routers/test_llm_router.py src/backend/polaris/tests/unit/delivery/http/routers/test_llm_v2.py src/backend/polaris/tests/unit/delivery/http/test_endpoint_policy.py -q`; `rtk ruff check`; `rtk mypy`; negative route scan. |
| LR-43 | Agent nested v2 HTTP route aliases | `delivery.http.routers.agent` is mounted by the global v2 router under `/v2`, but the router also registered child paths such as `/v2/sessions` and `/v2/turn`, creating executable nested aliases like `/v2/agent/v2/sessions` and `/v2/agent/v2/turn`. | Removed the nested child route registrations and kept a single canonical mounted surface under `/v2/agent/*`; module-level direct router tests still exercise the internal `/agent/*` shape, while full-app v2 tests now assert nested aliases return 404. | `rtk pytest src/backend/polaris/tests/unit/delivery/http/routers/test_agent_v2.py src/backend/polaris/tests/integration/delivery/routers/test_agent_router.py src/backend/polaris/delivery/http/routers/test_agent_router_canonical.py -q`; `rtk ruff check`; `rtk mypy`; negative route scan. |
| LR-44 | Cognitive runtime receipt envelope source | `CognitiveRuntimeService.record_runtime_receipt` accepted `turn_envelope` both as the explicit contract field and as an embedded key inside `payload`, creating two possible envelope sources for one receipt. | Removed the embedded-payload compatibility path; receipt calls now fail closed when `payload` contains reserved `turn_envelope`, forcing callers through `RecordRuntimeReceiptCommandV1.turn_envelope` / the explicit runtime argument. | `rtk pytest src/backend/polaris/cells/factory/cognitive_runtime/tests/test_public_service.py -q -k "receipt or handoff"`; `rtk ruff check`; `rtk mypy`; negative source scan. |
| LR-45 | Generic workflow role-specific stubs | `generic_pipeline_workflow.py` still defined PM/Director/QA compatibility wrapper classes after the real role workflows moved to dedicated modules; the Director/QA wrappers returned simulated success, including a static QA `passed=True`. | Removed the unused role-specific stub wrappers from the generic workflow module; `workflows/__init__.py` continues to expose the real PM/Director/QA workflow classes, and an architecture guard now rejects role-specific stubs inside the generic workflow. | `rtk pytest src/backend/polaris/tests/refactor/test_architecture_guard.py src/backend/polaris/cells/orchestration/workflow_runtime/tests/test_workflow_runtime_models.py -q -k "WorkflowArchitecture or QAWorkflow"`; `rtk ruff check`; `rtk mypy`; negative source scan. |
| LR-46 | Role default tool surface | Director role profiles and the Director anchor still exposed `precision_edit` as a default write tool while the prompt text already marked it deprecated in favor of `edit_blocks`, keeping a brittle edit tool in the model-facing default capability surface. | Removed `precision_edit` from the role-profile SSOT, internal fallback config, and Director anchor; Director now advertises `edit_blocks` / `repo_apply_diff` / normal write tools by default while lower-level tool normalization can still recognize historical `precision_edit` calls. | `rtk pytest src/backend/polaris/cells/roles/profile/tests/test_registry.py src/backend/polaris/cells/roles/profile/tests/test_profile_source_parity.py src/backend/polaris/tests/unit/cells/roles/profile/test_builtin_profiles.py -q -k "director or profile_source_parity or core_role_yaml"`; `rtk ruff check`; `rtk mypy`; negative profile/anchor scan. |
| LR-47 | LLM tool-runtime Director prompt | The disabled text-tool integration prompt still taught Director to call `precision_edit`, so any caller of `DirectorToolIntegration.get_system_prompt()` could reintroduce the deprecated edit tool into model-visible guidance even after role profiles stopped advertising it. | Updated the Director tool prompt to use `edit_blocks` for replacements and added a regression test that the prompt contains `edit_blocks` but not the deprecated tool name; text-wrapped tool execution remains fail-closed. | `rtk pytest src/backend/polaris/cells/llm/tool_runtime/tests/test_role_integrations.py src/backend/polaris/tests/architecture/test_cell_layer_migration.py -q -k "role_integrations or tool_protocol or DirectorToolIntegration or cell_layer"`; `rtk ruff check`; `rtk mypy`; negative source scan. |
| LR-48 | Provider tool schema export | `ToolSpecRegistry.generate_llm_schemas()` exported deprecated tools by default, so `precision_edit` could still appear in final provider request tool schemas even after role profiles and prompt text stopped recommending it. | Added an explicit `include_deprecated` switch and made provider-facing schema generation exclude deprecated tools by default; direct registry lookup and explicit compatibility schema export still recognize historical tools. | `rtk pytest src/backend/polaris/kernelone/tool_execution/tests/test_tool_spec_registry.py src/backend/polaris/cells/llm/tool_runtime/tests/test_role_integrations.py src/backend/polaris/tests/architecture/test_cell_layer_migration.py src/backend/polaris/kernelone/llm/toolkit/tests/test_integration.py -q -k "llm_schema or deprecated or native_function or role_integrations or openai_tool_format or migration_generates_valid_schemas"`; `rtk ruff check`; `rtk mypy`; schema probe. |
| LR-49 | Role kernel model-visible edit guidance | TransactionKernel prompt templates, retry contracts, and decision templates still mentioned `precision_edit`, so deprecated tool names could remain in provider messages even after schema export was hidden. | Removed deprecated tool names from model-visible guidance and recovery ordering; retained compatibility classification sets that only recognize historical tool observations. | `rtk pytest src/backend/polaris/cells/roles/kernel/tests/test_tool_sequence_templates.py src/backend/polaris/cells/roles/kernel/tests/test_transaction_kernel_facade.py -q -k "tool_sequence or retry_forced_write or precision_edit or retry_context or decision_message or forced_write"`; `rtk ruff check`; `rtk mypy`; prompt-template source scan. |
| LR-50 | Tool failure recovery guidance | Failure budget escalation still instructed models to retry deprecated exact-search editing and ranked append/write fallbacks before robust targeted edits. | Replaced the recovery guidance with `edit_blocks` line-range / `edit_file` first, `write_file` only for create/full replacement, and `append_to_file` only for explicit append tasks. | `rtk pytest src/backend/polaris/kernelone/tool_execution/tests/test_failure_budget.py src/backend/polaris/kernelone/tool_execution/tests/test_error_classifier.py -q`; `rtk ruff check`; `rtk mypy`; negative recovery-text scan. |
| LR-51 | Provider schema deprecated recommendation | The default `write_file` provider schema description still recommended the deprecated edit tool for partial modifications, so deprecated text could remain in final provider tool descriptions even though the deprecated schema itself was hidden. | Updated provider-facing schema wording to recommend `edit_blocks` / `edit_file`, and added a regression test that default LLM schemas do not contain the deprecated tool name. | `rtk pytest src/backend/polaris/kernelone/tool_execution/tests/test_tool_spec_registry.py -q -k "deprecated or default_llm_schemas or migration_generates_valid_schemas"`; `rtk ruff check`; `rtk mypy`; schema text probe. |
| LR-52 | ContextOS tool-output projection | Oversized tool results were projected as opaque `[Large output stored in receipt ...]` placeholders without prompt-visible non-failure evidence, so Director repair turns could see only receipt refs instead of actionable write/command summaries. | Added prompt-safe `[tool_output_summary]` projection for oversized non-failure tool results while preserving full raw output in `ReceiptStore`; failure summaries remain unchanged. | `rtk pytest src/backend/polaris/kernelone/context/tests/test_context_os_prompt_safety.py -q -k "tool_role_offload or large_successful_tool_receipt or tool_output_summary or tool_failure_summary or successful_director_policy"`; `rtk ruff check`; `rtk mypy`. |
| LR-53 | Syntax policy deprecated edit API | `SyntaxPolicy` exposed an unused `validate_precision_edit_search()` method and deprecated-tool-specific error wording even after model-facing edit guidance moved to `edit_blocks` / `edit_file`. | Replaced the dead API with generic `validate_exact_search_text()` semantics and added tests that reject syntax-glued exact-search text while the retired method name is no longer public. | `rtk pytest src/backend/polaris/cells/roles/kernel/internal/policy/tests/test_syntax_policy.py -q`; `rtk ruff check`; `rtk mypy`; negative symbol scan. |
| LR-54 | Tool runtime optional handler terminology | `runtime_executor.py` still described optional `tools.main` handler import as a legacy fallback/registration surface, making the KernelOne-native executor look like it had a second old execution fact source. | Renamed the discovery surface to optional external handlers and native list-tools registry entries without changing execution behavior; added a source guard that rejects `legacy` terminology in `runtime_executor.py`. | `rtk pytest src/backend/polaris/tests/unit/kernelone/tool_execution/test_runtime_executor.py -q -k "runtime_executor_source or list_tools or invoke_unknown_tool or accepts_llm_natural"`; `rtk ruff check`; `rtk mypy`; negative source scan. |
| LR-55 | ToolComposer deprecated edit planning | `ToolComposer` still put the deprecated exact-search edit tool first for `file_edit`, and its capability registry included deprecated tool specs by default. | Added `ToolSpecRegistry.get_all_tools(include_deprecated=False)` for planning consumers, filtered ToolComposer capabilities, and changed file-edit planning to prefer `edit_blocks` / `edit_file` / `search_replace` / `repo_apply_diff`; graph examples now use `edit_file`. | `rtk pytest src/backend/polaris/kernelone/tool_execution/tests/test_tool_spec_registry.py src/backend/polaris/kernelone/tool_execution/tests/test_composer.py -q -k "deprecated_specs or deprecated_edit_tool or select_tools"`; `rtk ruff check`; `rtk mypy`; negative composer/graph scan. |
| LR-56 | Role kernel retry deprecated edit selection | Role-kernel retry selection, required-tool equivalence, and forced-write prompt-layer detection could still actively choose or authorize the deprecated exact-search edit tool even after provider schemas and model guidance stopped advertising it. | Removed the deprecated exact-search edit tool from forced-write retry priority, bootstrap existing-content priority, `search_replace` equivalents, and forced-write prompt-layer detection; compatibility recognition remains only for historical observations. | `rtk pytest src/backend/polaris/cells/roles/kernel/tests/test_tool_sequence_templates.py src/backend/polaris/cells/roles/kernel/tests/test_kernel_prompt_builder_integration.py src/backend/polaris/cells/roles/kernel/tests/test_transaction_kernel_facade.py -q -k "forced_write or equivalent_hint or required_tool_equivalents or deprecated_exact_edit or write_verify_contract_hint"`; `rtk ruff check`; `rtk mypy`; negative active-surface scan. |
| LR-57 | Backend tool runtime deprecated discovery | `BackendToolRuntime.list_tools()` still exposed the deprecated exact-search edit tool through its KernelOne-native discovery list, and direct runtime invocation could still route it as a native tool. | Removed the deprecated exact-search edit tool from the native runtime tool list; added tests that list-tools excludes it and direct invocation fails closed as an unknown tool. | `rtk pytest src/backend/polaris/tests/unit/kernelone/tool_execution/test_runtime_executor.py -q -k "deprecated_exact_edit or list_tools or invoke_unknown_tool or runtime_executor_source"`; `rtk ruff check`; `rtk mypy`; negative runtime-executor scan. |
| LR-58 | Write-tool active vs compatibility fact source | The canonical `WRITE_TOOLS` fact source had to keep deprecated exact-edit for historical receipts, but active retry/contract planning also consumed it, letting compatibility tools satisfy future mutation planning. | Added `ACTIVE_WRITE_TOOLS` / `DEPRECATED_WRITE_TOOLS`, kept `WRITE_TOOLS` for observation compatibility, and migrated retry narrowing, retry context, task contract construction, and escalation no-write checks to active write tools. | `rtk pytest src/backend/polaris/cells/roles/kernel/internal/transaction/tests/test_protocol_canonicalization.py src/backend/polaris/cells/roles/kernel/tests/test_tool_sequence_templates.py src/backend/polaris/cells/roles/kernel/tests/test_transaction_kernel_facade.py -q -k "active_write_tools or mutation_retry_tool_definitions or retry_forced_write or equivalent_hint or required_tool_equivalents"`; `rtk ruff check`; `rtk mypy`; negative active-planning scan. |
| LR-59 | PhaseManager local write-tool set | `phase_manager.py` maintained a local `_WRITE_TOOLS` set and phase write whitelist that still admitted deprecated exact-edit, drifting from the canonical active/compat write-tool split. | Replaced local write classification with canonical `WRITE_TOOLS` for historical receipt observation and `ACTIVE_WRITE_TOOLS` for future phase whitelists; added a test for observed-but-not-admitted deprecated writes. | `rtk pytest src/backend/polaris/cells/roles/kernel/tests/test_phase_timeout_loop_fix.py src/backend/polaris/cells/roles/kernel/internal/transaction/tests/test_protocol_canonicalization.py -q -k "deprecated_exact_edit or write_tools_single_source or active_write_tools or write_tool_phases"`; `rtk ruff check`; `rtk mypy`; negative phase-manager scan. |
| LR-60 | Tool sequence local edit-tool set | `tool_sequence_templates.py` maintained a local edit/write collection that still included deprecated exact-edit, so deprecated-only required groups could trigger active edit recovery guidance. | Replaced the local edit/write collection with canonical `ACTIVE_WRITE_TOOLS`; deprecated-only contracts now fall through to generic tool-failure guidance instead of active edit recovery. | `rtk pytest src/backend/polaris/cells/roles/kernel/tests/test_tool_sequence_templates.py -q -k "recovery_protocol or General-Mutation or retry_forced_write"`; `rtk ruff check`; `rtk mypy`; negative tool-sequence scan. |
| LR-61 | Stream orchestrator local write-tool set | `stream_orchestrator.py` maintained a local `_WRITE_TOOL_NAMES` receipt-observation set that duplicated canonical write-tool facts and still named deprecated exact-edit directly. | Replaced the local set with canonical `WRITE_TOOLS`, preserving historical write-observation semantics while removing the duplicated list. | `rtk pytest src/backend/polaris/cells/roles/kernel/internal/transaction/tests/test_stream_orchestrator.py src/backend/polaris/cells/roles/kernel/internal/transaction/tests/test_protocol_canonicalization.py -q -k "HasWriteTools or write_tools_single_source"`; `rtk ruff check`; `rtk mypy`; negative stream-orchestrator scan. |
| LR-62 | Session orchestrator local write-tool set | `session_orchestrator.py` maintained a second local write-tool receipt-observation set and named the deprecated exact-edit tool directly, drifting from the canonical active/compat write-tool split. | Replaced the local set with canonical `WRITE_TOOLS`, preserving historical write receipt observation while removing duplicated runtime-state classification data. | `rtk pytest src/backend/polaris/cells/roles/runtime/internal/tests/test_session_orchestrator.py -q -k "canonical_write_tool_observation or read_only_termination_exemption"`; `rtk ruff check`; `rtk mypy`; negative session-orchestrator scan. |
| LR-63 | LLM caller active tool pruning | `tool_helpers.py` still kept the deprecated exact-edit tool in active write/tool-pruning sets, so weak-model materialization or repair-preserve schema narrowing could retain the retired tool if a caller supplied it. | Derived file-parameter write tools and anchored edit tools from canonical `ACTIVE_WRITE_TOOLS`, and filtered `DEPRECATED_WRITE_TOOLS` when an active anchored edit path exists. | `rtk pytest src/backend/polaris/cells/roles/kernel/internal/llm_caller/tests/test_tool_helpers.py -q -k "deprecated_exact_edit or RestrictToolDefinitions"`; `rtk ruff check`; `rtk mypy`; negative llm-caller tool-helper scan. |
| LR-64 | Exploration policy failed-edit local set | `ExplorationToolPolicy` kept a local failed-edit observation set that directly named deprecated exact-edit and only partially overlapped canonical write-tool facts. | Replaced the local list with canonical `WRITE_TOOLS` plus historical replace aliases, preserving diagnostic-read override semantics without duplicating retired tool names. | `rtk pytest src/backend/polaris/cells/roles/kernel/tests/test_exploration_edit_failure_override.py -q`; `rtk ruff check`; `rtk mypy`; negative exploration-policy scan. |
| LR-65 | Context tool-policy fallback mutating set | `_runtime_tool_policy_from_context` used a handwritten fallback mutating-tool blocklist that still named deprecated exact-edit when ToolSpecRegistry was unavailable. | Replaced the fallback with canonical `ACTIVE_WRITE_TOOLS` plus exec tools, so fail-closed context suppression blocks active mutations without projecting retired tools into audit state. | `rtk pytest src/backend/polaris/cells/roles/kernel/internal/kernel/tests/test_role_execution_kernel_contract.py -q -k "context_tool_policy"`; `rtk ruff check`; `rtk mypy`; negative tool-policy scan. |
| LR-66 | Tool batch existing-file guard deprecated name | `_tool_requires_existing_file` hand-maintained an existing-file guard list that directly named deprecated exact-edit even though this is compatibility execution validation. | Replaced the direct retired name with canonical `DEPRECATED_WRITE_TOOLS`, preserving historical existing-file validation while removing local deprecated literals. | `rtk pytest src/backend/polaris/cells/roles/kernel/internal/transaction/tests/test_path_security.py -q -k "deprecated_write_tools or rewrite"`; `rtk ruff check`; `rtk mypy`; negative tool-batch-executor scan. |
| LR-67 | Task contract write-group priority | `task_contract_builder.py` still ranked deprecated exact-edit inside active required-tool-group ordering and did not rank the current `edit_blocks` path. | Removed deprecated exact-edit from the active priority map and added `edit_blocks` as the preferred anchored edit option. | `rtk pytest src/backend/polaris/cells/roles/kernel/tests/test_transaction_kernel_facade.py -q -k "required_groups_prioritize_active_edit_blocks"`; `rtk ruff check`; `rtk mypy`; negative task-contract-builder scan. |
| LR-68 | Director materialization facade write evidence set | `director.runtime.public.service` maintained a local materialization write-tool evidence set and directly named deprecated exact-edit. | Replaced the local set with canonical `WRITE_TOOLS`, preserving historical write evidence projection without duplicating retired tool names in Director public service. | `rtk pytest src/backend/polaris/cells/director/runtime/tests/test_repair_kernel_contract.py -q -k "materialization_quality_facade_runs_schedule"`; `rtk ruff check`; `rtk mypy`; negative director-runtime-service scan. |
| LR-69 | AgentAccel deprecated exact-edit executor handler | `kernelone.llm.toolkit.executor.handlers.repo` still registered and implemented the deprecated exact-search edit tool, so a compatibility schema could still become a real write path if invoked through AgentAccel. | Removed the executor handler registration and dead implementation; deprecated schemas may still be recognized explicitly, but AgentAccel now fail-closes with no handler and leaves files unchanged. | `rtk pytest src/backend/polaris/tests/test_tool_validation.py -q -k "deprecated_exact_edit_tool_has_no_executor_handler"`; `rtk ruff check`; `rtk mypy`; negative executor handler scan. |
| LR-70 | Evaluation judge deprecated edit equivalence | Tool-calling judge/evaluation equivalence still accepted deprecated exact-edit as a positive edit/search substitute and prompt-contract guidance still named it as a mutation option. | Replaced positive equivalence and prompt guidance with `edit_blocks` / active edit tools; deprecated exact-edit may remain only in negative/security fixtures or compatibility tests. | `rtk pytest src/backend/polaris/cells/llm/evaluation/tests/test_tool_calling_matrix_prompt_contract.py -q`; `rtk pytest src/backend/polaris/cells/llm/evaluation/tests/test_validator_integration.py -q -k "only_write_tools_passes"`; `rtk ruff check`; `rtk mypy`; negative evaluation/judge scan. |
| LR-71 | Factory chain forensics write-tool catalog | `chain_forensics.py` hand-maintained a regex of write tools and directly listed the deprecated exact-edit tool, creating a second write-tool fact source in bench diagnostics. | The forensic write detector now derives from KernelOne `WRITE_TOOLS`; compatibility observation is preserved through the canonical catalog, not a local retired-tool literal. | `rtk pytest src/backend/scripts/factory_bench/test_chain_forensics.py -q`; `rtk ruff check src/backend/scripts/factory_bench/chain_forensics.py src/backend/scripts/factory_bench/test_chain_forensics.py`; `rtk mypy src/backend/scripts/factory_bench/chain_forensics.py src/backend/scripts/factory_bench/test_chain_forensics.py`; negative implementation scan. |
| LR-72 | Director execute diagnostic write-tool catalog | `execute_method.py` kept a local diagnostic write-tool set that directly listed the deprecated exact-edit tool, even though it was only observing write evidence. | Diagnostic write evidence now uses KernelOne `WRITE_TOOLS`; the local list is removed and a regression test locks the canonical binding. | `rtk pytest src/backend/polaris/cells/roles/adapters/tests/test_execute_method_write_catalog.py -q`; `rtk ruff check src/backend/polaris/cells/roles/adapters/internal/director/execute_method.py src/backend/polaris/cells/roles/adapters/tests/test_execute_method_write_catalog.py`; `rtk mypy src/backend/polaris/cells/roles/adapters/internal/director/execute_method.py src/backend/polaris/cells/roles/adapters/tests/test_execute_method_write_catalog.py`; negative implementation scan. |
| LR-73 | Transaction retry prompt deprecated edit guidance | Transaction retry guidance and facade tests still used the retired exact-edit tool as positive mutation guidance, leaking it back into model-visible recovery prompts. | Retry guidance now describes exact-match edits generically and positive transaction tests use `edit_blocks`; deprecated exact-edit is no longer a positive prompt/contract example. | `rtk pytest src/backend/polaris/cells/roles/kernel/tests/test_transaction_kernel_facade.py -q -k "write_verify_contract_hint or xinzeng_gengxin or required_groups_and_min_calls_hint or retry_contract"`; `rtk ruff check src/backend/polaris/cells/roles/kernel/internal/transaction/task_contract_builder.py src/backend/polaris/cells/roles/kernel/internal/transaction/retry_context_builders.py src/backend/polaris/cells/roles/kernel/tests/test_transaction_kernel_facade.py`; `rtk mypy src/backend/polaris/cells/roles/kernel/internal/transaction/task_contract_builder.py src/backend/polaris/cells/roles/kernel/internal/transaction/retry_context_builders.py src/backend/polaris/cells/roles/kernel/tests/test_transaction_kernel_facade.py`; negative transaction prompt scan. |
| LR-74 | Factory workspace-quality mutation token catalog | Factory workspace-quality repair evidence detection hand-maintained mutation tool names and directly listed the retired exact-edit tool. | Mutation evidence now derives from KernelOne `WRITE_TOOLS` plus repair operation tokens; historical deprecated write receipts remain observable through the canonical catalog. | `rtk pytest src/backend/polaris/cells/factory/pipeline/tests/test_factory_stage_executor_write_catalog.py -q`; `rtk ruff check src/backend/polaris/cells/factory/pipeline/internal/factory_stage_executor.py src/backend/polaris/cells/factory/pipeline/tests/test_factory_stage_executor_write_catalog.py`; `rtk mypy src/backend/polaris/cells/factory/pipeline/internal/factory_stage_executor.py src/backend/polaris/cells/factory/pipeline/tests/test_factory_stage_executor_write_catalog.py`; negative factory executor scan. |
| LR-75 | Tool-calling matrix positive retired edit fixtures | L3/L5 positive tool-calling matrix fixtures still required or accepted the retired exact-edit tool as a success-path mutation option. | Positive edit/write fixtures now require active `edit_blocks`; retired exact-edit remains only in L6 forbidden-tool/security fixtures. | `rtk pytest src/backend/polaris/cells/llm/evaluation/tests/test_tool_calling_matrix_prompt_contract.py -q`; `rtk proxy python polaris/cells/llm/evaluation/fixtures/tool_calling_matrix/validate_cases.py` from `src/backend`; fixture scan confirming remaining retired edit references are forbidden-tool negative cases only. |
| LR-76 | Filesystem write-fragment guidance | `write_file` edit-fragment rejection told models to use `edit_file/precision_edit`, leaking the retired exact-edit tool through an error message. | The guidance now recommends active `edit_blocks/edit_file` and a behavior test asserts the error no longer mentions the retired tool. | `rtk pytest src/backend/polaris/kernelone/llm/toolkit/executor/handlers/tests/test_filesystem_write_guidance.py -q`; `rtk ruff check src/backend/polaris/kernelone/llm/toolkit/executor/handlers/filesystem.py src/backend/polaris/kernelone/llm/toolkit/executor/handlers/tests/test_filesystem_write_guidance.py`; `rtk mypy src/backend/polaris/kernelone/llm/toolkit/executor/handlers/filesystem.py src/backend/polaris/kernelone/llm/toolkit/executor/handlers/tests/test_filesystem_write_guidance.py`; negative implementation scan. |
| LR-77 | Agentic benchmark audit fallback tool list | `agentic_benchmark/audit_cases.py` kept a fallback local valid-tool list that directly named the retired exact-edit tool even though registry loading is the canonical source. | The fallback list no longer names the retired tool; registry loading can still recognize explicit deprecated compatibility specs when available. | `rtk proxy python src/backend/polaris/cells/llm/evaluation/fixtures/agentic_benchmark/audit_cases.py`; `rtk ruff check src/backend/polaris/cells/llm/evaluation/fixtures/agentic_benchmark/audit_cases.py`; `rtk mypy src/backend/polaris/cells/llm/evaluation/fixtures/agentic_benchmark/audit_cases.py`; negative fallback scan. |
| LR-78 | Generic tool-output JSON parser sample | `test_tool_output_json.py` used the retired exact-edit tool as a generic JSON payload sample and still carried an old migrated-module import shim. | Replaced the neutral sample payload with active `edit_blocks` and imported the current `polaris.kernelone.tool_execution.output_json` module directly. | `rtk pytest src/backend/polaris/tests/test_tool_output_json.py -q`; `rtk ruff check src/backend/polaris/tests/test_tool_output_json.py`; `rtk mypy src/backend/polaris/tests/test_tool_output_json.py`; negative parser-test scan. |
| LR-79 | Tree-sitter availability active-tool sample | `test_ts_availability.py` used the retired exact-edit tool as a generic non-Tree-sitter-dependent sample. | Replaced the neutral tool sample with active `edit_blocks`; deprecated-tool coverage stays in explicit compatibility and negative tests. | `rtk pytest src/backend/polaris/kernelone/llm/toolkit/tests/test_ts_availability.py -q`; `rtk ruff check src/backend/polaris/kernelone/llm/toolkit/tests/test_ts_availability.py`; `rtk mypy src/backend/polaris/kernelone/llm/toolkit/tests/test_ts_availability.py`; negative Tree-sitter-availability test scan. |
| LR-80 | Exploration policy edit-failure sample | `test_exploration_edit_failure_override.py` used the retired exact-edit tool as the generic example for targeted edit failure cooldown override. | Replaced the generic failure sample with active `edit_blocks`; canonical write-tool observation still covers historical deprecated receipts through `WRITE_TOOLS`. | `rtk pytest src/backend/polaris/cells/roles/kernel/tests/test_exploration_edit_failure_override.py -q`; `rtk ruff check src/backend/polaris/cells/roles/kernel/tests/test_exploration_edit_failure_override.py`; `rtk mypy src/backend/polaris/cells/roles/kernel/tests/test_exploration_edit_failure_override.py`; negative exploration-test scan. |
| LR-81 | LLM stress integration doc retired edit claim | `TOOLS_INTEGRATION_SUMMARY.md` still documented the retired exact-edit tool as Director's normal code-edit capability and permission example. | Updated the documented edit capability and permission sample to active `edit_blocks`; added a compatibility-only note for the retired exact-edit tool. | `rtk proxy rg -n "precision_edit" src/backend/polaris/tests/llm_stress/TOOLS_INTEGRATION_SUMMARY.md`; Markdown-only change, no runtime test. |
| LR-82 | Tool error classifier/failure-budget generic samples | `error_classifier.py`, `failure_budget.py`, and their generic unit tests used the retired exact-edit tool as the default no-match write-tool example. | Moved generic classifier and failure-budget examples/tests to active `edit_blocks`; deprecated-tool behavior remains covered by explicit registry/compatibility tests. | `rtk pytest src/backend/polaris/kernelone/tool_execution/tests/test_error_classifier.py src/backend/polaris/kernelone/tool_execution/tests/test_failure_budget.py -q`; `rtk ruff check src/backend/polaris/kernelone/tool_execution/error_classifier.py src/backend/polaris/kernelone/tool_execution/failure_budget.py src/backend/polaris/kernelone/tool_execution/tests/test_error_classifier.py src/backend/polaris/kernelone/tool_execution/tests/test_failure_budget.py`; `rtk mypy ...`; negative classifier/failure-budget scan. |
| LR-83 | Transaction protocol deprecated-tool literal | `test_protocol_canonicalization.py` directly named the retired exact-edit tool while checking compatibility write classification, duplicating the canonical deprecated-tool catalog. | Replaced local retired-tool literals with iteration over `DEPRECATED_WRITE_TOOLS`, preserving compatibility assertions without creating a second fact source. | `rtk pytest src/backend/polaris/cells/roles/kernel/internal/transaction/tests/test_protocol_canonicalization.py -q`; `rtk ruff check src/backend/polaris/cells/roles/kernel/internal/transaction/tests/test_protocol_canonicalization.py`; `rtk mypy src/backend/polaris/cells/roles/kernel/internal/transaction/tests/test_protocol_canonicalization.py`; negative protocol-test literal scan. |
| LR-84 | Validate-tool-step deprecated literal | `test_validate_tool_step.py` hard-coded the retired exact-edit tool name while testing deprecated compatibility schema validation. | Replaced the local literal with `DEPRECATED_WRITE_TOOLS` catalog lookup and renamed the test to deprecated exact-edit compatibility. | `rtk pytest src/backend/polaris/tests/polaris/kernelone/tools/test_validate_tool_step.py -q`; `rtk ruff check src/backend/polaris/tests/polaris/kernelone/tools/test_validate_tool_step.py`; `rtk mypy src/backend/polaris/tests/polaris/kernelone/tools/test_validate_tool_step.py`; negative validate-tool-step literal scan. |
| LR-85 | Independent precision-editor implementation | `infrastructure/agent_core/precision_editor.py` was an unreferenced early standalone editor implementation, and `PatchComposer` still emitted `precision_editor_unavailable_or_rejected` as fallback evidence. | Deleted the unused standalone editor and renamed the composer fallback reason to `targeted_editor_unavailable_or_rejected`; remaining `precision_editor.py` mentions are historical docs that already state no independent file should exist. | `rtk pytest src/backend/polaris/cells/director/runtime/tests/test_repair_kernel_contract.py -q -k "patch_composer"`; `rtk pytest src/backend/polaris/cells/director/runtime/tests/test_repair_kernel_precise_edit_and_coverage.py -q -k "patch_composer"`; `rtk ruff check src/backend/polaris/cells/director/runtime/internal/repair_kernel/composer.py`; `rtk mypy src/backend/polaris/cells/director/runtime/internal/repair_kernel/composer.py`; reverse-reference scan. |
| LR-86 | AgentAccel deprecated-handler negative test literal | `test_tool_validation.py` hard-coded the retired exact-edit tool while asserting deprecated tools have no executor handler. | Replaced the local literal with `DEPRECATED_WRITE_TOOLS` lookup, preserving the negative handler assertion without duplicating the deprecated-tool catalog. | `rtk pytest src/backend/polaris/tests/test_tool_validation.py -q -k "deprecated_exact_edit"`; `rtk ruff check src/backend/polaris/tests/test_tool_validation.py`; `rtk mypy src/backend/polaris/tests/test_tool_validation.py`; negative AgentAccel validation literal scan. |
| LR-87 | Agentic benchmark orphan backup cases | `fixtures/agentic_benchmark/cases_backup/` contained 92 tracked duplicate JSON cases that no loader referenced, creating a second stale benchmark fixture surface. | Deleted the orphan backup directory; active agentic benchmark loading remains rooted at `fixtures/agentic_benchmark/cases`. | `rtk pytest src/backend/polaris/tests/test_llm_benchmark_loader.py -q`; `rtk pytest src/backend/polaris/tests/unit/cells/llm/evaluation/fixtures/test_audit_cases.py -q`; `rtk proxy rg -n "cases_backup" src/backend/polaris/cells/llm/evaluation src/backend/polaris/tests src/backend/scripts`; deleted-file count check. |
| LR-88 | CLI top-level chat description | `polaris.delivery.cli.__main__` still described `chat` as "Legacy role chat" even though the parser treats it as a retired alias for `console`. | Updated the top-level CLI description to "Retired role-interaction alias; use console instead" to match the actual parser contract. | `rtk pytest src/backend/polaris/delivery/cli/tests/test_cli_router.py -q -k "chat_subcommand"`; `rtk ruff check src/backend/polaris/delivery/cli/__main__.py`; `rtk mypy src/backend/polaris/delivery/cli/__main__.py`; negative CLI description scan. |
| LR-89 | KernelOne runtime shared-types facade | `polaris.kernelone.runtime.shared_types` is explicitly deprecated, but active PM CLI, LLM provider, permission-gate, process-service, Director planning, and tool-execution code still imported helpers from it, keeping the deprecated facade on the active dependency path. | Migrated active imports to the canonical owners in `polaris.kernelone.shared.path_utils` and `polaris.kernelone.shared.text_utils`; the deprecated module remains only as an external compatibility facade covered by `TestBackwardCompatibility`. | `rtk pytest src/backend/polaris/kernelone/tests/test_shared.py src/backend/polaris/tests/test_kernelone_safety_regressions.py src/backend/polaris/kernelone/tool_execution/tests/test_utils.py -q`; `rtk ruff check` on touched source files; `rtk mypy` on touched source files; negative active-import scan. |
| LR-90 | HTTP app factory legacy wording | The canonical FastAPI gateway still described `api.main` and router module locations with `Legacy` / `legacy paths` wording, causing active gateway scans to classify the canonical router assembly as a legacy route source. | Reworded the comments to `retired compatibility wrapper` and `historical package layout`, preserving the canonical app factory behavior while removing misleading legacy terminology from the active gateway. | `rtk ruff check src/backend/polaris/delivery/http/app_factory.py`; `rtk mypy src/backend/polaris/delivery/http/app_factory.py`; negative app-factory wording scan. |
| LR-91 | Control-flag role wrapper labels | `kernelone.fs.control_flags` documented current PM/Director stop-signal convenience wrappers as `Legacy alias`, making active role stop control look like deprecated compatibility surface even though runtime/process code still consumes those wrappers. | Reworded PM and Director stop-signal helper docstrings as role-scoped wrappers while keeping behavior and public imports unchanged. | `rtk pytest src/backend/polaris/kernelone/fs/tests/test_control_flags_quality.py -q`; `rtk ruff check src/backend/polaris/kernelone/fs/control_flags.py`; `rtk mypy src/backend/polaris/kernelone/fs/control_flags.py`; negative control-flag legacy-label scan. |
| LR-92 | Role-chat JetStream wording | The canonical `/v2/role/{role}/chat/jetstream` endpoint docstring still described itself relative to a `legacy HTTP streaming route` and `legacy role chat path`, even though product realtime is single-rail JetStream/WebSocket. | Reworded the docstring to refer to the removed HTTP streaming route and the current role-chat policy contract without legacy terminology. | `rtk pytest src/backend/polaris/tests/test_role_runtime_chat_router_helper.py -q`; `rtk ruff check src/backend/polaris/delivery/http/routers/role_chat.py`; `rtk mypy src/backend/polaris/delivery/http/routers/role_chat.py`; negative role-chat legacy wording scan. |
| LR-93 | ContextOS schema validation result alias | `context_os.schemas` and the package root still exported the deprecated `ValidationResult` alias beside the explicit `SchemaValidationResult`, preserving two public names for one ContextOS schema validation contract. | Removed the alias, changed validation functions to return/construct `SchemaValidationResult`, and updated tests to assert the old alias is no longer public from either module surface. | `rtk pytest src/backend/polaris/kernelone/context/tests/test_context_os_schemas.py -q`; `rtk ruff check src/backend/polaris/kernelone/context/context_os/schemas.py src/backend/polaris/kernelone/context/context_os/__init__.py src/backend/polaris/kernelone/context/tests/test_context_os_schemas.py`; `rtk mypy src/backend/polaris/kernelone/context/context_os/schemas.py src/backend/polaris/kernelone/context/context_os/__init__.py src/backend/polaris/kernelone/context/tests/test_context_os_schemas.py`; negative ContextOS schema alias scan. |
| LR-94 | ContextOS flat policy accessors | `StateFirstContextOSPolicy` still exposed deprecated flat property accessors such as `policy.max_open_loops`, even though active code uses grouped sub-policies (`policy.collection_limits.max_open_loops`, `policy.input_validation.max_messages`, etc.). | Removed the deprecated flat accessor block and converted tests to assert grouped sub-policies are the only public policy contract. | `rtk pytest src/backend/polaris/kernelone/context/tests/test_context_os_policies.py src/backend/polaris/kernelone/context/tests/test_continuity.py src/backend/polaris/tests/unit/kernelone/test_context_window_fallback.py -q`; `rtk ruff check src/backend/polaris/kernelone/context/context_os/policies.py src/backend/polaris/kernelone/context/tests/test_context_os_policies.py`; `rtk mypy src/backend/polaris/kernelone/context/context_os/policies.py src/backend/polaris/kernelone/context/tests/test_context_os_policies.py`; negative deprecated policy-accessor scan. |
| LR-95 | Bootstrap config alias audit | Bootstrap configuration still accepted historical flat keys, but the audit module, log message, event field names, and caller variables described this one-way migration as legacy architecture. | Renamed the audit module and event surface to `config_alias` / `source_key` terminology while preserving accepted flat-key migration behavior. | `rtk pytest src/backend/polaris/tests/unit/bootstrap/test_config.py src/backend/polaris/tests/unit/bootstrap/test_config_loader.py -q`; `rtk ruff check`; `rtk mypy`; negative bootstrap legacy-config audit scan. |
| LR-96 | Context cache facade | `polaris.kernelone.context.cache` and `KernelOneCacheManager` were a compatibility facade over the canonical tiered cache manager. | Deleted the facade module, moved singleton accessors into `cache_manager.py`, exported `TieredAssetCacheManager` from the package root, and updated cache tests to use the canonical manager. | `rtk test pytest src/backend/polaris/kernelone/context/tests/test_cache_manager.py src/backend/polaris/kernelone/context/tests/test_tiered_cache.py -q`; `rtk ruff check`; `rtk mypy`; negative context-cache facade scan. |
| LR-97 | Exploration default policy wording | `DefaultExplorationPolicy` is the current default selector policy implementation, but comments/docstrings described it as a backward-compatible alias. | Reworded the policy class and section comments as the canonical default implementation, preserving its injection behavior through `DefaultSelectorPolicy`. | `rtk pytest src/backend/polaris/kernelone/context/tests/test_working_set.py src/backend/polaris/kernelone/context/tests/test_context_subsystem.py -q`; `rtk ruff check src/backend/polaris/kernelone/context/exploration_policy.py`; `rtk mypy src/backend/polaris/kernelone/context/exploration_policy.py`; negative exploration-policy compatibility-alias scan. |
| LR-98 | Process control port wording | `ProcessControlPort` is an active process lifecycle Protocol, but section comments and exports labeled it as legacy. | Reworded the protocol/export comments as the current process lifecycle contract while keeping the active `ProcessControlPort` API unchanged. | `rtk pytest src/backend/polaris/tests/test_command_executor.py src/backend/polaris/tests/test_process_manager.py src/backend/polaris/tests/test_async_process_runner.py -q`; `rtk ruff check src/backend/polaris/kernelone/process/contracts.py`; `rtk mypy src/backend/polaris/kernelone/process/contracts.py`; negative process-control legacy wording scan. |
| LR-99 | Process stop-flag `cache_root` parameter | KernelOne and roles-runtime stop-flag facades accepted `cache_root` only to delete or ignore it, preserving a misleading two-input process-control API even though control flags are workspace-scoped in `kernelone.fs.control_flags`. | Removed the unused `cache_root` parameter from the KernelOne process facade and roles-runtime facade, and updated the runtime reset route/tests to call the workspace-only API. | `rtk pytest src/backend/polaris/kernelone/fs/tests/test_control_flags_quality.py src/backend/polaris/tests/integration/delivery/routers/test_runtime_router.py src/backend/polaris/tests/unit/delivery/http/routers/test_runtime_v2.py -q -k "control_flags or runtime_clear or runtime_reset"`; `rtk ruff check`; `rtk mypy`; negative stop-flag cache-root scan. |
| LR-100 | Background manager alias class | `kernelone.process.background_manager.BackgroundManager` inherited from `BackgroundManagerV2` only to preserve old `run()` / `drain_completed()` names, but codegraph and source scans found no active import or caller. | Deleted the unused alias class and reworded the runtime-state redirection guard comment to the current ledger protection semantics. | `rtk pytest src/backend/polaris/tests/test_learn_claude_code_integration.py src/backend/polaris/tests/test_kernelone_hardening.py src/backend/polaris/tests/test_kernelone_observability_regressions.py -q -k "BackgroundManagerV2 or background_manager"`; `rtk ruff check src/backend/polaris/kernelone/process/background_manager.py`; `rtk mypy src/backend/polaris/kernelone/process/background_manager.py`; negative BackgroundManager alias scan. |
| LR-101 | Runtime WebSocket router terminology | `delivery.ws.runtime_endpoint` is the active v2 router assembly for `/v2/ws/runtime`, but its module docstring described the current router as a facade and named the retired v1 protocol as legacy. | Reworded the docstring to current router-assembly terminology and described v1 as retired, preserving the existing v2 router wiring. | `rtk proxy python -m py_compile src/backend/polaris/delivery/ws/runtime_endpoint.py`; `rtk ruff check src/backend/polaris/delivery/ws/runtime_endpoint.py`; `rtk mypy src/backend/polaris/delivery/ws/runtime_endpoint.py`; negative runtime-endpoint legacy/facade wording scan. |
| LR-102 | KernelOne state-machine error alias | `kernelone.state_machine` still re-exported `InvalidStateTransitionError`, and two production state-machine modules imported the error through that abstraction module instead of its authoritative owner in `kernelone.errors`. | Migrated production imports to `polaris.kernelone.errors`, removed the error from `kernelone.state_machine.__all__`, and added an architecture fence that prevents the error alias from returning. | `rtk pytest src/backend/polaris/tests/architecture/test_kernelone_state_machine_error_alias_fence.py src/backend/polaris/cells/roles/kernel/tests/test_state_machine.py -q`; `rtk ruff check src/backend/polaris/kernelone/state_machine.py src/backend/polaris/cells/roles/kernel/internal/turn_state_machine.py src/backend/polaris/domain/state_machine/task_phase.py src/backend/polaris/tests/architecture/test_kernelone_state_machine_error_alias_fence.py`; `rtk mypy src/backend/polaris/kernelone/state_machine.py src/backend/polaris/cells/roles/kernel/internal/turn_state_machine.py src/backend/polaris/domain/state_machine/task_phase.py src/backend/polaris/tests/architecture/test_kernelone_state_machine_error_alias_fence.py`; negative state-machine error-alias scan. |
| LR-103 | Context budget fallback constant | `kernelone.context.budget_gate.DEFAULT_FALLBACK_WINDOW` remained a deprecated public/root export even though model context windows are resolved through ModelCatalog and the only active use was a ContextOS overlay sentinel. | Removed the deprecated constant from `budget_gate` and the package root, localized the ContextOS overlay missing-window behavior to `_resolve_context_os_overlay_model_window`, and extended the budget alias fence. | `rtk pytest src/backend/polaris/cells/context/engine/tests/test_public_service.py src/backend/polaris/tests/architecture/test_kernelone_context_budget_usage_alias_fence.py -q`; `rtk ruff check src/backend/polaris/kernelone/context/budget_gate.py src/backend/polaris/kernelone/context/__init__.py src/backend/polaris/cells/context/engine/public/service.py src/backend/polaris/cells/context/engine/tests/test_public_service.py src/backend/polaris/tests/architecture/test_kernelone_context_budget_usage_alias_fence.py`; `rtk mypy src/backend/polaris/kernelone/context/budget_gate.py src/backend/polaris/kernelone/context/__init__.py src/backend/polaris/cells/context/engine/public/service.py src/backend/polaris/cells/context/engine/tests/test_public_service.py src/backend/polaris/tests/architecture/test_kernelone_context_budget_usage_alias_fence.py`; negative production `DEFAULT_FALLBACK_WINDOW` scan. |
| LR-104 | Workflow runtime backend alias | `kernelone.workflow.base.RuntimeBackend` aliased `RuntimeBackendPort`, and workflow runtime internal/public packages re-exported the shorter alias as a second type surface. | Removed the alias, migrated internal factory/adapter annotations and all workflow runtime package exports to `RuntimeBackendPort`, and added an architecture fence against restoring the alias. | `rtk pytest src/backend/polaris/tests/architecture/test_workflow_runtime_backend_alias_fence.py src/backend/polaris/tests/unit/cells/orchestration/workflow_runtime/internal/test_runtime_backend_adapter.py src/backend/polaris/tests/test_task_queue_runtime.py -q`; `rtk ruff check`; `rtk mypy`; negative production `RuntimeBackend` scan. |
| LR-105 | UEP topic aliases | `kernelone.events.uep_contracts` still defined deprecated `UEP_TOPIC_*` aliases beside the canonical `kernelone.events.topics.TOPIC_RUNTIME_*` constants, creating a second topic-name surface. | Deleted the `UEP_TOPIC_*` aliases and added a topic test that keeps `uep_contracts` limited to payload dataclasses. | `rtk pytest src/backend/polaris/tests/unit/kernelone/test_events_topics.py src/backend/polaris/kernelone/events/tests/test_uep_publisher.py -q`; `rtk ruff check src/backend/polaris/kernelone/events/uep_contracts.py src/backend/polaris/tests/unit/kernelone/test_events_topics.py`; `rtk mypy src/backend/polaris/kernelone/events/uep_contracts.py src/backend/polaris/tests/unit/kernelone/test_events_topics.py`; negative production `UEP_TOPIC_*` scan. |

## Open Residual Buckets

No open runtime residual buckets remain in this intake after LR-105. New findings
must be added with fresh codegraph evidence before edits.

## Remaining Allowed References

The retired exact-edit token may still appear only in the following audited
categories. These are not active/model-facing recommendations and must not be
used as a second tool fact source.

| Category | Allowed locations | Reason |
| --- | --- | --- |
| Canonical compatibility catalog | `polaris/kernelone/tools/tool_kinds.py`, `polaris/kernelone/tool_execution/tool_spec_registry.py` | Single source of truth for deprecated-tool recognition and explicit compatibility export. |
| Compatibility argument normalization | `polaris/kernelone/llm/toolkit/tool_normalization/normalizers/_precision_edit.py`, normalizer registry wiring | Historical calls may still be normalized before being rejected, classified, or explicitly exported. |
| Negative/default-surface tests | Role profile, role integration, llm caller, sequence template, filesystem guidance, chain forensics, and registry tests | Tests assert the retired tool is not advertised, not selected, not executed, or only present under explicit compatibility. |
| Security/adversarial fixtures | L6 tool-calling matrix cases, security ethics cases, active focus-drift benchmark cases | Fixtures list retired tools only as forbidden tools or adversarial probes. |
| Historical/governance docs | This ledger, root governance instructions, historical audits, and migration blueprints | Historical records explain why independent editor implementations and active exposure are forbidden. |

| Bucket | Priority | Disposition | Guardrail |
| --- | --- | --- | --- |
| CLI compatibility surfaces | Closed by LR-14 through LR-22 | Canonical CLI dispatch is `polaris.delivery.cli.__main__`; retired aliases are fail-closed or removed; retired backend aliases are rejected. | Do not reintroduce second parser/dispatch hosts, silent no-handler success, `test-window`, textual/rich backend aliases, or Director state override inputs. |
| Workspace/docs migration paths | Closed by LR-23 | New docs-init output is canonical `docs/product/*`; old flat docs are no longer generated or accepted by docs-init apply. Remaining old paths are read-only fallback only. | Product docs must stay first in candidate ordering; old `00_overview.md` / `10_requirements.md` may be read for migration but must not become write targets or primary facts. |
| HTTP route aliases | Closed by LR-24 and LR-25 | Docs-init and misc AGENTS/LanceDB/memos/Ollama actions use canonical `/v2/*` routes; retired aliases are no longer registered. | New delivery HTTP endpoints must be born under `/v2/*`; do not add non-v2 alias routes that execute the same business logic. |
| Runtime action aliases | Closed by LR-26 | Runtime clear/reset callers use `/v2/runtime/*`; retired action aliases are no longer registered. Storage-layout and migration-status compatibility paths were not part of this closure and require separate evidence if retired later. | Runtime mutating actions must use canonical `/v2/runtime/*` routes; do not restore `/runtime/clear` or `/runtime/reset-tasks`. |
| Runtime bootstrap aliases | Closed by LR-37 | Runtime storage-layout/migration-status callers use `/v2/runtime/storage/layout` and `/v2/runtime/migration/status`; retired non-v2 bootstrap aliases are no longer registered or bootstrap-rate-limit sensitive. | Runtime bootstrap/read-only layout APIs must stay under `/v2/runtime/*`; do not restore `/runtime/storage-layout`, `/runtime/migration-status`, `/runtime/storage/layout`, or `/v2/runtime/storage-layout`. |
| System control-plane aliases | Closed by LR-38 | Settings, state snapshot, and app shutdown callers use `/v2/settings`, `/v2/state/snapshot`, and `/v2/app/shutdown`; old non-v2 system aliases are no longer registered or bootstrap-rate-limit sensitive. | Do not restore `/settings`, `/state/snapshot`, or `/app/shutdown`; old `/state/snapshot` may appear only in static guards or negative route tests. |
| Cognitive runtime nested v2 aliases | Closed by LR-39 | Cognitive runtime keeps its existing `/cognitive-runtime/*` surface; the erroneous nested `/cognitive-runtime/v2/cognitive-runtime/*` aliases are no longer registered. | Do not add nested versioned child routes under a versionless router prefix; if cognitive runtime is later migrated to global `/v2/*`, do it as an explicit router move with caller migration and negative tests. |
| PM management nested v2 aliases | Closed by LR-40 | PM management document/task/requirement APIs use `/v2/pm/*`; PM management status/health/init use `/v2/pm/management/*` to avoid colliding with governed PM runtime `/v2/pm/status`; nested `/pm/v2/pm/*` aliases are no longer registered. | Do not add versioned child routes under the `/pm` router prefix. PM runtime state remains under `/v2/pm/status`; PM management state must stay under `/v2/pm/management/*`. |
| CE-managed DirectorPool legacy push scheduler | Closed by LR-41 | Director scheduling is not owned by Chief Engineer. TaskMarket pull leasing and Director task consumer conflict detection are the active scheduling/concurrency boundary. | Do not restore `chief_engineer.blueprint.internal.director_pool` or tests that model CE-direct assignment/reassignment. Conflict detection must stay in Director task consumer or another execution-control-plane component, not CE blueprint. |
| LLM config/status route aliases | Closed by LR-42 | LLM configuration, status, health, metrics, and runtime-status HTTP surfaces are canonical under `/v2/llm/*`. Non-v2 `/llm/*` config/status/runtime aliases are not registered. | Do not restore `/llm/config`, `/llm/config/migrate`, `/llm/status`, `/llm/runtime-status`, or `/llm/runtime-status/{role_id}` as executable routes; `/llm/status` may appear only in negative route tests. |
| Agent nested v2 route aliases | Closed by LR-43 | Agent routes are mounted once by the global v2 router and use canonical `/v2/agent/*` paths; nested `/v2/agent/v2/*` aliases are no longer registered. | Do not add versioned child routes under the `/agent` router prefix while it is included under the global `/v2` router. |
| Cognitive runtime receipt envelope source | Closed by LR-44 | Runtime receipts accept `turn_envelope` through the explicit command/runtime field only; receipt payload remains business evidence and cannot carry reserved envelope metadata. | Do not add migration code that pops reserved runtime contract fields out of generic receipt payloads; add explicit command fields instead. |
| Generic workflow role-specific stubs | Closed by LR-45 | Generic pipeline workflow is only the generic pipeline implementation; real PM/Director/QA workflows live in their dedicated workflow modules and exports. | Do not add role-specific compatibility wrappers or simulated success returns to `generic_pipeline_workflow.py`; use dedicated workflow modules or explicit adapters. |
| Role default deprecated write tool | Closed by LR-46 | Director default role profiles and anchors no longer advertise `precision_edit`; canonical model-facing edit guidance is `edit_blocks` with `repo_apply_diff` for complete diff application. | Do not re-add deprecated write tools to role profile whitelists or anchors. Compatibility recognition in tool normalization is allowed only to classify or safely execute historical calls, not to advertise them by default. |
| LLM tool-runtime deprecated write prompt | Closed by LR-47 | `DirectorToolIntegration.get_system_prompt()` no longer teaches models to use `precision_edit`; its edit guidance is aligned with `edit_blocks`. | Do not add deprecated tool names to model-visible prompt templates. ToolSpecRegistry may retain deprecated schemas only as compatibility recognition, not as recommended guidance. |
| Provider deprecated tool schemas | Closed by LR-48 | Provider-facing schema generation excludes deprecated tools unless a caller explicitly opts into compatibility export. | Do not rely on `generate_llm_schemas()` to advertise deprecated tools by default. Use explicit lookup or `include_deprecated=True` only in migration/evaluation contexts that intentionally need deprecated schemas. |
| Role kernel deprecated edit guidance | Closed by LR-49 | TransactionKernel model-visible edit guidance no longer mentions deprecated edit tools; prompt templates steer to `edit_blocks`, `edit_file`, `search_replace`, `repo_apply_diff`, and `write_file` as appropriate. | Deprecated tool names may remain only in compatibility classifiers, validators, and historical tests; they must not be added to model-visible positive guidance or fallback orders. |
| Tool failure deprecated recovery advice | Closed by LR-50 | Failure budget recovery advice now steers no-match loops toward verified targeted edits instead of deprecated exact-search retry. | Recovery suggestions that are fed back to models must prefer `edit_blocks`/`edit_file`; deprecated tool names may remain only as the observed failing tool name when classifying historical calls. |
| Provider schema deprecated recommendation | Closed by LR-51 | Default provider tool schema descriptions no longer recommend deprecated edit tools. | Default provider schema text must remain free of deprecated tool names; deprecated schemas may only be emitted through explicit compatibility export. |
| ContextOS tool-output projection | Closed by LR-52 | Oversized tool results keep full bytes in `ReceiptStore` but project a compact prompt-safe summary before the receipt placeholder so follow-up turns have actionable evidence without raw-output leakage. | Do not regress to opaque-only tool placeholders for oversized non-failure tool results; keep arbitrary raw tool text out of prompts and add structured summary fields instead. |
| Syntax policy deprecated edit API | Closed by LR-53 | Exact-search syntax validation is generic and no longer exposes a deprecated tool-specific method name or error prefix. | Do not add tool-name-specific syntax-policy public APIs for retired edit tools; keep syntax policy phrased around operation semantics such as exact-search text and diff hunks. |
| Tool runtime optional handler terminology | Closed by LR-54 | `BackendToolRuntime` keeps KernelOne-native execution as the primary path and names optional imported handlers as optional external handlers, not a legacy execution source. | Do not describe `tools.main` discovery as a legacy fallback or registration surface; optional handler imports must remain secondary to native KernelOne execution. |
| ToolComposer deprecated edit planning | Closed by LR-55 | Tool planning can still explicitly recognize compatibility tools, but active composition excludes deprecated specs and selects robust edit tools first. | Do not put deprecated tools in `ToolComposer` positive planning maps or examples; if a deprecated tool must stay registered, planning consumers must request non-deprecated specs. |
| Role kernel retry deprecated edit selection | Closed by LR-56 | Role-kernel retry and contract logic can still classify historical deprecated tool observations, but must not actively select the deprecated exact-search edit tool, accept it as a `search_replace` equivalent, or use it as a forced-write prompt-layer trigger. | Keep retry priorities, required-tool equivalents, and forced-write prompt-layer checks aligned with `edit_blocks` / `edit_file` / `search_replace` / `repo_apply_diff`; deprecated tools may remain only in compatibility classifiers, historical tests, or explicit migration exports. |
| Backend tool runtime deprecated discovery | Closed by LR-57 | KernelOne backend runtime discovery and native direct invocation no longer expose the deprecated exact-search edit tool. | Do not add deprecated tools to `_DIRECT_TOOL_NAMES`; compatibility recognition belongs in explicit migration/export surfaces, not runtime list-tools discovery or native direct execution. |
| Write-tool active vs compatibility fact source | Closed by LR-58 | `WRITE_TOOLS` remains an observation-compatible materialization classifier, while `ACTIVE_WRITE_TOOLS` is the only set that active retry/contract planning should consume. | New model-facing, retry, planning, or contract code must use `ACTIVE_WRITE_TOOLS`; receipt, lifecycle, and historical-result classification may use `WRITE_TOOLS`. |
| PhaseManager local write-tool set | Closed by LR-59 | PhaseManager no longer owns a local write-tool collection; it uses canonical compatibility write tools for receipt observation and active write tools for phase admission. | Do not add local `_WRITE_TOOLS` collections to phase/state code. Phase admission must use `ACTIVE_WRITE_TOOLS`; receipt observation may use `WRITE_TOOLS`. |
| Tool sequence local edit-tool set | Closed by LR-60 | Positive sequence and recovery protocol generation uses canonical `ACTIVE_WRITE_TOOLS`, not a local edit/write list. | Do not maintain local active edit-tool sets in prompt/contract template code; deprecated tools must not trigger active recovery guidance. |
| Stream orchestrator local write-tool set | Closed by LR-61 | Stream continuation checks use canonical `WRITE_TOOLS` for receipt observation instead of a local write-tool list. | Do not maintain local write-tool observation lists in stream orchestration; use `WRITE_TOOLS` for historical receipt classification and `ACTIVE_WRITE_TOOLS` for future planning/admission. |
| Session orchestrator local write-tool set | Closed by LR-62 | Read-only termination exemptions use canonical `WRITE_TOOLS` for receipt observation instead of a local write-tool list. | Do not maintain local write-tool observation lists in session orchestration; use `WRITE_TOOLS` for historical receipt classification and `ACTIVE_WRITE_TOOLS` for future planning/admission. |
| LLM caller active tool pruning | Closed by LR-63 | Weak-model materialization and repair-preserve tool-set narrowing derive active write/edit tools from `ACTIVE_WRITE_TOOLS`; deprecated exact edit is filtered when an active anchored edit path exists. | Provider tool pruning and schema pinning must not hand-maintain retired edit tools; compatibility recognition belongs in explicit deprecated exports, normalizers, historical receipts, or tests. |
| Exploration policy failed-edit observation | Closed by LR-64 | Diagnostic read overrides after failed write/edit tools consume canonical `WRITE_TOOLS` plus known historical replace aliases. | Do not maintain local failed-edit observation lists or direct deprecated tool names in exploration policy; this layer observes failures only and must not become an active tool-admission list. |
| Context tool-policy fallback mutating set | Closed by LR-65 | ContextGateway suppress-mutating fallback blocks canonical `ACTIVE_WRITE_TOOLS` and exec tools if ToolSpecRegistry is unavailable. | Do not maintain handwritten mutating-tool fallback lists in runtime tool policy; use canonical active write-tool facts and keep deprecated tools out of active block/admission projections. |
| Tool batch existing-file guard | Closed by LR-66 | Existing-file path rewriting keeps historical deprecated write tools behind `DEPRECATED_WRITE_TOOLS`, not handwritten retired literals. | Compatibility execution validation may consume `DEPRECATED_WRITE_TOOLS`; active tool admission and planning must keep using `ACTIVE_WRITE_TOOLS`. |
| Task contract write-group priority | Closed by LR-67 | Required write-tool group ordering now prefers active `edit_blocks` / diff/edit tools and no longer ranks deprecated exact edit. | Do not put retired tools into active priority maps; if a legacy contract explicitly names a deprecated tool, it may be observed as input but must not be promoted as a preferred option. |
| Director materialization facade write evidence | Closed by LR-68 | Director public service materialization facade uses canonical `WRITE_TOOLS` for write evidence projection. | Do not maintain local write-tool evidence sets in Director public service; use `WRITE_TOOLS` for historical receipt classification and `ACTIVE_WRITE_TOOLS` for active planning/admission. |
| AgentAccel deprecated exact-edit executor handler | Closed by LR-69 | AgentAccel no longer registers or implements deprecated exact-edit execution; explicit deprecated schema lookup remains a compatibility recognition surface only. | Do not re-add retired edit handlers to AgentAccel. Historical calls should be normalized/classified or fail closed, not executed as writes. |
| Evaluation judge deprecated edit equivalence | Closed by LR-70 | Tool-calling matrix and judge equivalence now reward active edit tools such as `edit_blocks`, not retired exact edit. | Do not include deprecated tools in positive equivalence groups or prompt-contract mutation guidance; reserve them for negative/security fixtures and compatibility observations only. |
| Factory chain forensics write-tool catalog | Closed by LR-71 | Chain forensics write detection derives from `WRITE_TOOLS`; historical deprecated write observations remain visible only through the canonical compatibility catalog. | Do not hand-maintain write-tool regexes or local retired-tool lists in bench diagnostics; use KernelOne write-tool facts. |
| Director execute diagnostic write-tool catalog | Closed by LR-72 | Director no-materialization diagnostics use `WRITE_TOOLS` for write evidence observation instead of a local retired-tool list. | Do not add local diagnostic write-tool sets in Director execution; use KernelOne write-tool facts. |
| Transaction retry deprecated edit guidance | Closed by LR-73 | Retry prompts describe exact-match edit risk without naming retired tools, and positive transaction tests use active edit tools. | Model-visible prompt/retry/contract examples must not recommend retired tools; keep deprecated names only in explicit compatibility or negative tests. |
| Factory workspace-quality mutation token catalog | Closed by LR-74 | Factory repair-evidence mutation detection uses `WRITE_TOOLS` plus explicit repair operation names instead of a local retired-tool list. | Do not maintain local mutation tool catalogs in factory pipeline diagnostics; derive write tools from KernelOne facts. |
| Tool-calling matrix positive retired edit fixtures | Closed by LR-75 | Success-path matrix fixtures require active edit/write tools; retired exact-edit remains only in forbidden-tool security cases. | Positive evaluation fixtures must not require retired tools; use negative fixtures for explicit deprecated-tool rejection. |
| Filesystem write-fragment guidance | Closed by LR-76 | `write_file` edit-fragment errors steer models to `edit_blocks/edit_file`, not retired exact-edit. | Tool error messages fed back to models must recommend active tools only; use tests for negative retired-tool assertions. |
| Agentic benchmark audit fallback tool list | Closed by LR-77 | Agentic benchmark audit fallback no longer hand-lists retired tools; compatibility validity comes from ToolSpecRegistry. | Keep fallback tool lists active/minimal and rely on canonical registry for compatibility specs. |
| Generic JSON parser retired-tool sample | Closed by LR-78 | Generic stdout JSON parser tests now use active `edit_blocks` in neutral samples and import the current package module directly. | Do not use retired tools as arbitrary sample data in tests that are not explicitly about deprecated compatibility or rejection; do not preserve migrated-module import shims in active tests. |
| Tree-sitter availability retired-tool sample | Closed by LR-79 | Tree-sitter availability tests now use active `edit_blocks` as the neutral non-Tree-sitter-dependent write-tool sample. | Availability/filtering tests that are not about deprecated compatibility must use active tools; keep retired tools in explicit compatibility or rejection tests only. |
| Exploration policy retired-tool sample | Closed by LR-80 | Diagnostic read override tests now use active `edit_blocks` for generic targeted edit failures. | Behavioral policy tests should use active tools unless they explicitly assert deprecated compatibility or rejection. |
| LLM stress doc retired edit claim | Closed by LR-81 | LLM stress integration docs now describe Director edit capability with active `edit_blocks` and explicitly classify retired exact-edit as compatibility-only. | Stress docs must not present retired tools as active Director capabilities or permission examples. |
| Tool error classifier/failure-budget retired samples | Closed by LR-82 | Generic no-match classifier and failure-budget examples now use active `edit_blocks`. | Core examples and neutral unit tests should use active tools; deprecated tools belong in explicit compatibility, rejection, or historical-observation tests. |
| Transaction protocol retired-tool literal | Closed by LR-83 | Compatibility write classification tests now source retired tool names from `DEPRECATED_WRITE_TOOLS` instead of local literals. | Tests that need deprecated names for compatibility classification must derive them from the canonical catalog. |
| Validate-tool-step retired-tool literal | Closed by LR-84 | Deprecated exact-edit validation tests now source the retired tool name from `DEPRECATED_WRITE_TOOLS`. | Compatibility schema tests must derive retired tool names from the canonical deprecated-tool catalog. |
| Independent precision-editor implementation | Closed by LR-85 | The unused `infrastructure/agent_core/precision_editor.py` implementation is deleted; Director runtime composer uses neutral targeted-editor evidence naming. | Do not reintroduce standalone editor implementations outside KernelOne ToolSpecRegistry/tool normalization and Director runtime repair contracts. |
| AgentAccel deprecated-handler literal | Closed by LR-86 | AgentAccel negative handler tests now derive retired tool names from `DEPRECATED_WRITE_TOOLS`. | Negative executor tests for deprecated tools must source names from the canonical deprecated-tool catalog. |
| Agentic benchmark backup case directory | Closed by LR-87 | Orphaned `cases_backup` fixture copies are removed; active fixture loading has a single `cases` source. | Do not add backup fixture directories under active evaluation packages; use git history or explicit archive docs for historical cases. |
| CLI top-level chat description | Closed by LR-88 | The top-level CLI help now describes `chat` as a retired alias and points users to `console`. | User-facing CLI descriptions must match current parser/dispatch contracts; do not use legacy wording for retired aliases. |
| KernelOne runtime shared-types facade | Closed by LR-89 | Active code imports path/text helpers from `polaris.kernelone.shared.*`; `polaris.kernelone.runtime.shared_types` is retained only as an external compatibility facade. | Do not add new production imports from `runtime.shared_types`; import helpers from `shared.path_utils`, `shared.text_utils`, or `shared.terminal` directly. |
| HTTP app factory terminology | Closed by LR-90 | The canonical FastAPI gateway uses retired/historical compatibility wording instead of legacy terminology. | Do not describe canonical gateway route assembly as legacy; use explicit retired compatibility or historical layout language when preserving a wrapper. |
| Control-flag role wrappers | Closed by LR-91 | PM/Director stop-signal helper functions are current role-scoped wrappers, not legacy aliases. | Do not label active control-signal helpers as legacy; reserve compatibility wording for actual migration boundaries with tests. |
| Role-chat JetStream endpoint wording | Closed by LR-92 | The JetStream role-chat endpoint is documented as the current single realtime starter and policy consumer, not as a legacy route replacement. | Do not describe current realtime role chat as legacy; old HTTP streaming should be named as removed/retired only when needed for migration notes. |
| ContextOS schema validation result alias | Closed by LR-93 | ContextOS schema validation exposes the explicit `SchemaValidationResult` type only; the generic `ValidationResult` alias is removed from the module and package root. | New ContextOS schema validation code must import/use `SchemaValidationResult`; do not restore a generic package-root validation-result alias. |
| ContextOS flat policy accessors | Closed by LR-94 | `StateFirstContextOSPolicy` exposes grouped policy objects only; flat deprecated property accessors are removed. | New ContextOS policy code must use grouped sub-policies such as `context_window`, `collection_limits`, `token_budget`, `input_validation`, and `attention_runtime`; do not restore flat policy accessors. |
| Bootstrap config alias audit | Closed by LR-95 | Bootstrap configuration still accepts historical flat keys, but the audit module, event type, log name, fields, and caller variables now use `config_alias` / `source_key` terminology instead of presenting the migration surface as a legacy architecture path. | Keep the one-way config alias migration observable through `config_alias_audit`; do not restore `legacy_config_audit`, `record_legacy_config_migration`, or `legacy_key` fields in active bootstrap config code. |
| Context cache facade | Closed by LR-96 | `polaris.kernelone.context.cache` and `KernelOneCacheManager` were a compatibility facade over the canonical tiered cache manager. Singleton accessors now live in `cache_manager.py`, package exports point to `TieredAssetCacheManager`, and tests exercise the canonical manager directly. | New context cache code must import from `polaris.kernelone.context.cache_manager`; do not restore `context/cache.py` or the `KernelOneCacheManager` facade class. |
| Exploration default policy wording | Closed by LR-97 | `DefaultExplorationPolicy` is the current default selector policy implementation, but comments/docstrings described it as a backward-compatible alias. | Keep `DefaultExplorationPolicy` as the active default implementation and describe actual injection behavior; do not label active strategy classes as compatibility aliases without a retired replacement surface. |
| Process control port wording | Closed by LR-98 | `ProcessControlPort` is an active process lifecycle Protocol, but section comments and exports labeled it as legacy. | Keep `ProcessControlPort` documented as the current process lifecycle contract; do not mark active Port/Protocol interfaces as legacy unless a replacement is actually authoritative. |
| Process stop-flag cache-root parameter | Closed by LR-99 | PM and Director stop-signal cleanup/path helpers are workspace-scoped APIs; `cache_root` is not part of the process-control contract. | Do not add `cache_root` back to stop-flag cleanup/path helper signatures. Use storage layout APIs for runtime-root resolution and keep control-flag helpers workspace-only. |
| Background manager alias class | Closed by LR-100 | `BackgroundManagerV2` is the only background command manager implementation; the old `BackgroundManager` alias and its old method names are removed. | Import and instantiate `BackgroundManagerV2` directly; do not restore `BackgroundManager.run()` or `BackgroundManager.drain_completed()` aliases. |
| Runtime WebSocket router terminology | Closed by LR-101 | `delivery.ws.runtime_endpoint` is the current v2 WebSocket router assembly and `/v2/ws/runtime` remains the product realtime entrypoint. | Keep implementation in `delivery.ws.endpoints.*`; do not describe the active router assembly as a legacy/facade path or restore v1 protocol handling. |
| KernelOne state-machine error alias | Closed by LR-102 | `InvalidStateTransitionError` is owned by `polaris.kernelone.errors`; `polaris.kernelone.state_machine` owns state-machine abstractions only. | Import `InvalidStateTransitionError` from `polaris.kernelone.errors`; do not restore an error re-export from `kernelone.state_machine`. |
| Context budget fallback constant | Closed by LR-103 | Model window limits are resolved through ModelCatalog or explicit ContextOS policy fields; `DEFAULT_FALLBACK_WINDOW` is not a public/root budget contract. | Do not restore `DEFAULT_FALLBACK_WINDOW`; local missing-window sentinels must remain private to the caller that needs them. |
| Workflow runtime backend alias | Closed by LR-104 | Workflow runtime typing uses the explicit `RuntimeBackendPort` protocol name across KernelOne, internal runtime engine, and public workflow-runtime exports. | Do not restore `RuntimeBackend`; public exports and type annotations must use `RuntimeBackendPort`. |
| UEP topic aliases | Closed by LR-105 | UEP topic strings are owned by `polaris.kernelone.events.topics.TOPIC_RUNTIME_*`; `uep_contracts` owns payload dataclasses only. | Do not restore `UEP_TOPIC_*` aliases in `uep_contracts`; import canonical topic constants from `events.topics`. |
| Message-bus sync handler adapter alias | Closed by LR-106 | Synchronous message-bus handler adaptation is an active capability exposed as `SyncMessageHandlerAdapter`, not a legacy adapter surface. | Do not restore `LegacySyncHandlerAdapter`; import and export `SyncMessageHandlerAdapter` directly. |
| Domain token estimator constant alias | Closed by LR-107 | Token estimation constants are owned by `polaris.kernelone.llm.engine.TokenEstimator`; `TokenService` only delegates estimation and budget behavior. | Do not restore `TokenService.CHARS_PER_TOKEN`; import estimator constants from `TokenEstimator` only when a caller truly needs the heuristic constant. |
| Runtime WebSocket role-token alias | Closed by LR-108 | Runtime WebSocket role filtering exposes `RUNTIME_OBSERVABLE_ROLE_TOKENS`; it is broader than TaskMarket consumers and must not be published as `CONSUMER_ROLE_TOKENS`. | Do not restore `CONSUMER_ROLE_TOKENS`; use `RUNTIME_OBSERVABLE_ROLE_TOKENS` for runtime observability filters. |
| NATS event-kind aliases | Closed by LR-109 | NATS `JetStreamConstants` owns stream, subject, and consumer configuration only; event kind constants remain owned by `polaris.kernelone.events.constants` and runtime payloads. | Do not restore `JetStreamConstants.EVENT_KIND_*`; import canonical event constants from KernelOne when needed. |
| Workflow-engine handler registry alias | Closed by LR-110 | `workflow_engine` exports the concrete `CellHandlerRegistry` and DI protocol `HandlerRegistryPort`; the generic `HandlerRegistry` alias is removed. | Do not restore `workflow_engine.HandlerRegistry`; import `CellHandlerRegistry` for the Cell implementation or `HandlerRegistryPort` for the KernelOne DI protocol. |
| Tool-execution short error aliases | Closed by LR-111 | Tool execution exports descriptive validator error constants such as `ERROR_STRING_TOO_LONG` and `ERROR_INTEGER_TOO_SMALL`; short aliases are removed. | Do not restore `ERROR_MIN_LENGTH`, `ERROR_MAX_LENGTH`, `ERROR_PATTERN`, `ERROR_MINIMUM`, or `ERROR_MAXIMUM`; use descriptive validator constants. |
| KernelOne runtime shared-types facade | Closed by LR-112 | `polaris.kernelone.runtime.shared_types` is removed; shared terminal, text, and path helpers are owned by `polaris.kernelone.shared.*`. | Do not restore `runtime/shared_types.py`; import from `polaris.kernelone.shared.terminal`, `shared.text_utils`, or `shared.path_utils`. |
| Arsenal route aliases | Closed by LR-27 | Arsenal product calls use `/arsenal/v2/*`; non-v2 Arsenal aliases are no longer registered. | Keep Arsenal endpoints under the router's canonical `/arsenal/v2/*` namespace; do not add parallel non-v2 aliases. |
| Factory run route aliases | Closed by LR-28 | Factory run operations use `/v2/factory/runs*`; old `/factory/runs*` aliases are no longer registered. | Factory run control-plane APIs must remain under `/v2/factory/runs*`; do not restore non-v2 aliases. |
| Logs route aliases | Closed by LR-29 | Logs APIs use `/logs/v2/*`; old `/logs/*` aliases are no longer registered, and frontend `logsV2Service` targets the actual backend namespace. | Keep logs endpoints under `/logs/v2/*` unless the router itself is intentionally migrated; do not add parallel `/logs/*` aliases or `/v2/logs/*` callers. |
| Memory route aliases | Closed by LR-30 | Memory APIs use `/memory/v2/*`; old `/memory/*` aliases are no longer registered, and frontend `memoryV2Service` targets the actual backend namespace. | Keep memory endpoints under `/memory/v2/*` unless the router itself is intentionally migrated; do not add parallel `/memory/*` aliases or `/v2/memory/*` callers. |
| Primary probe labels | Closed by LR-31 | `/health`, `/ready`, and `/live` are supported non-v2 probes, not retired aliases. | Do not label supported primary probes as deprecated unless a replacement and removal plan exists. |
| Provider route aliases | Closed by LR-32 | Provider APIs use `/v2/llm/providers*`; old `/llm/providers*` aliases are no longer registered. | Keep provider management endpoints under `/v2/llm/providers*`; do not restore parallel non-v2 provider aliases. |
| Interview action route aliases | Closed by LR-33 | Interview action APIs use `/v2/llm/interview/*`; old `/llm/interview/{ask,save,cancel}` aliases are no longer registered, and frontend validation/types target the canonical paths. | Keep interview action endpoints under `/v2/llm/interview/*`; do not restore parallel non-v2 action aliases. Separate `/llm/interview` configuration/status surfaces require their own evidence before any future migration. |
| Permissions nested v2 aliases | Closed by LR-34 | Permissions APIs use `/v2/permissions/*`; nested `/v2/permissions/v2/*` aliases are no longer registered. | Do not create router-prefix-plus-inner-v2 duplicate namespaces; if a router prefix is already versioned, child routes must not repeat `/v2`. |
| LLM test route aliases | Closed by LR-35 | LLM readiness test APIs use `/v2/llm/test*`; old `/llm/test*` aliases are no longer registered. | Keep LLM test execution, report, transcript, and streaming APIs under `/v2/llm/test*`; do not restore parallel non-v2 aliases. |
| File read route alias | Closed by LR-36 | File reads use `/v2/files/read`; old `/files/read` is no longer registered. | Keep file read calls under `/v2/files/read`; `/files/read` may appear only in negative tests or static guards that forbid stale file-tail polling. |
| Domain vocabulary, not debt | Not counted | Tech Radar uses `deprecated` as a library ring; PM requirements use `deprecated` as a soft-delete state. | Do not remove; these are business states, not architecture drift. |
| Generated descriptor text | Not counted | Generated packs may still contain retired names until descriptor refresh. | Refresh in a descriptor wave after source convergence; do not hand-edit generated packs. |

## Next Closure Order

Current intake is closed after LR-112. Reopen only with a new evidence-backed ledger item.
