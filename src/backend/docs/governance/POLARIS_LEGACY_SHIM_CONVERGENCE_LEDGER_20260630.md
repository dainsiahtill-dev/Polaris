# Polaris Legacy/Shim Convergence Ledger

Status: Active  
Created: 2026-06-30  
Scope: Polaris meta-platform source only. This ledger is not a runtime fact source.  

This ledger tracks remaining compatibility, legacy, shim, and fallback surfaces that
can still affect architecture convergence. It separates actionable runtime debt from
historical documentation, protocol compatibility labels, provider "OpenAI-compatible"
business wording, React `Suspense fallback`, and accepted config migration code.

## Current Count

| Class | Count | Meaning |
| --- | ---: | --- |
| Closed in this convergence pass | 237 | Removed, retired, or converted into an audited sunset path and verified. |
| P1 open | 0 | Still close to execution, LLM/tool, QA, or state projection paths. |
| P2 open | 0 | Exposed API/UI/CLI compatibility surfaces that should be retired after callers move. |
| P3 accepted with sunset | 0 | Kept for user config or historical data migration; requires an expiry policy, not immediate deletion. |

## Ledger

| ID | Severity | Status | Gap | Evidence | Required Exit Criteria | Next Action |
| --- | --- | --- | --- | --- | --- | --- |
| LS-68J | P3 | Closed | Neural Syndicate consensus exported `VoteResult` as a backward-compatible alias for canonical `ConsensusResult`. | `src/backend/polaris/kernelone/multi_agent/neural_syndicate/consensus.py`; `src/backend/polaris/kernelone/multi_agent/neural_syndicate/__init__.py`; exact scans showed `VoteResult` had no production or test callers outside the alias/export itself. | Remove `VoteResult`, expose `ConsensusResult` from the package root, update docs, and add an architecture fence against reintroducing the alias. | Closed by Neural Syndicate consensus alias retirement pass; verify with consensus/orchestrator tests, architecture fence, ruff, mypy, py_compile, and governance gates. |
| LS-68I | P3 | Closed | LLM streaming exported deprecated `StreamResult = LLMStreamResult`, creating an ambiguous name beside the canonical process-layer `StreamResult`. | `src/backend/polaris/kernelone/llm/engine/stream/config.py`; `src/backend/polaris/kernelone/llm/engine/stream/__init__.py`; `src/backend/polaris/kernelone/llm/engine/stream_executor.py`; codegraph/exact scans showed production code uses canonical `LLMStreamResult` and only tests/facade exports pinned the old LLM alias. | Remove the LLM `StreamResult` alias, keep `LLMStreamResult` canonical, update tests, and add an architecture fence against reintroducing the ambiguous alias. | Closed by LLM stream result alias retirement pass; verify with stream config/executor tests, architecture fence, ruff, mypy, py_compile, and governance gates. |
| LS-68H | P3 | Closed | KernelOne time utilities still exported private compatibility aliases `_utc_now`, `_utc_now_iso`, and `_utc_now_str`, and Context Engine consumed `_utc_now` directly from `time_utils`. | `src/backend/polaris/kernelone/utils/time_utils.py`; `src/backend/polaris/kernelone/utils/__init__.py`; `src/backend/polaris/kernelone/context/engine/{models.py,engine.py,__init__.py}`; codegraph/exact scans showed only Context Engine imported the private time helper directly while other production usage already imports canonical helpers and optionally renames them locally. | Remove private time aliases from module/package exports, migrate Context Engine to canonical `utc_now`, update tests to assert canonical-only exports, and add an architecture fence. | Closed by KernelOne time utility alias retirement pass; verify with time/context engine tests, architecture fence, ruff, mypy, py_compile, and governance gates. |
| LS-68G | P3 | Closed | KernelOne JSON utilities still exported private compatibility aliases `_safe_json_loads` and `_parse_json_payload` beside canonical `safe_json_loads` and `parse_json_payload`. | `src/backend/polaris/kernelone/utils/json_utils.py`; `src/backend/polaris/kernelone/utils/__init__.py`; codegraph/exact scans showed production callers import the canonical names, while old alias behavior was only pinned by KernelOne utility tests. | Remove private JSON aliases from module/package exports, update tests to assert canonical-only exports, and add an architecture fence. | Closed by KernelOne JSON utility alias retirement pass; verify with JSON utility tests, architecture fence, ruff, mypy, py_compile, and governance gates. |
| LS-68F | P3 | Closed | KernelOne memory kept private `_has_refs` compatibility aliases around the canonical `has_memory_refs` helper, and the anthropomorphic memory facade relied on `memory_store`'s incidental imported name. | `src/backend/polaris/kernelone/memory/memory_store.py`; `src/backend/polaris/kernelone/memory/__init__.py`; `src/backend/polaris/cells/runtime/projection/internal/anthropomorphic/memory_store.py`; codegraph/rg showed `_has_refs` had no callers while `has_memory_refs` remains the canonical public helper in `refs.py`. | Remove private `_has_refs` aliases, keep public `has_memory_refs`, make the facade import the helper from `polaris.kernelone.memory.refs`, and add an architecture fence. | Closed by KernelOne memory alias retirement pass; verify with memory retrieval tests, facade import smoke, architecture fence, ruff, mypy, py_compile, and catalog governance gate. |
| LS-68E | P3 | Closed | Delivery CLI and roles profile Cells kept private `_utc_now` compatibility aliases around canonical time helpers. | `src/backend/polaris/cells/delivery/cli/public/service.py`; `src/backend/polaris/cells/roles/profile/internal/schema.py`; exact scans showed the aliases were file-local only and not public contracts. | Remove private aliases, call/import canonical time helpers directly at the call sites/default factories, and add an architecture fence against reintroducing `_utc_now` aliases in these Cell files. | Closed by Cell private time-alias retirement pass; verify with delivery CLI/roles profile tests, architecture fence, ruff, mypy, py_compile, and catalog governance gate. |
| LS-68D | P3 | Closed | Context catalog and policy protocol Cells kept Cell-local `_utc_now_iso` compatibility aliases around canonical time helpers. | `src/backend/polaris/cells/context/catalog/service.py`; `src/backend/polaris/cells/policy/protocol/public/contracts.py`; codegraph and exact scans showed aliases were used only inside their owning files and were not public contracts. | Remove the Cell-local aliases, call the canonical time helpers directly, and add an architecture fence for these Cell-owned files. | Closed by Cell time-alias retirement pass; verify with context catalog tests, policy protocol smoke, architecture fence, ruff, mypy, py_compile, and catalog governance gate. |
| LS-68C | P3 | Closed | Bootstrap launch validation kept a deprecated generic `ValidationResult = LaunchValidationResult` alias even though production callers use the canonical validation functions. | `src/backend/polaris/bootstrap/launch_validation.py`; codegraph showed callers target `validate_launch_request`, `validate_environment`, and `bootstrap_validation`, while the alias was only pinned by the local unit test. | Remove the generic alias, return/construct `LaunchValidationResult` directly, keep bootstrap behavior unchanged, and add an architecture fence against reintroducing the alias. | Closed by bootstrap validation alias retirement pass; verify with bootstrap tests, architecture fence, ruff, mypy, py_compile, and catalog governance gate. |
| LS-68B | P3 | Closed | Audit diagnosis toolkit kept private `_utc_now_iso = utc_now_iso` compatibility aliases in internal command and failure-hop helpers. | `src/backend/polaris/cells/audit/diagnosis/internal/toolkit/service.py`; `src/backend/polaris/cells/audit/diagnosis/internal/toolkit/hops.py`; codegraph/rg showed the aliases were private same-file indirection rather than public contracts. | Remove the private aliases, call canonical `utc_now_iso()` directly, and add an architecture fence preventing the alias from returning to the toolkit. | Closed by audit diagnosis time-alias retirement pass; verify with toolkit tests, architecture fence, ruff, mypy, py_compile, and catalog governance gate. |
| LS-68A | P3 | Closed | Storage layout carried a `resolve_polaris_roots = resolve_polaris_roots` no-op compatibility self-alias that added legacy noise without preserving any distinct public contract. | `src/backend/polaris/cells/storage/layout/internal/layout_business.py` line-level audit; codegraph showed real callers target the canonical `resolve_polaris_roots` function and the self-assignment only rebinds the same object. | Remove the self-alias while keeping the canonical resolver in `__all__`; add a source-level architecture fence so the no-op alias cannot return. | Closed by storage layout self-alias retirement pass; verify with focused storage layout tests, architecture fence, ruff, mypy, py_compile, and catalog governance gate. |
| LS-00 | P2 | Closed | LLM Provider edit state kept a legacy `editingProvider` / `startEditProvider` / `stopEditProvider` path beside canonical `editingProviderId`. | `src/frontend/src/app/store/providerStore.ts`; `src/frontend/src/app/components/llm/state/providerReducer.ts`; `ProviderContext.tsx`; `CanonicalProviderBridge.tsx`. | No `startEditProvider`, `stopEditProvider`, `START_EDIT_PROVIDER`, `STOP_EDIT_PROVIDER`, or `editingProvider:` references remain in `src/frontend/src/app/components/llm` / `src/frontend/src/app/store`; `npx tsc --noEmit` passes. | Closed by commit `00717e4c Remove legacy provider edit actions`. |
| LS-01 | P1 | Closed | `polaris.application.orchestration` remained as a 4.5K-line old implementation and was still exercised by tests after production callers were migrated. | Closed by commits `70f0356c`, `8525d4db`, `28a333fb`, `47ece30a`, and this LS-01 retirement pass. Production `delivery/` and `cells/` imports were migrated to public execution contracts; old `polaris/application/orchestration/` source and direct orchestrator tests were deleted; `mig-application-batch1` is now `retired`. | No `polaris.application.orchestration` production imports; retired old source files do not exist; `check_shim_markers.py --json` passes; architecture test fences runtime production roots and retired old test files. | Closed; continue with LS-03/LS-04 repair/fallback convergence after coordinating with active dirty repair-runtime changes. |
| LS-02 | P1 | Closed | Director adapter still exposed direct text/patch fallback control fields around `direct_fallback` and `allow_patch_fallback`. | Direct provider bypass in `_phase_direct_fallback` now records `runtime_provider_bypass_removed` only. `DirectorPatchExecutor.execute_tools` rejects textual tool protocols and Markdown/PATCH_FILE patch fallbacks with structured fail-closed evidence; no fallback path writes files, broadcasts file events, or persists file-edit receipts. | No executable direct provider fallback remains; all repair/patch attempts enter typed runtime repair or native tool lifecycle receipts; old fallback metadata becomes fail-closed evidence only. | Closed in this pass. Verified with `rtk pytest src/backend/polaris/cells/roles/adapters/tests/test_director_realtime_file_events.py -q -k "markdown_patch_file or write_only_tool_fallback"`, `rtk pytest src/backend/polaris/cells/roles/adapters/tests/test_director_patch_executor_pure.py -q -k "direct_fallback or patch_path or markdown"`, `rtk pytest src/backend/polaris/cells/roles/adapters/tests/test_director_adapter_pure.py -q -k "allow_patch_fallback or empty_write_retry or direct_fallback"`, `rtk pytest src/backend/polaris/cells/roles/adapters/tests/test_execute_standard_llm_flow_chars.py -q`, and targeted `rtk ruff check`. |
| LS-03 | P1 | Closed | Materialization quality repair had a broad adapter runtime-port facade beside Director Runtime public scheduling. | Runtime public now owns materialization allowed-path projection, source-tool candidate/plan-probe filtering, bridge metadata projection, TypeScript semantic materialization repairs, quality-gate entry, and the materialization quality repair facade. The old `materialization_quality_repair_bridge.py` module was deleted. Callback execution now lives in `materialization_quality_callback_ports.py`; read-only summary/receipt projection lives in `materialization_quality_evidence_ports.py`; `materialization_quality_runtime_ports.py` only exports the public facade functions used by production callers. | No old bridge entrypoint exists; production paths use typed runtime public schedule/facade; callback execution, evidence projection, and facade ownership are separate. | Closed by LS-03A through LS-03J. Continue concrete language mutator migration under LS-04C-D. |
| LS-04 | P1 | Closed | `deterministic_repairs/*` language modules still contained concrete repair mutators even though package root was fenced. | `src/backend/polaris/cells/roles/adapters/internal/director/deterministic_repairs/` remains only as a helper/facade package for non-mutating compatibility helpers. Production code outside that directory is fenced from importing concrete strategy-host modules; package facade does not export concrete mutators; every hardcoded legacy source_tool token has runtime catalog registration and migration status. TypeScript/JavaScript direct mutators are closed by LS-04C1 through LS-04C28; Rust adapter direct wrappers are closed by LS-04D1; Rust aggregate/direct entrypoints are closed by LS-04D2; generic declared-target direct helpers are closed by LS-04D3, declared-target file fabrication is retired by LS-04D4, Java direct postpass writers are retired by LS-04D5, Go/C++ legacy postpass hosts are retired by LS-04D6, Python legacy smoke host is retired by LS-04D7, and Rust bottom/postpass host is retired by LS-04D8. | Concrete mutators are migrated into `director.runtime.internal.repair_kernel` or removed; adapter imports cannot call mutators directly; architecture test blocks concrete repair imports outside runtime. | Closed by LS-04A through LS-04E. New deterministic repair behavior must enter `director.runtime.repair_kernel` as typed Plan → Compose → Policy → Execute → Receipt rules, not adapter-owned strategy-host writers. |
| LS-05 | P1 | Closed | Closed: Role Kernel no longer has RoleExecutionKernel-era second execution surfaces over TransactionKernel. | The retired turn_engine/kernel_bridge facades and loop components are deleted; RoleExecutionKernel is a minimal public API shell; non-stream and stream flows delegate to owner modules for prompt setup, invocation setup, completion projection, failure projection, stream event projection, tool runtime, context configuration, and result/task-boundary projection. | TransactionKernel is the only implementation surface; remaining public methods are canonical API entrypoints, not compatibility facades. | Closed by LS-05A1-LS-05A102 plus LS-05B/LS-05C, with negative marker scans and role-kernel transaction/stream/control-plane regression tests. |
| LS-06 | P1 | Closed | Deprecated `LLMCaller` facade remained beside `LLMInvoker`. | `LLMCaller` is no longer exported from `polaris.cells.roles.kernel`, `polaris.cells.roles.kernel.public`, `roles.kernel.public.service`, or `roles.kernel.internal.llm_caller`; request preparation, fallback/retry builders, and event helper behavior now delegate to canonical non-facade services (`LLMRequestPreparer` / `LLMEventEmitter`). `RoleExecutionKernel` no longer exposes `inject_llm_caller`, `_get_llm_caller`, `_injected_llm_caller`, or `_llm_caller`; kernel DI uses `LLMInvoker` naming end-to-end. The `internal/llm_caller/caller.py` facade and the two stale direct-facade test suites were deleted; helper coverage moved to `test_llm_caller_helpers.py`, while invocation/request-preparation behavior stays pinned by canonical `LLMInvoker` and `LLMRequestPreparer` suites. | All role runtime paths call `LLMInvoker`; `LLMCaller` is deleted; final provider request audit has one canonical writer. | Closed by LS-06G. Boundary coverage now asserts `caller.py` does not exist and no production direct import can reappear. |
| LS-07 | P1 | Closed | Legacy textual tool-call protocol detection is still present while native tool lifecycle is the canonical path. | `role_integrations` now fail-closes bracketed legacy text protocols without execution. `OutputParser.parse_execution_tool_calls` is native-tool-only for executable dispatch; JSON text and Gemma-style textual wrappers return no executable calls without provider-native tool payloads. Provider extraction no longer promotes JSON/text response content into native tool calls. `TurnDecisionDecoder`, `StreamEngine`, and `LLMInvoker` no longer recover or request text-mode tool fallback; native calls preserve `source="native_tool_call"` on canonical `ToolCall` values before dispatch accounting. | Text/JSON protocol wrappers cannot become executable tool calls through free-form assistant text; old role integrations do not execute bracketed protocols; provider/native extraction only trusts provider-native payloads; executable tool calls must originate from provider-native payloads and continue into lifecycle receipts. | Closed in this pass. Verified with `rtk pytest src/backend/polaris/cells/llm/tool_runtime/tests/test_role_integrations.py src/backend/polaris/cells/roles/kernel/tests/test_output_parser_json_fallback.py src/backend/polaris/tests/test_output_parser_patch_file.py -q`, `rtk pytest src/backend/polaris/cells/roles/kernel/tests/test_native_tool_normalization.py src/backend/polaris/cells/roles/kernel/internal/llm_caller/tests/test_tool_helpers.py src/backend/polaris/cells/roles/kernel/tests/test_llm_caller_text_fallback.py -q`, `rtk pytest src/backend/polaris/cells/roles/kernel/tests/test_llm_invoker_decomposition_characterization.py -k 'native_tools_unavailable_text_fallback_is_retired or native_tool_text_fallback_rung_is_retired' -q`, targeted architecture fence, and targeted `rtk ruff check` / `rtk mypy`. |
| LS-08 | P2 | Closed | LLM Provider state still has a broad Canonical-to-legacy bridge and mapped legacy state. | Deleted the unused `CanonicalProviderBridge.tsx` wide bridge, removed its `state/index.ts` exports, and moved the only live utility (`extractProviderIdFromConnectivityKey`) into `connectivityStore.ts`. Internal search shows no remaining `CanonicalProviderBridge`, `CanonicalBridgeProvider`, `useCanonicalBridge`, or `mapCanonicalToLegacyState` consumers. | UI state consumers now rely on canonical store/data-manager surfaces; no broad canonical-to-ProviderState bridge remains. | Closed by provider bridge retirement pass. Verified with `rtk npm test -- src/frontend/src/app/components/llm/utils/__tests__/providerModelResolver.test.ts`, `rtk npm run typecheck -- --pretty false`, and targeted negative `rtk rg` for bridge symbols. |
| LS-09 | P2 | Closed | CLI/HTTP compatibility aliases still expose old route semantics. | Main CLI retired `chat`, `status`, `workflow`, and `test-window` aliases now return explicit migration responses and no longer delegate to `polaris_cli.main()`. PM/Director v2 route docs and request descriptions now describe governed orchestration endpoints, not compatibility endpoints or old fields. | Deprecated top-level CLI aliases fail closed; HTTP v2 run endpoints remain canonical governed endpoints; tests assert no legacy CLI host dispatch. | Closed by CLI route retirement pass. Verified with `rtk pytest src/backend/polaris/tests/unit/delivery/cli/test_cli_commands.py -q`, `rtk ruff check ...`, `rtk mypy ...`, and targeted `rtk rg` for old route/compat strings. |
| LS-10 | P2 | Closed | Runtime/log pipeline still accepts legacy channel and event field shapes. | Backend dual-write adapter `infrastructure/log_pipeline/adapters.py` is retired and removed from package exports, KFS direct-write baseline, and `events.fact_stream` descriptor projection by LS-10A; duplicated channel constants are retired into `polaris.kernelone.runtime.channel_contracts`; product WS no longer exposes file-tail stream helpers or legacy `_llm` channel constants after LS-10C1; `CHANNEL_FILES` is canonical runtime.v2-only after LS-10C2; frontend runtime connection no longer subscribes to `runtime_events`, runtime hook/util channel classification is canonical-only after LS-10C3, and ContextOS/log viewer projections no longer use `runtime_events` / `runtime_event` frontend fallbacks after LS-10C4. | Ingest compatibility is isolated in one anti-corruption adapter; all UI/API projections read canonical runtime.v2 / Run Ledger facts; legacy file reads are bounded to an explicit historical-data migration path with tests; no product runtime path writes both legacy and canonical facts. | Closed by LS-10A through LS-10C4. New live runtime projections must use canonical `system`, `process`, and `llm` channels only; historical files remain archive/read metadata, not WS channel names. |
| LS-11 | P2 | Closed | Frontend dialogue and ContextOS views carried parser compatibility for multiple historical response shapes. | Added `roleChatProtocol.ts` and `finalProviderRequestProtocol.ts` as explicit protocol anti-corruption adapters. `useRoleChat.ts` and `ContextViewerModal.tsx` no longer own inline response-shape normalization; `useChatStreamWS.ts` and `useAIDialogue.ts` now share the same Jetstream start response type. | Compatibility remains isolated in named adapters with tests; UI components consume normalized contracts. | Closed by frontend protocol adapter pass. Verified with `rtk npm test -- src/frontend/src/app/components/ai-dialogue/__tests__/roleChatProtocol.test.ts src/frontend/src/app/components/ai-dialogue/__tests__/useRoleChat.test.ts`, `rtk npm test -- src/frontend/src/app/components/contextos/finalProviderRequestProtocol.test.ts src/frontend/src/app/components/contextos/ContextViewerModal.test.tsx src/frontend/src/app/components/contextos/contextosViewModel.test.ts`, and `rtk npm run typecheck -- --pretty false`. |
| LS-12 | P3 | Closed | Config and provider registries keep legacy key migration for existing user settings. | Added `polaris.legacy_config_migration_event.v1` with `legacy-config-sunset.v1` policy, bounded diagnostic records, and structured warning logs from `ConfigSettings.migrate_legacy_inputs` and `ConfigLoader._canonicalize_flat_config`. | Legacy key migration remains for compatibility, but every migrated key now records `source`, `legacy_key`, `canonical_key`, and sunset metadata. | Closed by legacy config audit/sunset pass. Verified with `rtk pytest src/backend/polaris/tests/unit/bootstrap/test_config.py::TestSettings::test_migrate_legacy_inputs src/backend/polaris/tests/unit/bootstrap/test_config_loader.py::TestConfigLoaderLoad::test_load_with_workspace_reads_global_settings -q`, `rtk ruff check ...`, and `rtk mypy ...`. |
| LS-13 | P3 | Closed | Historical artifact/path aliases remain for old workspace data. | Legacy artifact keys remain readable, but `ArtifactService` write resolution now rejects legacy key aliases such as `PLAN` and `PM_TASKS_CONTRACT`; new writes must use canonical registry keys. Added `migrate_legacy_artifact_aliases` and `src/backend/scripts/migrate_legacy_artifact_aliases.py` as an explicit dry-run-first migration command for historical path aliases. | Keep read compatibility until migration, all new writes use canonical keys only, and migration command exists for old workspace data. | Closed in this pass. Verified with `rtk pytest src/backend/polaris/cells/audit/verdict/internal/tests/test_artifact_service.py -q -k "legacy_key or write_text_rejects or write_json_rejects or migrate_legacy"`, targeted `rtk ruff check`, and `rtk proxy python src/backend/scripts/migrate_legacy_artifact_aliases.py --workspace /tmp/polaris-artifact-alias-smoke`. |
| LS-14 | P2 | Closed | Roles adapters Director schema still exported a deprecated `ValidationResult` alias beside `DirectorValidationResult`. | `DirectorOutput.validation` now uses `DirectorValidationResult` directly, `roles.adapters` schema tests no longer preserve alias-only coverage, and no roles.adapters Director schema/test references to the retired alias remain. | Director schema contracts expose one explicit validation-result type; generated descriptor metadata reflects the current schema surface; alias removal is covered by focused schema tests, static checks, and negative grep. | Closed by LS-14A. No open parent convergence cuts remain in this ledger. |
| LS-15 | P2 | Closed | `polaris.infrastructure.llm.token_service` remained as a deprecated re-export shim beside canonical `polaris.domain.services`. | Production imports in `polaris.application` and `infrastructure.llm.token_tracking_wrapper` now consume `TokenService` / `get_token_service` from `polaris.domain.services`; the infrastructure re-export file is deleted and fenced. | No production import of `polaris.infrastructure.llm.token_service`; retired file does not exist; token-service behavior and token-tracking lazy resolution still pass focused tests. | Closed by LS-15A. No open parent convergence cuts remain in this ledger. |
| LS-16 | P2 | Closed | `roles.runtime.internal.skill_loader` remained as a deprecated skill-system facade beside canonical `polaris.kernelone.single_agent.skill_system`. | Runtime code already imports KernelOne `SkillLoader` directly; the remaining surface was roles-runtime public exports plus a test suite preserving the old facade. The deprecated internal module and facade-specific tests are deleted, and public boundaries no longer export `SkillLoader`, `RoleSkillManager`, `create_skill_loader`, or `create_role_skill_manager`. | No production import of `polaris.cells.roles.runtime.internal.skill_loader`; retired file does not exist; roles-runtime public `__all__` does not expose retired skill facade aliases; KernelOne skill-system tests still cover the canonical implementation. | Closed by LS-16A. No open parent convergence cuts remain in this ledger. |
| LS-17 | P2 | Closed | `polaris.infrastructure.accel.verify.orchestrator` remained as a backward-compat facade beside refactored `polaris.infrastructure.accel.verify.verify.core`. | `CodeIntelligenceService`, `AsyncCodeIntelligenceService`, accel CLI, post-hook, and package-root verification exports now import `run_verify` from the refactored core module; the facade file is deleted and fenced. | No production import of `polaris.infrastructure.accel.verify.orchestrator`; retired file does not exist; accel verify package root still exposes the canonical `run_verify` API. | Closed by LS-17A. No open parent convergence cuts remain in this ledger. |
| LS-18 | P2 | Closed | DI factory public surface still exported removed singleton factories that only raised `NotImplementedError`, plus no-op reset shims kept alive by the global pytest fixture. | Removed `create_kernel_audit_runtime`, `create_omniscient_audit_bus`, `create_provider_manager`, and their no-op test reset helpers from `polaris.infrastructure.di.factories`, `polaris.infrastructure.di` lazy exports, and `tests/conftest.py`. | DI public exports contain only live factories/resets; removed factories are not defined; production code cannot import the retired names. | Closed by LS-18A. No open parent convergence cuts remain in this ledger. |
| LS-19 | P2 | Closed | `polaris.cells.llm.evaluation.internal.deterministic_judge` remained as a deprecated re-export shim beside the decomposed canonical `internal.judge` package. | Production callers now import `judge_agentic_case` from `internal.judge.orchestrator`; judge tests and security tests import helpers from the canonical `internal.judge` package/submodules; the old re-export module is deleted and fenced. | No production import of `polaris.cells.llm.evaluation.internal.deterministic_judge`; retired file does not exist; deterministic judge behavior remains covered by canonical judge tests. | Closed by LS-19A. No open parent convergence cuts remain in this ledger. |
| LS-20 | P2 | Closed | `polaris.bootstrap.runtime_health` remained as a deprecated re-export shim beside canonical `polaris.application.health`. | Codegraph showed only tests still imported the shim; tests now import canonical application health helpers directly, the re-export file is deleted, and an architecture fence blocks retired path/import reintroduction. | No production import of `polaris.bootstrap.runtime_health`; retired file does not exist; runtime health behavior remains covered through `polaris.application.health` tests. | Closed by LS-20A. No open parent convergence cuts remain in this ledger. |
| LS-21 | P2 | Closed | `polaris.bootstrap.ports.backend_bootstrap` re-exported `BackendBootstrapError` even though KernelOne owns the error hierarchy. | Codegraph/text audit showed no production callers relying on the port-module re-export; the re-export is deleted and an architecture fence blocks error authority drift back into the bootstrap port module. | `polaris.bootstrap.ports.backend_bootstrap` no longer exposes `BackendBootstrapError`; production code does not import the error from the port module; port protocol alias behavior remains covered. | Closed by LS-21A. No open parent convergence cuts remain in this ledger. |
| LS-22 | P2 | Closed | `polaris.bootstrap.ports.backend_bootstrap` silently degraded launch contract types to `Any` if the canonical launch contract import failed. | The port module now imports `BackendLaunchRequest` and `BackendLaunchResult` directly from `polaris.bootstrap.contracts.backend_launch`; protocol tests assert `get_type_hints` resolves concrete launch contract types instead of `Any`. | Port annotations fail fast if the launch contract is unavailable; no runtime `ImportError -> Any` fallback remains; bootstrap port alias behavior still passes. | Closed by LS-22A. No open parent convergence cuts remain in this ledger. |
| LS-23 | P2 | Closed | `parse_export_type` silently converted unknown export types into `PM_TASK_DRAFT`, hiding invalid domain inputs behind a misleading fallback. | Codegraph showed no production callers depend on the fallback; domain tests now assert invalid and empty export types raise clear `ValueError` messages. | Invalid export type values fail fast with actionable errors; valid values still parse to `ExportType`; no silent fallback to PM task draft remains. | Closed by LS-23A. No open parent convergence cuts remain in this ledger. |
| LS-24 | P2 | Closed | `ReviewRecord.from_dict` silently accepted missing QA review identity fields and created empty `review_id` / `task_id` / `title` records. | The QA audit verdict record loader now requires non-empty review identity fields; snapshot loading logs and skips invalid records instead of materializing empty QA review facts. | Missing review identity fields raise `ValueError`; optional fields still keep defaults; QA review round-trip behavior remains covered. | Closed by LS-24A. No open parent convergence cuts remain in this ledger. |
| LS-25 | P2 | Closed | FinOps budget state deserialization silently accepted missing numeric facts and materialized missing `limit` / `amount` values as `0`. | Codegraph showed `BudgetRecord.from_dict` and `UsageRecord.from_dict` feed `BudgetKFSStore._deserialize_state`; corrupt KFS records with missing budget limits or usage amounts could become valid in-memory facts. | Required numeric budget facts raise `ValueError` when missing or not parseable as integers; optional metadata defaults remain compatible; corrupt persisted records are skipped by the existing loader warning path. | Closed by LS-25A. No open parent convergence cuts remain in this ledger. |
| LS-26 | P2 | Closed | TaskMarket work item deserialization silently accepted missing core identity/status fields and could materialize empty `task_id` / `trace_id` / `run_id` / `workspace` / `stage` / `status` values from JSON store rows. | Codegraph showed `TaskWorkItemRecord.from_dict` feeds `TaskMarketJSONStore.load_items`; public publish contracts already require these fields, but the JSON restore boundary bypassed that validation. | Work item restore requires non-empty identity fields and known queue/status values; JSON store logs and skips corrupt rows instead of legalizing them or aborting the whole load. | Closed by LS-26A. No open parent convergence cuts remain in this ledger. |
| LS-27 | P2 | Closed | Task runtime execution session restore silently accepted missing core session facts and could materialize missing `task_id` as `0` or missing `status` as `active`. | Codegraph showed `TaskExecutionSession.from_dict` feeds `TaskRuntimeService._read_session`; `_read_session` already has a corrupt-session warning path, but the model boundary legalized malformed snapshots before that path could trigger. | Execution session restore requires non-empty session/owner/status/lease facts and known session statuses; malformed snapshots raise `ValueError` and are ignored by the existing session reader recovery path. | Closed by LS-27A. No open parent convergence cuts remain in this ledger. |
| LS-28 | P2 | Closed | Factory run restore silently treated missing lifecycle status as `pending`, making corrupt run snapshots look like fresh runnable work. | Codegraph showed `FactoryRun.from_dict` feeds `FactoryStore.get_run`; the store already catches malformed run records, but the model boundary converted absent `status` into `FactoryRunStatus.PENDING` before corruption could be observed. | Factory run restore requires an explicit known lifecycle status; malformed run snapshots raise `ValueError` and are skipped by the existing store warning path. | Closed by LS-28A. No open parent convergence cuts remain in this ledger. |
| LS-29 | P2 | Closed | Runtime v2 event envelope deserialization silently generated missing event identity, run identity, channel, kind, and timestamp facts. | Codegraph showed `RuntimeEventEnvelope.from_dict` feeds `JetStreamConsumerManager._consume_messages_loop`; the consumer already catches `ValueError`, ACKs malformed messages, and keeps the WebSocket loop alive, but the envelope boundary previously legalized corrupt wire events. | Runtime event wire restore requires explicit schema, event, workspace, run, channel, kind, and timestamp facts; malformed messages fail fast and are dropped by the consumer warning path. | Closed by LS-29A. No open parent convergence cuts remain in this ledger. |
| LS-30 | P2 | Closed | Kernel audit event restore allowed missing audit identity/hash-chain facts and audit store restored last hash from raw malformed tail events. | Codegraph showed `KernelAuditEvent.from_dict` feeds `AuditStore.query` / `verify_chain`, while `_load_last_hash` used the raw last JSON record directly; malformed tail records could be skipped by query/verify yet still become the next append's `prev_hash`. | Persisted audit event restore requires explicit identity, version, hash-chain, signature, and object payload sections; audit store last-hash recovery uses the last valid restored event only. | Closed by LS-30A. No open parent convergence cuts remain in this ledger. |
| LS-31 | P2 | Closed | `polaris.kernelone.llm.tools.contracts` remained as a deprecated re-export shim beside canonical KernelOne LLM tool contracts. | Current-source audit showed no external production imports of `polaris.kernelone.llm.tools.contracts`, while package bootstrap exposed internal `tools.normalizer` / `tools.runtime` relative imports through the shim. | Retired re-export file does not exist; `tools` package internals import canonical contracts directly; production code cannot import the retired module or `contracts` from the deprecated tools package. | Closed by LS-31A. No open parent convergence cuts remain in this ledger. |
| LS-32 | P2 | Closed | `polaris.kernelone.llm.toolkit.native_function_calling` remained as a deprecated native-tool facade beside canonical parser/runtime layers. | Codegraph showed no production callers of `NativeFunctionCallingHandler` / `execute_with_native_function_calling`; current-source audit showed only toolkit package-root exports and tests kept the facade alive. The old facade preserved malformed-argument parse errors, but canonical parser/runtime had to own that fail-closed behavior before deletion. | Retired facade file does not exist; toolkit package root no longer exports retired facade symbols; canonical native parser preserves argument `parse_error`; KernelToolCallingRuntime blocks parse-error calls before executor side effects. | Closed by LS-32A. No open parent convergence cuts remain in this ledger. |
| LS-33 | P2 | Closed | Migration ledger evidence for retired `polaris/cells/compatibility/` used a vague directory-level claim that failed `legacy_file_coverage_audit`. | `check_legacy_coverage.py --workspace src/backend --json` reported `mig-compatibility-cell-removal` with note `Entire directory removed`, which is explicitly classified as insufficient file-granularity evidence. | Retired empty-cell source ref records an explicit empty file list and verification basis instead of vague directory wording; legacy coverage gate passes. | Closed by LS-33A. No open parent convergence cuts remain in this ledger. |

## Open Gap Subledger

The remaining parent gaps break down into 0 open concrete parent closure cuts, plus
closed cuts LS-03A, LS-03B, LS-03C, LS-03D1, LS-03D, LS-03E, LS-03F, LS-03G, LS-03H, LS-03I, LS-03J, LS-04A, LS-04B, LS-04C1, LS-04C2, LS-04C3, LS-04C4, LS-04C5, LS-04C6, LS-04C7, LS-04C8, LS-04C9, LS-04C10, LS-04C11, LS-04C12, LS-04C13, LS-04C14, LS-04C15, LS-04C16, LS-04C17, LS-04C18, LS-04C19, LS-04C20, LS-04C21, LS-04C22, LS-04C23, LS-04C24, LS-04C25, LS-04C26, LS-04C27, LS-04C28, LS-04D1, LS-04D2, LS-04D3, LS-04D4, LS-04D5, LS-04D6, LS-04D7, LS-04D8, LS-04E, LS-05, LS-05A, LS-05A1, LS-05A2, LS-05A3, LS-05A4, LS-05A5, LS-05A6, LS-05A7, LS-05A8, LS-05A9, LS-05A10, LS-05A11, LS-05A12, LS-05A13, LS-05A14, LS-05A15, LS-05A16, LS-05A17, LS-05A18, LS-05A19, LS-05A20, LS-05A21, LS-05A22, LS-05A23, LS-05A24, LS-05A25, LS-05A26, LS-05A27, LS-05A28, LS-05A29, LS-05A30, LS-05A31, LS-05A32, LS-05A33, LS-05A34, LS-05A35, LS-05A36, LS-05A37, LS-05A38, LS-05A39, LS-05A40, LS-05A41, LS-05A42, LS-05A43, LS-05A44, LS-05A45, LS-05A46, LS-05A47, LS-05A48, LS-05A49, LS-05A50, LS-05A51, LS-05A52, LS-05A53, LS-05A54, LS-05A55, LS-05A56, LS-05A57, LS-05A58, LS-05A59, LS-05A60, LS-05A61, LS-05A62, LS-05A63, LS-05A64, LS-05A65, LS-05A66, LS-05A67, LS-05A68, LS-05A69, LS-05A70, LS-05A71, LS-05A72, LS-05A73, LS-05A74, LS-05A75, LS-05A76, LS-05A77, LS-05A78, LS-05A79, LS-05A80, LS-05A81, LS-05A82, LS-05A83, LS-05A84, LS-05A85, LS-05A86, LS-05A87, LS-05A88, LS-05A89, LS-05A90, LS-05A91, LS-05A92, LS-05A93, LS-05A94, LS-05A95, LS-05A96, LS-05A97, LS-05A98, LS-05A99, LS-05A100, LS-05A101, LS-05A102, LS-05B, LS-05C, LS-06A, LS-06B, LS-06C, LS-06D, LS-06E, LS-06F, LS-06G, LS-10A, LS-10B, LS-10C1, LS-10C2, LS-10C3, and LS-10C4 below. New gaps must be opened as a new LS item with concrete evidence rather than reopening a closed cut.

Additional closed cuts: LS-14A, LS-15A, LS-16A, LS-17A, LS-18A, LS-19A, LS-20A, LS-21A, LS-22A, LS-23A, LS-24A, LS-25A, LS-26A, LS-27A, LS-28A, LS-29A, LS-30A, LS-31A, LS-32A, LS-33A, LS-41A, LS-42A, LS-43A, LS-44A, LS-45A, LS-46A, LS-47A, LS-48A, LS-49A.

| Sub-ID | Parent | Priority | Owner Layer | Concrete Remaining Gap | Closure Cut |
| --- | --- | --- | --- | --- | --- |
| LS-03A | LS-03 | P1 | `director.runtime.public` | Closed: materialization bridge no longer computes runtime-shadow allowed paths through direct `plan_director_repair` calls. | Closed by `QueryDirectorRepairMaterializationAllowedPathsV1` / `query_director_repair_materialization_allowed_paths`; bridge only consumes `allowed_paths`. Verified with targeted runtime contract/export tests, bridge target-runtime test, scheduler materialization test, and negative grep for `plan_director_repair` in the bridge. |
| LS-03B | LS-03 | P1 | `director.runtime.public` | Closed: source-tool candidate and plan-probe filtering no longer live in `materialization_quality_repair_bridge.py`. | Closed by `QueryDirectorRepairMaterializationPlanProbeV1` / `query_director_repair_materialization_plan_probe`; runtime public now owns coverage-to-candidate and plan-probe-to-plannable filtering, including step-owned `runtime_source_tools`. Verified with `rtk pytest src/backend/polaris/cells/director/runtime/tests/test_repair_kernel_contract.py -q -k "materialization_plan_probe or public_materialization_quality_schedule"`, `rtk pytest src/backend/polaris/cells/director/runtime/tests/test_repair_kernel_public_exports.py -q`, targeted `rtk ruff check`, and negative `rtk grep` showing no `QueryDirectorRepairPlanProbeV1` / `query_director_repair_plan_probe` or `_MATERIALIZATION_*SOURCE_TOOLS` ownership remains in the bridge. |
| LS-03C | LS-03 | P1 | `director.runtime.public` | Closed: `scheduler_bridge`, migration debt, and callback receipt lifecycle projection are now published by the runtime public bridge metadata projector. | Closed by expanding `ProjectDirectorRepairMaterializationBridgeMetadataV1` / `project_director_repair_materialization_bridge_metadata` to own scheduler bridge evidence, migration debt, adapter projection debt, and receipt lifecycle projection. The bridge now collects adapter evidence and republishes aliases from the runtime-owned summary. The runtime schedule also exposes consistent `runtime_source_tools` for post-execution and materialization steps, and TypeScript materialization compiler repair includes `deterministic_typescript_unknown_member_access_repair`. Verified with `rtk pytest src/backend/polaris/cells/director/runtime/tests/test_repair_kernel_contract.py -q -k "post_execution_schedule or materialization_bridge_metadata or materialization_plan_probe or public_materialization_quality_schedule"`, `rtk pytest src/backend/polaris/cells/director/runtime/tests/test_repair_kernel_public_exports.py -q`, `rtk pytest src/backend/polaris/cells/roles/adapters/tests/test_director_materialization_quality_runtime_ports.py -q -k "summary or runtime or bridge"`, targeted `rtk ruff check`, and negative `rtk grep` showing no `_build_materialization_scheduler_bridge_summary`, generic plan-probe, or materialization source-tool constants remain in the bridge. |
| LS-03D1 | LS-03 | P1 | `roles.adapters.public` | Closed: the materialization repair schedule public boundary no longer exposes only a naked argument/tuple shape. | Closed by `RunDirectorMaterializationQualityRepairScheduleCommandV1` / `DirectorMaterializationQualityRepairScheduleResultV1` and `run_director_materialization_quality_repair_schedule_result`; the legacy tuple function now delegates through the typed result and records typed boundary evidence. Verified with `rtk pytest src/backend/polaris/cells/roles/adapters/tests/test_director_adapter_repair_bridge.py -q -k "materialization_public_schedule_entrypoint_forwards_bridge or runtime_schedule"`, `rtk pytest src/backend/polaris/cells/roles/adapters/tests/test_adapter_contract.py -q`, targeted `rtk ruff check`, `rtk ruff format`, and `rtk mypy`. |
| LS-03D | LS-03 | P1 | `roles.adapters.public` | Closed: public repair schedule wrapper no longer forwards to the large internal bridge entrypoint. | Closed by `DirectorRepairMaterializationQualityFacadeResultV1` / `run_director_materialization_quality_repair_facade`; runtime public now owns coverage preaudit, schedule reconciliation, scheduled callback execution, runtime step metadata projection, and facade summary. `roles.adapters.public.run_director_materialization_quality_repair_schedule_result` binds adapter runner/base-file evidence ports and calls the runtime facade directly. Verified with runtime contract/export tests, adapter bridge/public-wrapper tests, architecture boundary tests, targeted `rtk ruff check`, negative `rtk grep` proving `roles.adapters.public.service` no longer references `run_materialization_quality_repairs`, and negative bridge search proving schedule reconciliation/tool-result metadata projection helpers no longer live in the bridge. |
| LS-03E | LS-03 | P1 | `director.runtime.public` | Closed: `run_typescript_semantic_quality_repairs` no longer exists as a side entrypoint from quality gate to bridge. | Closed by folding TypeScript semantic repair source tools into the runtime-owned materialization TypeScript compiler schedule, including `deterministic_typescript_missing_export_repair`, `deterministic_typescript_hyphenated_identifier_repair`, and `deterministic_typescript_zod_type_class_collision_repair`. `quality_gate.py` no longer imports or calls the semantic side runner, and the bridge no longer defines or exports it. Verified with architecture boundary coverage, runtime schedule coverage, adapter pure regression coverage, negative old-symbol grep, targeted `rtk ruff check`, and targeted `rtk mypy`. |
| LS-03F | LS-03 | P1 | `roles.adapters.internal.director` | Closed: `quality_gate.py` and `execute_method.py` no longer call the internal `run_materialization_quality_repairs` bridge entrypoint directly. | Closed by routing both production paths through `RunDirectorMaterializationQualityRepairScheduleCommandV1` / `run_director_materialization_quality_repair_schedule_result`, preserving `convergence_verifier` on the typed command, deleting the bridge entrypoint/export, and keeping only callback runner/projection helpers. Verified with `rtk pytest src/backend/polaris/cells/roles/adapters/tests/test_quality_gate_post_llm_guard.py src/backend/polaris/cells/roles/adapters/tests/test_director_materialization_quality_runtime_ports.py -q`, `rtk pytest src/backend/polaris/cells/roles/adapters/tests/test_director_adapter_repair_bridge.py -q -k "materialization"`, `rtk pytest src/backend/polaris/cells/roles/adapters/tests/test_director_adapter_pure.py -q -k "deterministic_materialization_repair or scaffold_synthesis_default_off or business_contract_synthesis_default_off or typescript_relative_import_case_repair or typescript_unresolved_unused_import_repair or npm_test_script_repair_handles_ts_source_require_module_not_found or typescript_comma_expected_repair or typescript_return_object_comma_repair or typescript_enum_member_separator_repair or typescript_missing_closing_brace_repair or typescript_unresolved_identifier_repair or placeholder_node_test_synthesis_default_off"`, `rtk pytest src/backend/polaris/cells/roles/adapters/tests/test_deterministic_repair_strategy_catalog.py -q -k "materialization_quality_public_wrapper or materialization"`, `rtk pytest src/backend/polaris/tests/architecture/test_director_repair_kernel_boundary.py -q -k "materialization_quality or semantic_repairs"`, targeted `rtk ruff check`, `rtk git diff --check`, negative production grep, and codegraph search showing no `run_materialization_quality_repairs` symbol. |
| LS-03G | LS-03 | P1 | `roles.adapters.internal.director` | Closed: the old monolithic `materialization_quality_repair_bridge.py` module no longer exists. | Closed by moving the remaining adapter callback/evidence ports to `materialization_quality_runtime_ports.py`, updating production imports, renaming the focused adapter tests, and leaving public service as the typed entrypoint. Verified with negative grep for `materialization_quality_repair_bridge` outside this governance ledger and targeted runtime-port tests. |
| LS-03H | LS-03 | P1 | `roles.adapters.internal.director` | Closed: `materialization_quality_runtime_ports.py` no longer hosts callback runner implementations and read-only evidence projection in one large module. | Closed by splitting callback execution/base-file collection into `materialization_quality_callback_ports.py`, moving summary/receipt/migration projections into `materialization_quality_evidence_ports.py`, and keeping `materialization_quality_runtime_ports.py` as the production facade. Verified with targeted adapter, architecture, ruff, and mypy checks. |
| LS-03I | LS-03 | P1 | `roles.adapters.internal.director` | Closed: `materialization_quality_runtime_ports.py` no longer re-exports private callback/evidence helpers. | Closed by moving private tests/callers to `materialization_quality_callback_ports.py` or `materialization_quality_evidence_ports.py`, shrinking `runtime_ports` `__all__` to the four public facade functions, adding a negative grep for `materialization_quality_runtime_ports._`, and verifying with focused adapter, architecture, ruff, diff, and codegraph checks. |
| LS-03J | LS-03 | P1 | `roles.adapters.internal.director` | Closed: production materialization-quality repair callers no longer consume the legacy tuple conversion method. | Closed by changing `quality_gate.py` and `execute_method.py` to project `DirectorMaterializationQualityRepairScheduleResultV1` locally instead of calling `to_legacy_tuple`, refreshing roles.adapters descriptor metadata, and verifying `to_legacy_tuple` remains only on the public compatibility wrapper/result method and its test. |
| LS-04A | LS-04 | P1 | `director.runtime.repair_kernel` | Closed: Python unresolved-import/package-shadow/unittest runtime repairs no longer have adapter direct-write mutator definitions. The materialization path already dispatches these diagnostics through runtime source tools, runtime tests cover alias-only unresolved imports, package-shadow bridging, and unittest runtime-failure replacement, and no-alias unresolved imports fail closed without placeholder stubs. | Closed by deleting `_apply_deterministic_python_unittest_runtime_failure_repair`, `_apply_deterministic_unresolved_import_symbol_repair`, and `_apply_deterministic_python_package_shadow_bridge_repair` from `roles.adapters.internal.director.deterministic_repairs.python_repairs`. Verification: runtime Python repair tests, adapter materialization Python-import schedule tests, architecture repair-boundary fence, targeted `rtk ruff check`, `rtk git diff --check`, negative grep, and codegraph search. |
| LS-04B | LS-04 | P1 | `director.runtime.repair_kernel` | Closed: NPM runtime dependency and package script contract repairs no longer have adapter direct-write mutator definitions. Runtime owns `deterministic_runtime_dependency_repair` and `deterministic_npm_script_contract_repair`, including structured package.json planning, receipt metadata, and environment-prep signaling. | Closed by deleting `_apply_deterministic_runtime_dependency_repair` and `_apply_deterministic_npm_test_script_repair` from `roles.adapters.internal.director.deterministic_repairs.npm_repairs`, removing direct adapter-pure characterization calls, and fencing the old helper names in architecture tests. Verified with runtime NPM/script/dependency tests, materialization bridge schedule tests, adapter pure selection, architecture fence, targeted `rtk ruff check`, and negative grep. |
| LS-04C1 | LS-04 | P1 | `director.runtime.repair_kernel` | Closed: JavaScript missing test-target repair no longer has an adapter direct-write mutator definition. Runtime catalog/contract keeps `deterministic_javascript_test_missing_target_repair` as the canonical executable source_tool. | Closed by deleting `_apply_deterministic_javascript_test_missing_target_repair` and its adapter-pure direct-write characterization test, adding bridge/architecture negative fences, and verifying runtime JS repair catalog coverage. Verification: targeted runtime JS contract/coverage tests, materialization bridge test, architecture repair-boundary fence, targeted `rtk ruff check`, negative grep, and codegraph callers. |
| LS-04C2 | LS-04 | P1 | `director.runtime.repair_kernel` | Closed: JavaScript TypeScript-annotation cleanup no longer has an adapter direct-write mutator definition. Runtime owns `deterministic_javascript_typescript_annotation_repair` as a text-replace plan that also updates placeholder export contracts. | Closed by adding runtime contract coverage for the placeholder-contract case, deleting `_apply_deterministic_javascript_typescript_annotation_repair` and its adapter-pure characterization test, and adding bridge/architecture negative fences. Verified with targeted runtime JS annotation/missing-test tests, materialization bridge test, architecture repair-boundary fence, targeted `rtk ruff check`, negative grep, and codegraph search. |
| LS-04C3 | LS-04 | P1 | `director.runtime.repair_kernel` | Closed: TypeScript TS2345 number-to-string argument repair no longer has an adapter direct-write mutator definition. Runtime owns `deterministic_typescript_number_to_string_argument_repair` with precise `text_replace` planning and editor-backed execution coverage. | Closed by deleting `_apply_deterministic_typescript_number_to_string_argument_repair` and its adapter-pure characterization test, relying on existing runtime precise-edit coverage, and adding the helper to architecture negative fences. Verified with targeted runtime TS number-to-string tests, architecture repair-boundary fence, targeted `rtk ruff check`, negative grep, `rtk git diff --check`, and codegraph search. |
| LS-04C4 | LS-04 | P1 | `director.runtime.repair_kernel` | Closed: TypeScript canvas scale return-type repair no longer has an adapter direct-write mutator definition. Runtime owns `deterministic_typescript_canvas_scale_return_type_repair` with precise type replacement and coverage tests. | Closed by deleting `_apply_deterministic_typescript_canvas_scale_return_type_repair` and its adapter-pure characterization test, relying on existing runtime canvas-scale coverage, and adding the helper to architecture negative fences. Verified with targeted runtime TS canvas-scale test, architecture repair-boundary fence, targeted `rtk ruff check`, negative grep, `rtk git diff --check`, and codegraph search. |
| LS-04C5 | LS-04 | P1 | `director.runtime.repair_kernel` | Closed: TypeScript too-few-arguments repair no longer has an adapter direct-write mutator definition. Runtime owns `deterministic_typescript_too_few_arguments_repair` with declaration-default and clamp callsite planning coverage. | Closed by deleting `_apply_deterministic_typescript_too_few_arguments_repair` and its adapter-pure characterization tests, adding runtime contract coverage for trailing declaration defaults and two-argument clamp calls, and adding the helper to architecture negative fences. Verified with targeted runtime TS too-few-arguments tests, architecture repair-boundary fence, targeted `rtk ruff check`, negative grep, `rtk git diff --check`, and codegraph search. |
| LS-04C6 | LS-04 | P1 | `director.runtime.repair_kernel` | Closed: TypeScript missing-closing-brace repair no longer has an adapter direct-write mutator definition. Runtime owns `deterministic_typescript_missing_closing_brace_repair`, and materialization already executes it through the runtime bridge. | Closed by deleting `_apply_deterministic_typescript_missing_closing_brace_repair`, preserving public materialization schedule coverage, and adding the helper to architecture negative fences. Verified with targeted runtime TS missing-brace tests, adapter materialization schedule test, architecture repair-boundary fence, targeted `rtk ruff check`, negative grep, `rtk git diff --check`, and codegraph search. |
| LS-04C7 | LS-04 | P1 | `director.runtime.repair_kernel` | Closed: TypeScript uninitialized-property repair no longer has an adapter direct-write mutator definition. Runtime owns `deterministic_typescript_uninitialized_property_repair` with public planning coverage for TS2564 default initialization. | Closed by adding runtime public planning coverage for the old adapter behavior, deleting `_apply_deterministic_typescript_uninitialized_property_repair` and its adapter-pure test, and adding the helper to architecture negative fences. Verified with targeted runtime TS uninitialized-property test, architecture repair-boundary fence, targeted `rtk ruff check`, negative grep, `rtk git diff --check`, and codegraph search. |
| LS-04C8 | LS-04 | P1 | `director.runtime.repair_kernel` | Closed: JavaScript missing-export repair no longer has an adapter direct-write mutator definition. Runtime owns `deterministic_javascript_missing_export_repair` and now carries the old adapter characterization for contract functions, iterable class-method facades, metadata constants, semantic versions, and note-shape exports. | Closed by migrating the adapter-pure missing-export scenarios into runtime contract tests, deleting `_apply_deterministic_javascript_missing_export_repair`, and adding bridge/architecture negative fences. Verified with targeted runtime JS missing-export tests, adapter materialization bridge tests, architecture repair-boundary fence, targeted `rtk ruff check`, negative grep, `rtk git diff --check`, and codegraph search. |
| LS-04C9 | LS-04 | P1 | `director.runtime.repair_kernel` | Closed: TypeScript sourcefile diagnostics repair no longer has an adapter direct-write mutator definition or adapter-owned parser/rewriter. Runtime owns `deterministic_typescript_sourcefile_diagnostics_repair` and the diagnostics rewrite planner. | Closed by strengthening runtime sourcefile diagnostics planning, deleting `_apply_deterministic_typescript_sourcefile_diagnostics_repair` plus adapter-owned parser/rewriter helpers, and adding architecture negative fences. Verified with targeted runtime TS sourcefile diagnostics test, architecture repair-boundary fence, targeted `rtk ruff check`, negative grep, `rtk git diff --check`, and codegraph search. |
| LS-04C10 | LS-04 | P1 | `director.runtime.repair_kernel` | Closed: TypeScript vitest globals repair no longer has an adapter direct-write mutator definition or adapter-owned vitest parser/import/manifest helpers. Runtime owns `deterministic_typescript_vitest_globals_repair`, and materialization schedule coverage routes the repair through the runtime bridge. | Closed by deleting `_apply_deterministic_typescript_vitest_globals_repair` plus adapter-owned vitest import/manifest helpers, preserving public materialization schedule coverage, and adding architecture negative fences. Verified with targeted adapter materialization vitest test, runtime repair-kernel vitest/conservative-planner coverage, architecture repair-boundary fence, targeted `rtk ruff check`, negative grep, and `rtk git diff --check`. |
| LS-04C11 | LS-04 | P1 | `director.runtime.repair_kernel` | Closed: JavaScript ESM/CommonJS entrypoint repair no longer has an adapter direct-write mutator definition. Runtime owns `deterministic_javascript_esm_commonjs_entrypoint_repair`, including multiline `require.main` and `module.exports` block conversion. | Closed by migrating the adapter-pure ESM/CommonJS entrypoint scenarios into runtime contract tests, adding runtime whole-file `text_replace` fallback for complex CommonJS blocks, deleting `_apply_deterministic_javascript_esm_commonjs_entrypoint_repair` plus adapter-owned ESM helper/dead TS package-type branch, and adding bridge/architecture negative fences. Verified with targeted runtime JS ESM/CommonJS tests, adapter materialization bridge tests, architecture repair-boundary fence, targeted `rtk ruff check`, negative grep, `rtk git diff --check`, and codegraph search. |
| LS-04C12 | LS-04 | P1 | `director.runtime.repair_kernel` | Closed: TypeScript escaped-newline repair no longer has an adapter direct-write mutator definition or adapter-owned parser/rewrite helper exports. Runtime owns `deterministic_typescript_escaped_newline_repair` and the escaped line-comment repair planner. | Closed by adding runtime contract coverage, deleting `_apply_deterministic_typescript_escaped_newline_repair` and `_repair_typescript_escaped_newline_in_line_comments`, removing adapter facade exports for the old parser/rewrite helpers, and adding architecture negative fences. Verified with targeted runtime TS escaped-newline coverage, adapter tenant-middleware materialization coverage, architecture repair-boundary fence, targeted `rtk ruff check`, negative grep, `rtk git diff --check`, and codegraph search. |
| LS-04C13 | LS-04 | P1 | `director.runtime.repair_kernel` | Closed: JavaScript missing-method runtime repair no longer has an adapter direct-write mutator definition or adapter-owned class/constructor helper cluster. Runtime owns `deterministic_javascript_missing_method_runtime_repair`, including collection add/list aliases, imported loop-variable class repairs, and constructor object-contract repairs. | Closed by migrating the adapter-pure missing-method scenarios into runtime repair execution tests, deleting the adapter helper cluster, and adding bridge/architecture negative fences. Verified with targeted runtime JS missing-method tests, adapter materialization bridge tests, architecture repair-boundary fence, targeted `rtk ruff check`, negative grep, `rtk git diff --check`, and codegraph search. |
| LS-04C14 | LS-04 | P1 | `director.runtime.repair_kernel` | Closed: TypeScript reexported type-binding repair no longer has an adapter direct-write mutator definition or adapter-owned duplicate parser cluster. Runtime owns `deterministic_typescript_reexported_type_binding_repair` and editor-backed execution coverage for adding the local `import type` binding. | Closed by migrating the adapter-pure type-binding scenario into a runtime execution test, deleting `_apply_deterministic_typescript_reexported_type_binding_repair` plus duplicate type-position/reexport parser helpers, and adding architecture negative fences. Verified with targeted runtime TS reexported type-binding coverage, conservative planner coverage, architecture repair-boundary fence, targeted `rtk ruff check`, negative grep, and codegraph search. |
| LS-04C15 | LS-04 | P1 | `director.runtime.repair_kernel` | Closed: TypeScript runtime reexport repair no longer has an adapter direct-write mutator definition or adapter-owned reexport failure detector facade. Runtime owns `deterministic_typescript_reexport_repair` with editor-backed execution receipt coverage for appending the runtime barrel export. | Closed by deleting `_apply_deterministic_typescript_reexport_repair`, removing adapter/realtime direct-helper tests and facade exports, relying on runtime editor-backed reexport coverage, and adding architecture negative fences. Verified with targeted runtime TS reexport coverage, conservative planner coverage, architecture repair-boundary fence, targeted `rtk ruff check`, negative grep, and codegraph search. |
| LS-04C16 | LS-04 | P1 | `director.runtime.repair_kernel` | Closed: TypeScript tsconfig lib/module repair no longer has an adapter direct-write mutator definition or adapter-owned tsconfig diagnostic predicates. Runtime owns `deterministic_typescript_tsconfig_lib_repair`, including DOM lib, `import.meta` module, and ES2021 lib/target planning. | Closed by adding runtime DOM lib planning coverage, deleting `_apply_deterministic_typescript_tsconfig_lib_repair` plus duplicate tsconfig predicate helpers, removing adapter-pure direct-helper tests, and adding bridge/architecture negative fences. Verified with targeted runtime TS tsconfig-lib coverage, adapter runtime-schedule bridge coverage, architecture repair-boundary fence, targeted `rtk ruff check`, negative grep, and `rtk git diff --check`. |
| LS-04C17 | LS-04 | P1 | `director.runtime.repair_kernel` | Closed: TypeScript member-alias repair no longer has an adapter direct-write mutator definition. Runtime owns `deterministic_typescript_member_alias_repair`, including structural-drift aliases for position, brightness/intensity, petal radius, HSL color, and id suffixes. | Closed by migrating the adapter-pure structural-drift case into a runtime execution test, extending the runtime alias planner with the old adapter behavior, deleting `_apply_deterministic_typescript_member_alias_repair`, and adding bridge/architecture negative fences. Verified with targeted runtime TS member-alias coverage, adapter bridge/pure coverage, architecture repair-boundary fence, targeted `rtk ruff check`, negative grep, and codegraph search. |
| LS-04C18 | LS-04 | P1 | `director.runtime.repair_kernel` | Closed: HTML module-script TypeScript entrypoint repair no longer has an adapter direct-write mutator definition or adapter-owned HTML diagnostic parser. Runtime owns `deterministic_html_typescript_module_script_repair` with typed text-replace planning for HTML script source rewrites. | Closed by deleting `_apply_deterministic_html_typescript_module_script_repair` and duplicate HTML module-script parser helpers, relying on existing runtime HTML module-script planning coverage, and adding bridge/architecture negative fences. Verified with targeted runtime HTML module-script coverage, adapter bridge coverage, architecture repair-boundary fence, targeted `rtk ruff check`, negative grep, and codegraph search. |
| LS-04C19 | LS-04 | P1 | `director.runtime.repair_kernel` | Closed: TypeScript unresolved-identifier repair no longer has an adapter direct-write mutator definition or adapter-owned TS2304 parser/parameter-alias helpers. Runtime owns `deterministic_typescript_unresolved_identifier_repair`, including array assertion and function-parameter alias planning. | Closed by adding runtime parameter-alias planning coverage, deleting `_apply_deterministic_typescript_unresolved_identifier_repair` plus duplicate TS2304 parser/alias helpers, preserving the materialization schedule public test, and adding bridge/architecture negative fences. Verified with targeted runtime TS unresolved-identifier coverage, adapter materialization schedule coverage, adapter bridge coverage, architecture repair-boundary fence, targeted `rtk ruff check`, negative grep, and `rtk git diff --check`. |
| LS-04C20 | LS-04 | P1 | `director.runtime.repair_kernel` | Closed: JavaScript node test-script contract repair no longer has an adapter direct-write mutator definition or duplicate adapter-owned script detector/builder exports. Runtime owns `deterministic_node_test_script_contract_repair`, and `execute_method_repair_bridge.run_node_test_script_contract_repair` already invokes the runtime source tool. | Closed by deleting `_apply_deterministic_node_test_script_contract_repair`, `_is_overstrict_node_test_script_contract`, and `_build_substantive_node_test_script` from the adapter legacy strategy host, removing their package facade exports, and adding runtime execution coverage for the positive replacement path plus an architecture fence against reintroducing the helper through the bridge. Verified with targeted runtime JS contract test, adapter pure test, architecture repair-boundary fence, targeted `rtk ruff check`, negative grep, and `rtk git diff --check`. |
| LS-04C21 | LS-04 | P1 | `director.runtime.repair_kernel` | Closed: TypeScript missing-export repair no longer has an adapter direct-write mutator definition or adapter-owned TS2305/TS2724 parser, export-target resolver, and missing-export declaration/alias helper cluster. Runtime owns `deterministic_typescript_missing_export_repair`, including TS2724 and high-confidence same-module alias planning while preserving fail-closed behavior for unsafe constructed-symbol enum aliases. | Closed by moving safe suggestion/similar-symbol alias planning into the runtime missing-export planner, deleting `_apply_deterministic_typescript_missing_export_repair` plus duplicate parser/resolver/helper code, removing direct adapter-pure helper tests, preserving materialization route coverage, and adding bridge/architecture negative fences. Verified with targeted runtime TS missing-export coverage, adapter materialization route coverage, architecture repair-boundary fence, targeted `rtk ruff check`, negative grep, codegraph search, and `rtk git diff --check`. |
| LS-04C22 | LS-04 | P1 | `director.runtime.repair_kernel` | Closed: TypeORM model normalization no longer has an adapter direct-write mutator definition. Runtime owns `deterministic_typeorm_model_normalization_repair` as a text-replace plan behind the TypeScript repair dispatcher; adapter legacy strategy host retains only the read-only normalization helper facade used by compatibility tests. | Closed by deleting `_apply_deterministic_typeorm_model_normalization_repair`, removing its direct `DirectorToolExecutor` write path, preserving runtime TypeORM planning coverage, and adding bridge/architecture negative fences. Verified with targeted runtime TypeORM/migration coverage, adapter materialization bridge coverage, architecture repair-boundary fence, targeted `rtk ruff check`, negative grep, and `rtk git diff --check`. |
| LS-04C23 | LS-04 | P1 | `director.runtime.repair_kernel` | Closed: TypeScript Zod inferred type/class collision repair no longer has an adapter direct-write mutator module or descriptor-pack helper record. Runtime owns `deterministic_typescript_zod_type_class_collision_repair`, including renaming inferred data aliases and constructor/data property references. | Closed by adding explicit runtime planner coverage for the old rewrite behavior, deleting `zod_repairs.py`, removing the stale generated descriptor entry, and adding architecture negative fencing against reintroducing the direct helper. Verified with targeted runtime Zod coverage, adapter execution regression coverage, architecture repair-boundary/descriptor coverage, targeted `rtk ruff check`, negative grep, codegraph search, and `rtk git diff --check`. |
| LS-04C24 | LS-04 | P1 | `director.runtime.repair_kernel` | Closed: TypeScript scaffold repair no longer has an adapter direct-write mutator definition or adapter-pure direct-helper tests. Runtime owns `deterministic_typescript_scaffold_repair`, generating typed package and tsconfig create-file operations through the TypeScript repair dispatcher. | Closed by deleting `_apply_deterministic_typescript_scaffold_repair`, removing its direct `DirectorToolExecutor` write path, removing direct adapter-helper tests, and hardening bridge/architecture negative fences so the scaffold path remains runtime-owned. Verified with runtime conservative planner/scaffold coverage, adapter materialization bridge coverage, architecture repair-boundary fence, targeted `rtk ruff check`, negative grep, codegraph search, and `rtk git diff --check`. |
| LS-04C25 | LS-04 | P1 | `director.runtime.repair_kernel` | Closed: TypeScript entrypoint repair no longer has an adapter direct-write mutator definition or adapter-owned entrypoint parser/aggregator tests. Runtime owns `deterministic_typescript_entrypoint_repair`, generating typed create-file operations for missing source entrypoints behind compiled `dist/`/`build/`/`out/`/`bin/` package entrypoints. | Closed by deleting `_apply_deterministic_typescript_entrypoint_repair`, removing duplicate adapter entrypoint parser/aggregator helpers and direct adapter tests, migrating safe aggregator/no-overwrite behavior into runtime public planner coverage, and adding architecture negative fencing. Verified with targeted runtime entrypoint/scaffold/conservative planner coverage, adapter materialization bridge coverage, architecture repair-boundary fence, targeted `rtk ruff check`, negative grep, codegraph search, and `rtk git diff --check`. |
| LS-04C26 | LS-04 | P1 | `director.runtime.repair_kernel` | Closed: TypeScript relative-import/case repair no longer has an adapter direct-write mutator definition. Runtime owns the split `deterministic_typescript_relative_import_case_repair`, `deterministic_typescript_unused_import_repair`, and `deterministic_typescript_unique_export_import_repair` source tools for the three old fallback branches. | Closed by deleting `_apply_deterministic_typescript_relative_import_case_repair`, preserving only read-only compatibility helpers still used elsewhere, adding runtime public planner coverage for case rewrite, unresolved unused-import removal, and unique-export import repointing, and adding bridge/architecture negative fences. Verified with targeted runtime relative-import planner coverage, adapter materialization bridge/schedule coverage, architecture repair-boundary fence, targeted `rtk ruff check`, negative grep, codegraph search, and `rtk git diff --check`. |
| LS-04C27 | LS-04 | P1 | `director.runtime.repair_kernel` | Closed: TypeScript missing-member repair no longer has an adapter direct-write mutator definition or adapter-pure direct-helper tests. Runtime owns `deterministic_typescript_missing_member_repair` with conservative planner coverage for numeric arithmetic properties, string-like interface properties, indexed shapes, inline object shapes, required object literals, and explicit unplannable static factory cases. | Closed by deleting `_apply_deterministic_typescript_missing_member_repair`, removing direct adapter-helper tests, migrating safe behavior into runtime public planner coverage, preserving unsafe/static factory behavior as fail-closed no-plan, and adding bridge/architecture negative fences. Verified with targeted runtime missing-member coverage, adapter materialization bridge coverage, architecture repair-boundary fence, targeted `rtk ruff check`, negative grep, codegraph search, and `rtk git diff --check`. |
| LS-04C28 | LS-04 | P1 | `director.runtime.repair_kernel` | Closed: a stale TypeScript repair-result writer helper remained after TypeScript/JavaScript mutators were migrated to runtime-owned source tools. It had no codegraph callers but still imported the legacy controlled-write bridge and could be mistaken for an executable adapter write path. | Closed by deleting `_write_typescript_repair_results`, `_typescript_repair_diff_excerpt`, and the now-unused `controlled_legacy_write_text` / `difflib` / `contextlib` imports from `typescript_repairs.py`. Verified by codegraph caller audit showing no callers before deletion and negative grep for `controlled_legacy_write_text` in `typescript_repairs.py`. |
| LS-04C | LS-04 | P1 | `director.runtime.repair_kernel` | Closed: TypeScript/JavaScript deterministic repairs no longer contain adapter direct-write mutator definitions after LS-04C1 through LS-04C27. Remaining direct mutators under LS-04 are now Rust/generic declared-target work tracked separately by LS-04D. | Closed by deleting or migrating all TypeScript/JavaScript direct repair helpers to runtime-owned source tools and adding architecture/bridge negative fences against reintroduction. |
| LS-04D1 | LS-04 | P1 | `director.runtime.repair_kernel` | Closed: Rust adapter direct-write wrapper definitions are retired. Runtime owns the materialization/execution entrypoints for crate-import, derive, missing-lib-target, lib-root-facade, line-suggestion, unresolved-pub-use, and trait-import source tools; adapter tests no longer call `_apply_deterministic_rust_*` helpers. | Closed by deleting all `_apply_deterministic_rust_*` wrappers, removing direct adapter-helper tests for retired wrappers, keeping runtime-backed Rust behavior tests for dependency, line-suggestion, unresolved-pub-use, and trait-import, adding runtime crate-import execution coverage, and hardening bridge/architecture negative fences. Verified with targeted Rust repair tests, materialization Rust bridge test, adapter aggregate/runtime bridge tests, architecture repair-boundary fence, production negative grep, and direct-mutator scan showing only generic `_apply_deterministic_*` helpers remain. |
| LS-04D2 | LS-04 | P1 | `director.runtime.repair_kernel` | Closed: Rust aggregate postpass and direct Rust entrypoint helpers no longer expose `run_all_rust_post_repairs`, `_run_rust_post_repair_round`, `_annotate_rust_post_repair_records`, `repair_rust_crate_imports`, `repair_rust_dependencies`, or `repair_rust_lib_root_facade`. | Closed by deleting the legacy aggregate callback and unused direct Rust entrypoint helpers, upgrading tests to assert these entrypoints are absent, and keeping post-execution Rust execution on runtime source_tool bridge bindings. Verified with targeted adapter Rust repair/bridge/pure/strategy tests, runtime Rust repair-kernel contract coverage, architecture Rust aggregate fence, targeted `rtk ruff check`, and production negative grep. |
| LS-04D3 | LS-04 | P1 | `director.runtime.repair_kernel` | Closed: generic declared-target direct executors no longer exist in the adapter legacy strategy host. The first cut moved declared-target source-tool identity and coverage into `director.runtime`, but still allowed runtime write-file fabrication from a nearby source file. | Closed by deleting `_apply_deterministic_missing_declared_target_repair`, `_apply_deterministic_pre_materialization_declared_target_repairs`, and `_apply_deterministic_declared_target_contract_repairs`, adding runtime declared-target planner/binding/coverage metadata, and hardening adapter/architecture negative fences. Superseded by LS-04D4 for the later architectural decision that missing declared targets are TaskBoundary facts, not repair write operations. |
| LS-04D4 | LS-04 | P1 | `director.runtime.repair_kernel` | Closed: declared-target file fabrication is retired. `deterministic_missing_declared_target_repair`, `deterministic_pre_materialization_declared_target_repair`, and `deterministic_declared_target_contract_repair` are now metadata-only `director.runtime` catalog entries that classify `incomplete_materialization` / `task_boundary_required`; they are no longer executable runtime repair bindings and no longer appear as `adapter_strategy_host`. | Closed by deleting the generic hygiene declared-target plan builders and runtime bindings, marking the matching registry rules `runtime_plan_available=False`, exposing de-duplicated runtime blocker reasons, and fixing plan-probe coverage so metadata-only source tools still identify the responsible unplannable source tool. Verified with `rtk pytest src/backend/polaris/cells/director/runtime/tests/test_repair_kernel_contract.py -q -k "missing_declared_target or pre_materialization_declared_target or scaffold_marker_quality"`, `rtk pytest src/backend/polaris/tests/architecture/test_director_repair_kernel_boundary.py -q -k "repair_kernel or declared_target or scaffold_marker or projection"`, runtime catalog projection showing `adapter_strategy_host_count=0`, and negative grep for declared-target plan builders/bindings. |
| LS-04D5 | LS-04 | P1 | `director.runtime.repair_kernel` | Closed: Java post-execution direct writers remained in the adapter legacy strategy host after the bridge had already switched to runtime-owned Java repair source tools. | Closed by deleting `deterministic_repairs/java_repairs.py` and the obsolete adapter test that exercised `run_all_java_post_repairs`; architecture coverage now asserts the legacy Java module does not exist and the bridge uses runtime source tools (`deterministic_java_accessor_alias_repair`, `deterministic_java_test_dependency_repair`) instead. |
| LS-04D6 | LS-04 | P1 | `director.runtime.repair_kernel` | Closed: Go and C++ direct postpass writer hosts remained after the bridge had already switched to runtime-owned Go/C++ source tools. | Closed by deleting `deterministic_repairs/go_repairs.py`, `deterministic_repairs/cpp_repairs.py`, and the obsolete adapter C++ host test; architecture coverage now asserts the legacy Go/C++ modules do not exist and the post-execution bridge continues to use runtime source tools instead. |
| LS-04D7 | LS-04 | P1 | `director.runtime.repair_kernel` | Closed: Python smoke helpers remained in the adapter legacy strategy host after materialization/runtime smoke paths had moved to `execute_method_repair_bridge.py`, and the cross-file symbol block builder had moved to `artifact_quality_diagnostics.py`. | Closed by deleting `deterministic_repairs/python_repairs.py`, removing the package-facade re-export, and adding architecture coverage that the legacy Python module is absent while materialization quality repair continues through runtime callback ports. |
| LS-04D8 | LS-04 | P1 | `director.runtime.repair_kernel` | Closed: Rust bottom/postpass helper host remained as the last language repair module with controlled legacy writer sites after the bridge had already switched to runtime-owned Rust source tools. | Closed by deleting `deterministic_repairs/rust_repairs.py`, migrating missing-module regression coverage to the runtime `deterministic_rust_missing_module_file_repair` path, changing the expected contract from guessed enum/struct generation to comment-only topology stubs, and removing the last unused `controlled_legacy_write_text` writer from `_common.py`. Architecture coverage now asserts the legacy Rust module is absent and no legacy strategy-host writer exception remains. |
| LS-04D | LS-04 | P1 | `director.runtime.repair_kernel` | Closed: the remaining language repair strategy-host execution surfaces under `deterministic_repairs/` are retired. Current scan: direct `_apply_deterministic_*_repair` definitions are 0; Python, Java, Go, C++, and Rust legacy direct postpass modules are deleted. | Closed by LS-04D1 through LS-04D8. New repair behavior must enter `director.runtime.repair_kernel` as typed Plan → Compose → Policy → Execute → Receipt rules, not adapter-owned writer hosts. |
| LS-04E | LS-04 | P1 | Architecture tests | Closed: every legacy deterministic source_tool token is now required to have a runtime catalog registration and explicit `implementation_status`; executable rules must be owned by `director.runtime`. | Closed with `test_legacy_deterministic_mutator_tokens_have_runtime_migration_status`; remaining LS-04C-D are concrete migration/removal cuts. |
| LS-05A1 | LS-05 | P1 | Role kernel / transaction boundary | Closed: `turn_execution.py` no longer owns duplicate turn/stream tool-surface planning or duplicate proposal no-tool gating. The planner now lives in `src/backend/polaris/cells/roles/kernel/internal/transaction/tool_surface.py`; `RoleExecutionKernel._request_forces_no_transaction_tools` delegates to the canonical request gate. | Closed by `TransactionToolSurfacePlan` / `plan_transaction_tool_surface`, lazy planner imports that preserve kernel package initialization order, and direct planner coverage for no-tool and mode-specific pin-target behavior. Verified with targeted ruff, mypy, compileall, `test_tool_surface.py`, `test_stream_nonstream_parity_transaction_kernel.py`, `test_role_kernel_transaction_wiring.py`, `test_transaction_kernel_facade.py`, `test_execution_control_plane_ledger.py`, and `test_tool_helpers.py`. |
| LS-05A2 | LS-05 | P1 | Role kernel / commit protocol | Closed: `RoleExecutionKernel` no longer exposes commit-protocol static shims (`_pre_commit_validate`, `_execute_commit_protocol`, `_post_commit_seal`, `_commit_turn_to_snapshot`, `_build_turn_history_and_events`). `turn_execution.py` and commit/snapshot tests now consume `commit_protocol.py` directly. | Closed by deleting the wrapper methods from `core.py`, updating `commit_protocol.py` documentation, aligning execution-control fake ledgers with the real `TurnLedger` protocol, and verifying direct commit-protocol tests plus role execution ledger coverage. Negative scan now finds those function names only in canonical `commit_protocol.py` definitions and direct consumers. |
| LS-05A3 | LS-05 | P1 | Role kernel / tool policy | Closed: `RoleExecutionKernel` no longer exposes tool-policy static shims (`_cognitive_runtime_blocked_tools`, `_iter_tool_policy_values`, `_normalize_tool_policy_name`, `_filter_cognitive_blocked_tool_definitions`, `_runtime_tool_policy_from_context`, `_apply_runtime_tool_policy`). Core tool execution and facade tests now consume `tool_policy.py` directly. | Closed by deleting the wrapper methods from `core.py`, updating `tool_policy.py` documentation, removing stale fake-kernel policy methods, and verifying direct tool-policy coverage plus execution-control/transaction facade tests. Negative scan now finds those function names only in canonical `tool_policy.py` definitions. |
| LS-05A4 | LS-05 | P1 | Role kernel / request gating | Closed: `RoleExecutionKernel` no longer exposes request/tool-contract gating static shims (`_tool_contract_requires_no_tools`, `_benchmark_requires_no_tools`, `_request_forces_no_transaction_tools`). Tests and tool-surface planning now consume `request_tool_gating.py` directly. | Closed by deleting wrapper methods from `core.py`, removing stale fake-kernel gating methods, migrating direct assertions to canonical `request_tool_gating.py`, and verifying targeted ruff, mypy, compileall, request-gating negative scan, and role-kernel transaction/facade/execution-control tests. |
| LS-05A5 | LS-05 | P1 | Role kernel / context handoff | Closed: `RoleExecutionKernel` no longer exposes `_build_context_handoff_pack`; handoff metadata assembly now enters through `kernel.context_assembly.build_context_handoff_pack`. | Closed by deleting the class method and stale imports from `core.py`, moving the `turn_execution.py` handoff branch and direct tests to the canonical builder, and verifying targeted ruff, mypy, compileall, old-method negative scan, handoff contract tests, transaction wiring tests, and transactional integration tests. |
| LS-05A6 | LS-05 | P1 | Role kernel / dead session helpers | Closed: dead stream context/session helper surfaces are removed. `RoleExecutionKernel._build_context_request_for_stream`, unused `context_assembly.build_context_request_for_stream`, and the unreferenced `kernel/session_manager.py` module no longer exist in non-generated source. | Closed by deleting the dead method/function/module after codegraph and source scans found no non-generated callers, and verifying targeted ruff, mypy, compileall, transaction/facade/handoff tests, and negative scan excluding generated descriptor projections. |
| LS-05A7 | LS-05 | P1 | Role kernel / dead run-loop copies | Closed: dead extracted run-loop modules `kernel/turn_runner.py` and `kernel/turn_orchestrator.py` are removed. Active execution remains in `core.py` plus canonical `turn_execution.py`; no non-generated imports or class references remain. | Closed by deleting the unreferenced modules after codegraph/source scans, moving the architecture reverse-dependency fence sample path from deleted `turn_runner.py` to active `core.py`, and verifying targeted ruff, mypy, compileall, exact architecture fence, role-kernel transaction/facade/integration tests, and old-module negative scan. |
| LS-05A8 | LS-05 | P1 | Role kernel / transaction execution delegates | Closed: `RoleExecutionKernel` no longer exposes `_execute_transaction_kernel_turn` or `_execute_transaction_kernel_stream`; production `run` / `run_stream` and tests call canonical `turn_execution.py` free functions directly. | Closed by deleting the delegate methods from `core.py`, migrating direct tests and stream characterization coverage to `execute_transaction_kernel_turn` / `execute_transaction_kernel_stream`, updating the execution module wording, and verifying targeted ruff, mypy, compileall, role-kernel transaction/facade/stream/integration tests, and old-delegate negative scan. |
| LS-05A9 | LS-05 | P1 | Role kernel / transaction factory delegate | Closed: `RoleExecutionKernel` no longer exposes `_create_transaction_kernel`; all production TransactionKernel assembly enters through `transaction_factory.create_transaction_kernel`. | Closed by deleting the class delegate from `core.py`, wiring `turn_execution.py` and `roles.runtime.public.service.create_transaction_controller` to the canonical factory, moving direct factory tests to `create_transaction_kernel(kernel, ...)`, moving execution tests to patch `turn_execution.create_transaction_kernel`, and verifying targeted ruff, mypy, compileall, role-kernel transaction/runtime strategy tests, and old-method negative scan. |
| LS-05A10 | LS-05 | P1 | Role kernel / dead tool-loop copy | Closed: stale `internal/kernel/tool_loop.py` is removed. It had no non-generated imports and duplicated older gateway cache / single-tool execution behavior that no longer matched current `core.py` semantics. | Closed by deleting the dead module after codegraph and source import scans, and verifying targeted compileall, role-kernel transaction/tool-dispatch tests, and old-module negative scan. |
| LS-05A11 | LS-05 | P1 | Role kernel / dead prompt-parse-event copy | Closed: stale `internal/kernel/prompt_parse_event.py` is removed. It had no non-generated imports and duplicated prompt/event/context helper behavior that is currently owned by `core.py` and active collaborators. | Closed by deleting the dead module after codegraph and source import scans, and verifying targeted compileall, role-kernel prompt/stream/transaction tests, and old-module negative scan. |
| LS-05A12 | LS-05 | P1 | Role kernel / dead tool-dispatch copy | Closed: stale `internal/kernel/tool_dispatch.py` is removed. It had no non-generated imports and duplicated single-tool dispatch / gateway-boundary behavior that is currently owned by `core.py` and canonical transaction factory call-time indirection. | Closed by deleting the dead module after codegraph and source import scans, and verifying targeted compileall, role-kernel tool-dispatch/transaction tests, and old-module negative scan. |
| LS-05A13 | LS-05 | P1 | Role kernel / dead budget-tracker copy | Closed: stale `internal/kernel/budget_tracker.py` is removed. It had no non-generated imports and duplicated RoleTurnResult/statistics construction that is currently owned by active `core.py` control flow. | Closed by deleting the dead module after codegraph and source import scans, and verifying targeted compileall, role-kernel transaction/result tests, and old-module negative scan. |
| LS-05A14 | LS-05 | P1 | Role kernel / request gating compatibility shim | Closed: unused `benchmark_requires_no_tools` compatibility shim is removed. Canonical no-tool gating remains `tool_contract_requires_no_tools` plus `request_forces_no_transaction_tools`. | Closed by deleting the old function, verifying no non-generated references remain, and running request-gating / transaction tool-surface tests. |
| LS-05A15 | LS-05 | P1 | Role kernel / write-tools compatibility alias | Closed: unused `internal.kernel._WRITE_TOOL_NAMES` compatibility alias is removed from package exports. Canonical write-tool classification remains `transaction.constants.WRITE_TOOLS`. | Closed by deleting the alias after codegraph and source scans found no non-generated consumers, and verifying targeted import/canonicalization tests plus old-alias negative scan. |
| LS-05A16 | LS-05 | P1 | Role kernel / core namespace re-exports | Closed: unused `core.py` backward-compat namespace re-exports are removed for delivery-mode helpers/constants, transaction turn-id helpers, `ValidationReport`, `TranscriptEvent`, `dataclass`, and `_get_suggestions_for_error`. | Closed by moving tests to canonical sibling modules, deleting the re-export imports and alias, updating the turn-id module contract comment, and verifying targeted role-kernel wiring/facade tests plus old-namespace negative scans. |
| LS-05A17 | LS-05 | P1 | Role kernel / effect-policy exception alias | Closed: unused `EffectPolicyViolation` alias is removed. Canonical strict-mode effect-policy failures raise and export `EffectPolicyViolationError`. | Closed by deleting the alias after codegraph and source scans found no non-generated consumers, updating the effect-policy docs/API list, and verifying targeted transaction effect-policy/tool-batch tests plus old-alias negative scan. |
| LS-05A18 | LS-05 | P1 | Role kernel / retry-orchestrator stdlib re-exports | Closed: unused `retry_orchestrator.py` top-level standard-library re-export imports (`json`, `os`, `re`, `Path`) are removed. Canonical retry helpers remain in their sibling modules and the canonical `RetryOrchestrator` entrypoint is unchanged. | Closed by deleting the unused imports after codegraph and source scans found no non-generated consumers, and verifying targeted transaction retry tests plus old-import negative scan. |
| LS-05A19 | LS-05 | P1 | Role kernel / stream execution legacy wording | Closed: active TransactionKernel stream execution comments no longer describe the path as a compatibility shim or legacy facade behavior. | Closed by updating `turn_execution.py` wording after codegraph confirmed the function is an active stream execution path, then verifying targeted stream/transaction tests and legacy-wording scans. |
| LS-05A20 | LS-05 | P1 | Role kernel / tool lifecycle dual-write field | Closed: TransactionKernel lifecycle writers no longer emit the legacy `tool_call_lifecycle` projection field on new events. Canonical events write `tool_call_lifecycle_receipt`; Run Ledger projection keeps read-only fallback for historical events. | Closed by deleting the duplicate field from dropped-dispatch and tool-batch writers, updating canonical test fixtures, and verifying projection/run-ledger tests plus writer negative scans. |
| LS-05A21 | LS-05 | P1 | Role kernel / job token write scope | Closed: old `allowed_paths` and `authorized_paths` no longer derive write capability scope in Role Kernel job-token handling. Explicit write authority must come from `allowed_write_paths`, `authorized_write_paths`, `target_files`, or canonical capability scope fields; old read-oriented paths now fail closed when no write scope is present. | Closed by deleting `_LEGACY_SCOPE_KEYS`, making legacy path fields read-only scope signals, updating ToolBatchExecutor token projection to use explicit write scope only, carrying the same job token on lifecycle and gate ledger events, and verifying gateway/tool-batch tests plus negative scope scans. |
| LS-05A22 | LS-05 | P1 | Role kernel / TransactionKernel documentation wording | Closed: active TransactionKernel helper modules no longer describe current controller/package boundaries as legacy facades, shims, or re-export compatibility when the wording is purely historical. | Closed by rewording docstrings/comments in `internal/kernel/{commit_protocol,context_assembly,delivery_mode,turn_execution}.py` and active `internal/transaction/*` helper modules after codegraph and marker scans separated doc-only wording from live `core.py` public entrypoints and the `retry_orchestrator.py` re-export surface. |
| LS-05A23 | LS-05 | P1 | Role kernel / request appendix helper | Closed: `RoleExecutionKernel` no longer exposes `_process_deprecated_params` as a private compatibility method. | Closed by adding canonical `internal.kernel.request_appendix.build_prompt_appendix_from_request`, moving run/run_stream to that helper, and migrating direct tests off the kernel method; the remaining old request-field behavior was retired later by LS-05A25. |
| LS-05A24 | LS-05 | P1 | Role kernel / public entrypoint wording | Closed: `RoleExecutionKernel` docs no longer describe the current public coordination entrypoint as a Facade-mode migration artifact. | Closed by rewording `core.py` module/class/method docstrings and service-boundary comments; marker scans now leave only the bounded request-appendix compatibility helper and `retry_orchestrator.py` re-export surface for future cuts. |
| LS-05A25 | LS-05 | P1 | Role kernel / deprecated request field | Closed: `RoleTurnRequest.system_prompt` is removed from the public role-turn request schema and constructor. | Closed by retiring the deprecated field from `roles.profile.internal.schema.RoleTurnRequest`, simplifying request appendix assembly to canonical `prompt_appendix` / `extra_context`, and converting migration compatibility coverage to canonical appendix behavior. |
| LS-05A26 | LS-05 | P1 | Role kernel / retry helper test imports | Closed: tests no longer import split retry helper functions/constants from the `retry_orchestrator.py` aggregate path. | Closed by moving retry helper test imports to their canonical owner modules (`read_bootstrap_progress`, `retry_context_builders`, `retry_escalation_policy`, `retry_tool_definitions`, and `bootstrap_followup`), leaving production imports on `RetryOrchestrator` only. |
| LS-05A27 | LS-05 | P1 | Role kernel / retry orchestrator exports | Closed: `retry_orchestrator.py` no longer publishes split helper functions/constants through its explicit `__all__` surface or claims a byte-compatible helper re-export contract. | Closed by shrinking `__all__` to `RetryOrchestrator` plus its runtime protocols and rewriting module ownership docs so helper callers use canonical owner modules. |
| LS-05A28 | LS-05 | P1 | Role kernel / stale compatibility wording | Closed: active role-kernel entrypoints no longer describe canonical accessors, request appendix handling, package exports, or default tool execution as compatibility or old paths. | Closed by rewording current-path comments/docstrings in `core.py`, `suggestions.py`, and `transaction/__init__.py`; behavior is unchanged. |
| LS-05A29 | LS-05 | P1 | Role kernel / tool-batch controller proxy | Closed: `TurnTransactionController._execute_tool_batch` no longer exists as a test-only proxy over `ToolBatchExecutor.execute_tool_batch`. | Closed by moving internal tests to call `controller._tool_batch_executor.execute_tool_batch(...)` directly and deleting the controller proxy method; production behavior is unchanged because no non-test caller used the proxy. |
| LS-05A30 | LS-05 | P1 | Role kernel / retry controller proxies | Closed: `TurnTransactionController` no longer exposes retry/bootstrap proxy methods over `RetryOrchestrator`. | Closed by moving internal tests to call `controller._retry_orchestrator.retry_tool_batch_after_contract_violation(...)` directly, deleting `_retry_tool_batch_after_contract_violation` and unused `_execute_read_bootstrap_batch` from the controller; production behavior is unchanged because no non-test caller used these proxy methods. |
| LS-05A31 | LS-05 | P1 | Role kernel / finalization controller proxy | Closed: `TurnTransactionController._build_finalization_context` no longer exists as a proxy over `FinalizationHandler._build_finalization_context`. | Closed by deleting the unused controller method after codegraph/source scans showed tests and production already use the `FinalizationHandler` owner directly. |
| LS-05A32 | LS-05 | P1 | Role kernel / stream LLM controller proxy | Closed: `TurnTransactionController._call_llm_for_decision_stream` no longer exists as a proxy over `StreamOrchestrator._call_llm_for_decision_stream_impl`. | Closed by wiring controller retry and stream execution closures directly to the `StreamOrchestrator` owner and moving tests to monkeypatch/call that owner directly; `execute_stream` remains the public runtime entrypoint. |
| LS-05A33 | LS-05 | P1 | Role kernel / stream execution controller proxy | Closed: `TurnTransactionController._execute_turn_stream` no longer exists as a private proxy over `StreamOrchestrator.execute_turn_stream`. | Closed by connecting public `execute_stream` directly to `StreamOrchestrator.execute_turn_stream` while preserving turn-session, correlation, TruthLog, and per-turn tool-choice override handling in the public entrypoint; internal tests now exercise the stream owner directly. |
| LS-05A34 | LS-05 | P1 | Role kernel / controller helper re-exports | Closed: `TurnTransactionController` no longer re-exports delivery-intent marker helpers or correlation ContextVars. | Closed by moving materialize-marker tests to `transaction.delivery_intent_resolver`, shrinking controller `__all__` to its real public surface, and rewording delivery-intent resolver ownership docs away from migration-symbol compatibility. |
| LS-05A35 | LS-05 | P1 | Role kernel / controller event-type reach-in | Closed: tests no longer import `CompletionEvent` through `turn_transaction_controller.py`. | Closed by moving `test_transaction_controller.py` to the canonical `polaris.cells.roles.kernel.public.turn_events.CompletionEvent` owner; controller still uses the event internally but no longer acts as the test import source. |
| LS-05A36 | LS-05 | P1 | Role kernel / controller config-state reach-ins | Closed: callers no longer import `TransactionConfig`, `TurnLedger`, or `TurnStateMachine` through `turn_transaction_controller.py`. | Closed by moving production and tests to canonical owners (`transaction.ledger.TransactionConfig`, `transaction.ledger.TurnLedger`, and `turn_state_machine.TurnStateMachine`) and making the controller internal config import private so it cannot continue as an accidental compatibility export. |
| LS-05A37 | LS-05 | P1 | Role kernel / contract-test naming | Closed: the RoleExecutionKernel public contract test no longer presents the current path as a Facade refactor or backward-compatibility suite. | Closed by renaming `test_facade_refactor.py` to `test_role_execution_kernel_contract.py`, rewording stale test classes/docstrings, and updating affected verification documents and governance cards to the canonical test path. |
| LS-05A38 | LS-05 | P1 | Role kernel / task-boundary owner | Closed: `turn_execution.py` no longer owns Director task-boundary target extraction, completed-artifact projection, or Run Ledger verdict append helpers. | Closed by adding `internal.kernel.task_boundary` as the canonical Role Kernel adapter for control-plane task-boundary verdicts, moving direct tests to the owner module, and keeping `turn_execution.py` as an execution-flow consumer only. |
| LS-05A39 | LS-05 | P1 | Role kernel / result projection owner | Closed: `turn_execution.py` no longer owns RoleTurnResult `tool_calls`/`tool_results` projection from TransactionKernel batch receipts. | Closed by adding `internal.kernel.role_result_projection` as the canonical dict projection owner and direct projection tests while keeping transaction-layer `ToolResult` parsing separate. |
| LS-05A40 | LS-05 | P1 | Role kernel / tool-dispatch error projection owner | Closed: `turn_execution.py` no longer owns TransactionKernel error-path LLM metadata extraction or `tool_dispatch_dropped` Run Ledger lifecycle append logic. | Closed by adding `internal.kernel.tool_dispatch_projection` as the canonical error projection owner with direct metadata tests; `turn_execution.py` now only catches errors and calls the owner. |
| LS-05A41 | LS-05 | P1 | Role kernel / tool-choice planning owner | Closed: `turn_execution.py` no longer parses `_transaction_kernel_forced_tool_choice` from context overrides. | Closed by adding `tool_choice_override` to `TransactionToolSurfacePlan` and resolving provider-native function/required overrides inside `transaction.tool_surface`, with direct planner tests for `none`, function, and `required` cases. |
| LS-05A42 | LS-05 | P1 | Role kernel / controller boundary wording | Closed: `TurnTransactionController` no longer describes the active control boundary as a Facade or compatibility shim. | Closed by rewording module/class comments and callback-owner notes in `turn_transaction_controller.py`; behavior is unchanged and marker scans now find no Facade/legacy/shim wording in the active controller module. |
| LS-05A43 | LS-05 | P1 | Role kernel / residual delegate wording | Closed: active Role Kernel execution docs and public contract tests no longer use delegate wording that made current direct-call boundaries look like old proxy seams. | Closed by rewording `turn_execution.py` ownership docs and renaming the affected public contract tests from delegate wording to use/call wording; behavior and assertions are unchanged. |
| LS-05A44 | LS-05 | P1 | Role kernel / injected tool gateway adapter | Closed: the injected `ToolGatewayPort` bridge is no longer named or documented as a backward-compatible delegating gateway. | Closed by renaming `_DelegatingToolGateway` to `_ToolGatewayPortAdapter`, updating `KernelToolExecutor.create_gateway`, and moving direct tests to current adapter/default-path terminology. |
| LS-05A45 | LS-05 | P1 | Role kernel / Transcript IR internal shim | Closed: the retired `roles.kernel.internal.transcript_ir` re-export shim is deleted. | Closed after scans showed all live non-generated imports already use `roles.kernel.public.transcript_ir`; the public Transcript IR docs now state the old internal path is retired, and architecture coverage fences the deleted shim from returning. |
| LS-05A46 | LS-05 | P1 | Role kernel / ContextGateway render helper re-exports | Closed: `RoleContextGateway` no longer re-exports blueprint/verdict render helper functions for test import reach-in. | Closed by moving `test_blueprint_overview_render.py` imports to `context_gateway.gateway_helpers` and removing the helper re-export imports from `gateway.py`; production callers already used owner modules. |
| LS-05A47 | LS-05 | P1 | Role kernel / ContextGateway telemetry wrappers | Closed: `RoleContextGateway` no longer exposes context build or prefix drift telemetry delegate wrappers. | Closed by routing `build_context()` directly to `GatewayTelemetry`, moving emitter/consumer tests to the telemetry owner, preserving gateway-module monkeypatch behavior through `GatewayTelemetry`, and verifying no old telemetry wrapper symbols remain. |
| LS-05A48 | LS-05 | P1 | Role kernel / ContextGateway context-override wrappers | Closed: `RoleContextGateway` no longer exposes context override or history tool-message fallback delegate wrappers. | Closed by routing `build_context()` directly to `ContextOverrideProcessor`, moving fallback unit tests to the processor owner, and verifying no gateway wrapper definitions/calls remain. |
| LS-05A49 | LS-05 | P1 | Role kernel / ContextGateway projection formatter wrappers | Closed: `RoleContextGateway` no longer exposes projection message or ContextOS snapshot formatter delegate wrappers, and no longer keeps a formatter back-reference field. | Closed by moving gateway reach-in tests to `ProjectionFormatter`, routing `ProjectionDictBuilder` formatting directly to the formatter owner, refreshing `roles.kernel` descriptor metadata, and verifying no old gateway wrapper definitions/calls remain. |
| LS-05A50 | LS-05 | P1 | Role kernel / ContextGateway compression wrappers | Closed: `RoleContextGateway` no longer exposes token estimation, compression, or emergency fallback delegate wrappers. | Closed by moving direct compression tests to `CompressionEngine`, changing build-context tests to patch `TokenEstimator` / `CompressionEngine` collaborators directly, refreshing `roles.kernel` descriptor metadata, and verifying no old gateway wrapper definitions/calls remain. |
| LS-05A51 | LS-05 | P1 | Role kernel / ContextGateway projection dict wrapper | Closed: `RoleContextGateway` no longer exposes projection-dict assembly through `_build_projection_dict`. | Closed by routing build-context directly to `ProjectionDictBuilder.build`, moving fallback tests to patch the builder owner, refreshing `roles.kernel` descriptor metadata, and verifying no old gateway wrapper definition/call/docstring remains. |
| LS-05A52 | LS-05 | P1 | Role kernel / ContextGateway signal wrappers | Closed: `RoleContextGateway` no longer exposes signal-source, blueprint-step, or signal-budget delegate wrappers. | Closed by routing `ProjectionDictBuilder` directly to `SignalSourceProvider` and `build_blueprint_step_card`, moving tests to the owner surfaces, refreshing `roles.kernel` descriptor metadata, and verifying no old gateway wrapper definitions/calls remain. |
| LS-05A53 | LS-05 | P1 | Role kernel / ToolExecutor delegate wrappers | Closed: `RoleExecutionKernel` no longer exposes unused private ToolExecutor delegate wrappers or stale `patch_kernel` scripts. | Closed by deleting the unused `_create_gateway`, `_execute_tools`, write-budget, tool-event, deferred-notice delegate methods, deleting the retired `patch_kernel.py/.ps1` scripts, moving tests to `KernelToolExecutor`, refreshing `roles.kernel` descriptor metadata, and updating the direct-write allowlist. |
| LS-05A54 | LS-05 | P1 | Role kernel / tool-call parsing wrapper | Closed: `RoleExecutionKernel` no longer exposes `_parse_content_and_thinking_tool_calls`. | Closed by moving tool-call parsing and deduplication to `OutputParser`, removing tests and stream-visible-output monkeypatches that reached into the kernel wrapper, refreshing `roles.kernel` descriptor metadata, and verifying no old parser wrapper symbol remains. |
| LS-05A55 | LS-05 | P1 | Role kernel / unused turn materializer | Closed: the unexported `TurnMaterializer` component no longer exists as a test-only turn-engine residue. | Closed by deleting `turn_materializer.py`, moving transcript wrapper sanitation to `turn_engine.utils.sanitize_assistant_transcript_message`, wiring `OutputParser` to the canonical sanitizer, retargeting semantic-stage tests to the actual owner helpers, refreshing `roles.kernel` descriptor metadata, and verifying no deleted component imports remain. |
| LS-05A56 | LS-05 | P1 | Role kernel / prompt assembly owner | Closed: `RoleExecutionKernel` no longer owns prompt-profile attachment or prompt-layer option resolution methods. | Closed by adding `kernel.prompt_assembly` as the prompt-system assembly owner, wiring `run` / `run_stream` to call that owner, moving direct prompt-layer tests off private `RoleExecutionKernel` methods, refreshing `roles.kernel` descriptor metadata, and verifying the old private method names have no runtime definitions or callers. |
| LS-05A57 | LS-05 | P1 | Role kernel / context request owner | Closed: `RoleExecutionKernel` no longer owns role-turn `ContextRequest` projection through `_build_context`. | Closed by adding `kernel.context_request_builder.build_context_request`, wiring `run` to the owner, moving context request tests off the private kernel method, refreshing `roles.kernel` descriptor metadata, and verifying `_build_context` has no roles-kernel definitions or callers. |
| LS-05A58 | LS-05 | P1 | Role kernel / retired transaction selector | Closed: `RoleExecutionKernel` no longer exposes the retired `_use_transaction_kernel` private selector. | Closed by deleting the always-true selector, converting transaction-wiring coverage to a negative retired-selector contract plus canonical `execute_transaction_kernel_turn` availability, refreshing roles.kernel descriptor metadata, and verifying `_use_transaction_kernel` only remains in the negative test assertion. |
| LS-05A59 | LS-05 | P1 | Role kernel / retired response schema selector | Closed: `RoleExecutionKernel` no longer exposes the no-op `_get_response_schema` private selector. | Closed by deleting the selector, passing the current no-schema contract explicitly as `response_schema=None` into TransactionKernel execution, refreshing roles.kernel descriptor metadata, and verifying no roles-kernel source or descriptor publishes `_get_response_schema`. |
| LS-05A60 | LS-05 | P1 | Role kernel / ContextGatewayConfig factory owner | Closed: `RoleExecutionKernel` no longer owns ContextGatewayConfig factory invocation through `_build_context_gateway_config`. | Closed by adding `kernel.context_gateway_config_builder.build_context_gateway_config`, exposing the injected factory through a read-only kernel property, routing turn execution and runtime wiring tests through the owner helper, refreshing roles.kernel descriptor metadata, and verifying the old private method has no roles-kernel definitions or calls. |
| LS-05A61 | LS-05 | P1 | Role kernel / dead stream log helpers | Closed: `RoleExecutionKernel` no longer exposes unused `_emit_stream_log_event` or `_build_stream_log_writer` helpers. | Closed by deleting both dead helpers and their log-writer imports, updating stale stream-chat audit comments to describe the current kernel streaming boundary, refreshing roles.kernel descriptor metadata, and verifying the old helper names have no roles-kernel source or descriptor hits. |
| LS-05A62 | LS-05 | P1 | Role kernel / stream run-id owner | Closed: `RoleExecutionKernel` no longer owns stream run-id resolution through `_resolve_stream_run_id`. | Closed by adding `kernel.stream_run_id.resolve_stream_run_id`, routing `run_stream` through that owner, moving latest-run file I/O out of core, adding focused helper tests for explicit, latest-run, and fallback ids, refreshing roles.kernel descriptor metadata, and verifying the old private method has no roles-kernel source or descriptor hits. |
| LS-05A63 | LS-05 | P1 | Role kernel / event emission owner | Closed: `RoleExecutionKernel` no longer exposes `_emit_event` as an event-emitter delegate wrapper. | Closed by routing validation fail/retry/pass events directly to `KernelEventEmitter.emit_runtime_llm_event`, moving stale characterization tests off deleted `_build_context` and `_emit_event` private patches onto `build_context_request` and injected event-emitter owner seams, refreshing roles.kernel descriptor metadata, and verifying the deleted wrapper has no roles-kernel source or descriptor hits. |
| LS-05A64 | LS-05 | P1 | Role kernel / tool-gateway turn-key owner | Closed: `RoleExecutionKernel` no longer owns `_resolve_tool_gateway_turn_key`. | Closed by adding `kernel.tool_gateway_turn_key.resolve_tool_gateway_turn_key` and `resolve_explicit_turn_key`, routing tool execution cache reuse and explicit boundary reset through that owner, moving contract tests off the private static kernel method, refreshing roles.kernel descriptor metadata, and verifying the old private method has no roles-kernel source or descriptor hits. |
| LS-05A65 | LS-05 | P1 | Role kernel / single-tool execution owner | Closed: `RoleExecutionKernel` no longer exposes `_execute_single_tool`. | Closed by adding `kernel.tool_runtime_executor.execute_single_tool`, routing TransactionKernel tool runtime through that owner, moving direct tests and monkeypatch seams off the kernel private method, refreshing roles.kernel descriptor metadata, and verifying no roles-kernel source or descriptor publishes the deleted method. |
| LS-05A66 | LS-05 | P1 | Role kernel / output-parser owner | Closed: `RoleExecutionKernel` no longer exposes `_get_output_parser`. | Closed by adding `kernel.output_parser_provider.get_output_parser`, routing core structured parsing and TransactionKernel result parsing through that owner, moving lazy-load tests and execution-control fakes off the private kernel method, refreshing roles.kernel descriptor metadata, and verifying no roles-kernel source or descriptor publishes the deleted method. |
| LS-05A67 | LS-05 | P1 | Role kernel / prompt-builder owner | Closed: `RoleExecutionKernel` no longer exposes `_get_prompt_builder`. | Closed by adding `kernel.prompt_builder_provider.get_prompt_builder`, routing run/stream prompt fingerprint and system-prompt assembly through that owner, moving lazy-load tests and prompt-builder fakes off the private kernel method, refreshing roles.kernel descriptor metadata, and verifying no roles-kernel source or descriptor publishes the deleted method. |
| LS-05A68 | LS-05 | P1 | Role kernel / quality-checker owner | Closed: `RoleExecutionKernel` no longer exposes `_get_quality_checker`. | Closed by adding `kernel.quality_checker_provider.get_quality_checker`, routing validation-loop quality checks through that owner, moving lazy-load tests and validation-loop fakes off the private kernel method, refreshing roles.kernel descriptor metadata, and verifying no roles-kernel source or descriptor publishes the deleted method. |
| LS-05A69 | LS-05 | P1 | Role kernel / event-emitter owner | Closed: `RoleExecutionKernel` no longer exposes `_get_event_emitter`. | Closed by adding `kernel.event_emitter_provider.get_kernel_event_emitter`, routing observer run-id resolution and validation runtime events through that owner, moving lazy-load tests and run-loop event fakes off the private kernel method, refreshing roles.kernel descriptor metadata, and verifying no roles-kernel source or descriptor publishes the deleted method. |
| LS-05A70 | LS-05 | P1 | Role kernel / LLM-invoker owner | Closed: `RoleExecutionKernel` no longer exposes `_get_llm_invoker`. | Closed by adding `kernel.llm_invoker_provider.get_llm_invoker`, routing TransactionKernel assembly through that owner, moving the canonical-invoker contract test off the private kernel method, refreshing roles.kernel descriptor metadata, and verifying no roles-kernel source or descriptor publishes the deleted method. |
| LS-05A71 | LS-05 | P1 | Role kernel / DI contract tests | Closed: RoleExecutionKernel contract tests no longer assert injected-service private fields directly. | Closed by retargeting dependency-injection assertions to the current owner seams (`get_llm_invoker`, prompt/output/quality/event providers, and `execute_single_tool`) while preserving the public injection API and refreshing roles.kernel descriptor metadata. |
| LS-05A72 | LS-05 | P1 | Role kernel / provider wording | Closed: `RoleExecutionKernel` no longer documents injected-service defaults as being handled by deleted `_get_*` methods. | Closed by updating the core constructor comment to describe provider-owner lazy loading, preserving runtime behavior, refreshing roles.kernel descriptor metadata, and verifying the stale `_get_*` wording no longer appears in active roles-kernel core surfaces. |
| LS-05A73 | LS-05 | P1 | Role kernel / invoker unwrap fallback | Closed: TransactionKernel assembly no longer carries a dead `_get_invoker` unwrap fallback beside the canonical LLM invoker provider. | Closed by deleting the unreachable `_get_invoker` compatibility branch from `create_transaction_kernel`, keeping the canonical `get_llm_invoker` owner as the only assembly source, refreshing roles.kernel descriptor metadata, and verifying no `_get_invoker` references remain in roles paths. |
| LS-05A74 | LS-05 | P1 | Role kernel / LLM service delegates | Closed: `RoleExecutionKernel` no longer exposes `call` / `call_stream` public delegate entrypoints over the injected LLM invoker. | Closed by deleting the unused kernel-as-LLM service methods, removing their contract tests, preserving direct `LLMInvoker`/provider-owner usage for TransactionKernel assembly, refreshing roles.kernel descriptor metadata, and verifying no production caller targets the deleted methods. |
| LS-05A75 | LS-05 | P1 | Role kernel / tool-runtime turn boundary | Closed: `RoleExecutionKernel` no longer exposes `reset_tool_gateway_turn_boundary` as a public helper for TransactionKernel assembly. | Closed by moving explicit cached tool-gateway turn-boundary reset into `kernel.tool_runtime_executor.reset_cached_tool_gateway_turn_boundary`, routing `transaction_factory` through that owner, retargeting the idempotency contract test to the owner seam, refreshing roles.kernel descriptor metadata, and verifying no source or descriptor publishes the deleted kernel method. |
| LS-05A76 | LS-05 | P1 | Role kernel / RoleTurnResult metadata projection | Closed: `turn_execution.py` no longer owns successful RoleTurnResult provider/model/context-audit metadata projection inline. | Closed by adding `role_result_projection.role_result_metadata_from_profile`, routing both non-stream and stream success paths through that owner, adding focused projection tests for LLM response metadata and monitoring context audit precedence, refreshing roles.kernel descriptor metadata, and verifying no duplicate successful metadata projection block remains in `turn_execution.py`. |
| LS-05A77 | LS-05 | P1 | Role kernel / service contracts | Closed: deprecated public `IToolExecutor` service alias no longer exists beside canonical `CellToolExecutorPort`. | Closed by deleting the `IToolExecutor` Protocol and dynamic deprecation `__getattr__`, removing the service package export, updating active RoleExecutionKernel and KernelOne tool-contract wording to `CellToolExecutorPort`, refreshing roles.kernel descriptor metadata, and verifying no `IToolExecutor` references remain. |
| LS-05A78 | LS-05 | P1 | Role kernel / internal service contracts | Closed: internal `ToolExecutorProtocol` compatibility contract no longer exists beside canonical `CellToolExecutorPort`. | Closed by deleting the internal services `ToolExecutorProtocol` Protocol and dynamic deprecation `__getattr__`, retargeting `KernelServiceProtocol.tool_executor` and contract tests to `CellToolExecutorPort`, removing the internal testing alias, refreshing roles.kernel descriptor metadata, and verifying no `ToolExecutorProtocol` references remain. |
| LS-05A79 | LS-05 | P1 | Role kernel / task-boundary projection | Closed: deferred follow-up task-boundary Run Ledger event construction no longer lives inline in `turn_execution.py`. | Closed by moving the shared task-boundary event writer and deferred-follow-up append path into `kernel.task_boundary`, routing `turn_execution.py` through `append_deferred_followup_task_boundary_verdict`, adding owner-level coverage for the emitted Run Ledger command shape, refreshing roles.kernel descriptor metadata, and verifying the direct `build_deferred_followup_task_boundary_verdict` import is gone from `turn_execution.py`. |
| LS-05A80 | LS-05 | P1 | Role kernel / test dependency injection | Closed: shared role-kernel test harnesses no longer use post-construction `inject_*` mutators for fake LLM or default prompt-builder dependencies. | Closed by passing `llm_invoker` and `prompt_builder` through `RoleExecutionKernel` construction in the internal testing harness and shared pytest conftest, keeping test setup on the same constructor-time dependency boundary used by production/runtime assembly. |
| LS-05A81 | LS-05 | P1 | Role kernel / constructor dependency injection | Closed: `RoleExecutionKernel` no longer exposes post-construction `inject_*` mutator methods or test-side private injected-service field writes. | Closed by migrating remaining kernel tests to constructor-time `llm_invoker`, `prompt_builder`, and `event_emitter` dependencies, deleting the unused private prompt-builder patch helper, removing the public `inject_*` methods from `core.py`, and verifying no non-owner `inject_*` or injected-service field writes remain. |
| LS-05A82 | LS-05 | P1 | Role kernel / task-boundary branch owner | Closed: `turn_execution.py` no longer owns the ordinary-vs-deferred task-boundary verdict branch decision. | Closed by adding `append_role_turn_task_boundary_verdict` to `kernel.task_boundary`, routing non-stream and stream TransactionKernel completion paths through that owner, covering completed and deferred Run Ledger event shapes in owner tests, refreshing roles.kernel descriptor metadata, and verifying the old direct append helpers are no longer imported or called from `turn_execution.py`. |
| LS-05A83 | LS-05 | P1 | Role kernel / prompt template public boundary | Closed: `roles.kernel.public.prompt_templates_facade` no longer exists as a facade-named public shim over internal prompt templates. | Closed by adding canonical `roles.kernel.public.prompt_templates`, routing dialogue code/tests and package lazy exports through that module, deleting `prompt_templates_facade.py`, refreshing roles.kernel and llm.dialogue descriptor metadata, and verifying no `prompt_templates_facade` references remain. |
| LS-05A84 | LS-05 | P1 | Role kernel / transcript IR aliases | Closed: `roles.kernel.public.transcript_ir` no longer exports `ToolCall` / `ToolResult` backward-compat aliases beside explicit transcript types. | Closed by migrating transcript IR internals, conversation state, and transcript tests to `TranscriptToolCall` / `TranscriptToolResult`, removing the alias definitions and lazy package exports, refreshing roles.kernel descriptor metadata, and verifying old alias imports/exports no longer exist. |
| LS-05A85 | LS-05 | P1 | Role kernel / prompt-safety re-export | Closed: `roles.kernel.internal.context_gateway.prompt_safety` no longer exists as an internal re-export shim over KernelOne prompt-safety helpers. | Closed by routing `tool_loop_controller` and `ProjectionFormatter` directly to `polaris.kernelone.context.prompt_safety`, deleting the shim file, refreshing roles.kernel descriptor metadata, and verifying no imports of the retired context-gateway prompt-safety module remain. |
| LS-05A86 | LS-05 | P1 | Role kernel / redaction policy implementation | Closed: `roles.kernel.internal.policy.redaction_policy` no longer exists as a compatibility redaction implementation beside canonical `policy.layer.redaction`. | Closed by exporting `RedactionPolicy` from `policy.layer`, deleting the duplicate compatibility implementation, refreshing roles.kernel descriptor metadata, and verifying no imports of the retired redaction policy module remain. |
| LS-05A87 | LS-05 | P1 | Role kernel / sandbox policy implementation | Closed: `roles.kernel.internal.policy.sandbox_policy` no longer exists as a compatibility sandbox implementation beside canonical `policy.layer.sandbox`. | Closed by exporting `SandboxPolicy` from `policy.layer`, retiring the unused `SandboxDecision` package export, deleting the duplicate compatibility implementation, refreshing roles.kernel descriptor metadata, and verifying no imports of the retired sandbox policy module or type remain. |
| LS-05A88 | LS-05 | P1 | Role kernel / approval policy implementation | Closed: `roles.kernel.internal.policy.approval_policy` no longer exists as a compatibility approval implementation beside canonical `policy.layer.approval`. | Closed by exporting `ApprovalPolicy` from `policy.layer`, retiring the unused `ApprovalRequirement` package export, deleting the duplicate compatibility implementation, refreshing roles.kernel descriptor metadata, and verifying no imports of the retired approval policy module or type remain. |
| LS-05A89 | LS-05 | P1 | Role kernel / exploration workflow alias | Closed: `roles.kernel.internal.exploration_workflow` no longer exports the `ExplorationWorkflow` backward-compat alias beside `ExplorationWorkflowRuntime`. | Closed by migrating the exploration workflow tests to `ExplorationWorkflowRuntime`, deleting the alias from the implementation module, and verifying no roles-kernel source or tests reference the retired alias. |
| LS-05A90 | LS-05 | P1 | Role kernel / output parser alias | Closed: `roles.kernel.internal.output_parser` no longer exports the dead `ParsedToolCall` alias beside canonical parser/runtime tool-call types. | Closed by deleting the unused alias, renaming the stale parsed-tool-call test class to current terminology, and verifying no roles-kernel source or tests reference the retired alias text. |
| LS-05A91 | LS-05 | P1 | Role kernel / result projection owner | Closed: `RoleExecutionKernel.run` no longer inlines the common TransactionKernel-to-RoleTurnResult field projection in its transaction-error, validation-exhaustion, and success branches. | Closed by adding `role_turn_result_from_transaction_result` to `kernel.role_result_projection`, routing the three common core result paths through that owner, adding projection-owner coverage for copied fields/stats, refreshing roles.kernel descriptor metadata, and verifying the remaining direct `RoleTurnResult` construction in core is limited to pre-TransactionKernel setup failures. |
| LS-05A92 | LS-05 | P1 | Role kernel / transaction adapter error projection | Closed: `turn_execution.py` no longer inlines RoleTurnResult construction for tool-filter conflict and TransactionKernel exception paths. | Closed by adding `role_turn_error_result` to `kernel.role_result_projection`, routing adapter-boundary error paths through that owner, adding projection-owner coverage for profile/fingerprint/stat/metadata copying, and preserving existing tool-filter and tool-dispatch-dropped behavior tests. |
| LS-05A93 | LS-05 | P1 | Role kernel / completion result projection owner | Closed: `turn_execution.py` no longer inlines the normal committed-turn RoleTurnResult construction after ContextOS commit and task-boundary verdict append. | Closed by adding `role_turn_completion_result` to `kernel.role_result_projection`, routing the final committed-turn result through that owner, adding projection-owner coverage for copied content/tool/receipt/history/event/stat/metadata fields, and preserving transaction wiring behavior tests. |
| LS-05A94 | LS-05 | P1 | Role kernel / stream completion result projection | Closed: `turn_execution.py` no longer directly constructs RoleTurnResult for stream completion events. | Closed by routing the stream completion wrapper through `role_turn_completion_result`, preserving stream tool/result/receipt/history/event metadata behavior, and verifying no direct `RoleTurnResult(...)` construction remains in `turn_execution.py`. |
| LS-05A95 | LS-05 | P1 | Role kernel / setup failure result projection | Closed: `RoleExecutionKernel.run` no longer directly constructs RoleTurnResult for pre-TransactionKernel setup failures. | Closed by routing profile, request appendix, prompt fingerprint, system-prompt, and context-request setup failures through `role_turn_error_result`, adding owner coverage for pre-profile default stats, and verifying no direct `RoleTurnResult(...)` construction remains in `core.py` or `turn_execution.py`. |
| LS-05A96 | LS-05 | P1 | Role kernel / turn prompt setup owner | Closed: `RoleExecutionKernel.run` and `run_stream` no longer duplicate profile, prompt appendix, prompt fingerprint, and system-prompt setup. | Closed by adding `kernel.turn_prompt_setup`, routing both public turn entrypoints through that owner, adding focused setup/error-mapping tests, and verifying `core.py` no longer imports or calls the underlying prompt setup helpers directly. |
| LS-05A97 | LS-05 | P1 | Role kernel / non-stream turn flow owner | Closed: `RoleExecutionKernel.run` no longer owns the non-streaming retry, validation, metrics, and TransactionKernel dispatch loop. | Closed by adding `kernel.non_stream_turn_flow.execute_non_stream_role_turn`, routing the public `run` API through that owner, retargeting tests to the new owner seam, adding focused owner tests, and verifying `core.py` no longer imports the non-stream TransactionKernel dispatch or quality-validation helpers directly. |
| LS-05A98 | LS-05 | P1 | Role kernel / stream turn flow owner | Closed: `RoleExecutionKernel.run_stream` no longer owns stream run-id resolution, fingerprint publishing, stream TransactionKernel dispatch, or stream error event projection. | Closed by adding `kernel.stream_turn_flow.execute_stream_role_turn`, routing the public `run_stream` API through that owner, adding direct stream-flow tests, and verifying `core.py` no longer imports stream execution helpers directly. |
| LS-05A99 | LS-05 | P1 | Role kernel / stream event projection owner | Closed: `turn_execution.py` no longer owns the stream event translation state machine, completion RoleTurnResult projection, projection-outcome feedback, stream event publishing, or stream task-boundary verdict append. | Closed by adding `kernel.stream_event_projection.StreamEventProjector`, routing `execute_transaction_kernel_stream` through that owner, preserving the public stream function boundary, and verifying stream/control-plane tests still cover the translated event matrix and task-boundary side effects. |
| LS-05A100 | LS-05 | P1 | Role kernel / transaction invocation setup owner | Closed: `turn_execution.py` no longer duplicates ContextOS request assembly, ContextGateway construction, provider-message normalization, or tool-surface planning between non-stream and stream TransactionKernel adapters. | Closed by adding `kernel.transaction_invocation_setup.build_transaction_invocation_setup`, routing both TransactionKernel adapter functions through that owner, adding owner tests for turn-vs-stream marker behavior, and verifying `turn_execution.py` no longer imports ContextGateway, ToolLoopController, delivery-marker helpers, or the tool-surface planner directly. |
| LS-05A101 | LS-05 | P1 | Role kernel / transaction turn completion owner | Closed: `turn_execution.py` no longer owns non-streaming successful TransactionKernel result parsing, structured output extraction, projection feedback, ContextOS commit, task-boundary verdict append, or final RoleTurnResult assembly. | Closed by adding `kernel.transaction_turn_completion.build_transaction_turn_completion_result`, routing successful non-streaming TransactionKernel results through that owner, adding direct owner coverage for projection/commit/verdict side effects, and preserving transaction wiring and stream/control-plane regression tests. |
| LS-05A102 | LS-05 | P1 | Role kernel / transaction failure projection owner | Closed: `turn_execution.py` no longer owns tool-filter conflict result/event projection, failed projection-outcome feedback, TransactionKernel exception metadata, or tool_dispatch_dropped evidence append. | Closed by adding `kernel.transaction_failure_projection`, routing non-stream and stream adapter failure branches through that owner, adding direct owner coverage for conflict result/event behavior, and preserving transaction wiring and stream-control regression tests. |
| LS-05A | LS-05 | P1 | Role kernel / final transaction-turn seam | Closed: `RoleExecutionKernel` is a thin API shell and the old `turn_execution.py` free-function execution seam no longer exists. | Closed by retiring `turn_execution.py`, introducing `TransactionTurnExecutor` as the explicit application service over `TransactionKernel`, routing non-stream and stream flows through that service, migrating tests to the new service boundary, refreshing the active decomposition blueprint, and adding an architecture fence that blocks old file/function reintroduction. |
| LS-05B | LS-05 | P1 | Role kernel | Closed: old `turn_engine/engine.py` execution facade and retired loop components (`context_pruner.py`, `quota_manager.py`, `result_builder.py`, `round_executor.py`, `tool_executor.py`) are deleted. `turn_engine.__init__` no longer exports `TurnEngine`; runtime foundation projections now report `roles.runtime + ContextOS + TransactionKernel`; Scout recon gating moved to `transaction.recon_policy`; active roles-kernel docs now point at `TRANSACTION_KERNEL_EXECUTION_GUIDE.md`. | Closed by deleting the old execution facade/components and stale direct TurnEngine parity/component tests, retargeting policy/recon/materialization tests to `RoleExecutionKernel`, `OutputParser`, and `resolve_recon_required`, extending `test_turn_engine_compat_fence.py`, and verifying with negative grep for old imports/components, targeted role-kernel/resident tests, ruff, mypy, and compileall. |
| LS-05C | LS-05 | P1 | Role kernel | Closed: retired `kernel/turn_engine.py` and `kernel_bridge.py` second facades are deleted and fenced. Production import scan no longer finds imports of `polaris.cells.roles.kernel.internal.kernel.turn_engine` or `polaris.cells.roles.kernel.internal.kernel_bridge`; only generated descriptors and the architecture fence retain historical names. | Closed by deleting both retired files, extending `test_turn_engine_compat_fence.py` to fail if either file or `TurnEngineExecutor` is reintroduced, and verifying with precise import grep, `rtk pytest src/backend/polaris/tests/architecture/test_turn_engine_compat_fence.py -q`, `rtk pytest src/backend/polaris/cells/roles/kernel/internal/kernel/tests/test_role_execution_kernel_contract.py -q -k "TurnEngine or KernelCore or transaction or DependencyInjection"`, and `rtk proxy python -m compileall -q src/backend/polaris/cells/roles/kernel src/backend/polaris/cells/roles/runtime src/backend/polaris/cells/roles/adapters`. |
| LS-06A | LS-06 | P1 | LLM caller | Closed: `LLMCaller` still carried direct event helper implementations after request preparation and fallback builders had moved to canonical services. | Closed by turning the facade `_emit_call_*` methods into compatibility shims over `LLMEventEmitter`, preserving signatures while removing the second direct `emit_llm_event` implementation from `caller.py`; component coverage pins the delegation. |
| LS-06B | LS-06 | P1 | Role kernel | Closed: `RoleExecutionKernel` no longer exposes `LLMCaller`-named injection/cache/accessor paths. | Closed by replacing `inject_llm_caller`, `_get_llm_caller`, `_injected_llm_caller`, and `_llm_caller` with `LLMInvoker`-named DI/accessor/cache paths and adding negative facade coverage. |
| LS-06C | LS-06 | P1 | LLM tool messaging | Closed: legacy text-tool rejection notice still told callers to use `LLMCaller` native tool calling even though the canonical runtime path is `LLMInvoker`. | Closed by updating `role_integrations.py` to point at `RoleExecutionKernel / LLMInvoker` while preserving fail-closed text protocol behavior. Verified with targeted role integration tests and negative grep for the old notice. |
| LS-06D | LS-06 | P1 | LLM final request audit | Closed: streamed LLM calls could emit a `context_snapshot_ref` backed by a ContextStore snapshot without `provider_request` because `LLMInvoker` injected a four-argument stream snapshot writer while `StreamEngine` only passes `provider_request` to five-argument writers. | Closed by updating the default `LLMInvoker` stream snapshot writer to accept and forward `provider_request` to `AIExecutor._store_context_messages`, and by adding component coverage that the default stream wiring is provider-request-aware and that streamed `call_start` snapshot storage receives the canonical `roles.kernel.llm_caller.context_audit` provider request payload. |
| LS-06E | LS-06 | P1 | LLM request preparation | Closed: `LLMCaller._prepare_llm_request` still carried a full duplicate provider request preparation implementation beside `LLMRequestPreparer`, creating a second place for context projection, tool schema, response format, prompt profile, and execution-envelope metadata to drift. | Closed by replacing the facade's private preparation body with a delegation to `LLMRequestPreparer`, removing the duplicate implementation and its request-construction imports while preserving the deprecated `LLMCaller` API for compatibility callers. |
| LS-06F | LS-06 | P1 | LLM fallback request builders | Closed: `LLMCaller` still duplicated structured fallback and reasoning-truncation retry request builders even though `LLMRequestPreparer` owned the canonical implementations. | Closed by delegating the facade's fallback/retry helper methods to `LLMRequestPreparer`, deleting the duplicate builder logic and related error-handling/TaskType dependencies from `caller.py`, and adding component tests that pin both helpers to the preparer. |
| LS-06G | LS-06 | P1 | LLM caller facade | Closed: `caller.py` no longer exists as an implementation-local `LLMCaller` compatibility facade, and stale direct-facade test suites no longer keep the symbol alive. | Closed by deleting `internal/llm_caller/caller.py`, deleting the two stale direct-facade suites, preserving helper coverage in `test_llm_caller_helpers.py`, updating active migration docs/cell verification metadata, and extending boundary tests to assert the facade file is absent. |
| LS-10A | LS-10 | P2 | Runtime logging | Closed: log pipeline no longer exposes or executes the legacy dual-write adapter layer. `polaris.infrastructure.log_pipeline.adapters` is deleted, package exports no longer include `adapt_emit_event`, `adapt_emit_llm_event`, `adapt_emit_dialogue`, or `get_legacy_channel_path`, the KFS direct-write baseline no longer lists the removed module, and the `events.fact_stream` descriptor pack no longer publishes adapter capabilities. | Closed with `test_log_pipeline_dual_write_adapters_are_removed` in `src/backend/polaris/tests/architecture/test_jsonl_emit_event_invariants.py`; verified by targeted negative grep plus descriptor/baseline projection checks. Separate global KFS direct-write regressions are tracked in `POLARIS_KFS_DIRECT_WRITE_CONVERGENCE_LEDGER_20260701.md`. |
| LS-10B | LS-10 | P2 | Runtime channels | Closed: `kernelone/runtime/defaults.py` and `domain/director/constants.py` no longer define mixed channel constants locally. Both re-export `CHANNEL_FILES` and `NEW_CHANNEL_METADATA` from `polaris.kernelone.runtime.channel_contracts`, where historical aliases and canonical runtime.v2 journal channels are centralized. | Closed with `channel_contracts.py` and runtime regression coverage in `src/backend/polaris/kernelone/runtime/tests/test_channel_contracts.py`; verified with `rtk pytest src/backend/polaris/kernelone/runtime/tests/test_channel_contracts.py -q`, targeted `rtk ruff check`, `rtk mypy`, and negative `rtk grep` for local `CHANNEL_FILES` / `NEW_CHANNEL_METADATA` assignments in the old modules. |
| LS-10C | LS-10 | P2 | UI/API projection | Closed: WS/channel readers and frontend telemetry no longer normalize historical runtime event channels or event kinds. | Product UI/API routes through canonical runtime.v2/Run Ledger projection; historical workspace files remain read-only archive metadata, not live WS channel names. |
| LS-10C1 | LS-10 | P2 | Runtime WS channel helpers | Closed: product runtime WS no longer exports file-tail stream helpers, mixed `STREAM_CHANNELS`, `LEGACY_LLM_CHANNELS`, or `resolve_channel_path`; canonical channel helpers only classify `llm`, `system`, and `process`. | Closed by deleting unused `channel_stream.py` / `journal_stream.py`, removing mixed channel exports from runtime WS modules, and retargeting tests so historical `_llm` suffixes and file channels are negative cases. |
| LS-10C2 | LS-10 | P2 | Runtime channel contract split | Closed: `CHANNEL_FILES` is canonical runtime.v2-only; historical workspace archive paths are exposed only through explicit `HISTORICAL_CHANNEL_FILES` archive/read contracts. | Closed by splitting channel contracts at the KernelOne owner and updating domain/runtime projection reexports and tests so `pm_report`, `runtime_events`, and other archive paths no longer appear in `CHANNEL_FILES`. |
| LS-10C3 | LS-10 | P2 | Frontend runtime stream channels | Closed: `useRuntimeConnection` no longer subscribes to `runtime_events`; `useRuntime` maps generic runtime.v2 events to canonical `system` and LLM envelopes to canonical `llm`; frontend process channel classification accepts only `system` / `process`, with historical artifact source names bounded to an explicit source fallback. | Closed with focused hook/util tests and typecheck; remaining runtime event label debt is isolated in ContextOS telemetry projection and fixtures. |
| LS-10C4 | LS-10 | P2 | ContextOS and log viewer projections | Closed: ContextOS telemetry now treats execution logs as canonical `system` events, tests no longer fixture `runtime_events`, `WebSocketEventType` no longer includes `runtime_event`, and log modal/viewer live subscriptions use only `process`, `system`, or `llm` while retaining historical paths as archive metadata. | Closed with focused ContextOS/log tests, targeted lint/typecheck, and negative grep for `runtime_events` / `runtime_event` in `src/frontend/src/app`. |
| LS-14A | LS-14 | P2 | Roles adapters / Director schema alias | Closed: roles.adapters Director schema no longer exports the deprecated `ValidationResult` alias beside explicit `DirectorValidationResult`. | Closed by typing `DirectorOutput.validation` as `DirectorValidationResult`, deleting alias-only tests, refreshing roles.adapters descriptor metadata, and verifying no roles.adapters Director schema/test code references the retired alias. |
| LS-15A | LS-15 | P2 | Infrastructure LLM / token-service re-export | Closed: `infrastructure.llm.token_service` no longer exists as a backward-compat re-export beside the domain service owner. | Closed by migrating production imports to `polaris.domain.services`, deleting the retired re-export file, and adding `test_infrastructure_llm_token_service_fence.py` to block file/import reintroduction. |
| LS-16A | LS-16 | P2 | Roles runtime / skill-loader facade | Closed: `roles.runtime.internal.skill_loader` no longer exists as a deprecated facade over KernelOne skill loading. | Closed by deleting the internal facade and its self-preserving tests, removing the four retired skill exports from roles-runtime public boundaries, and adding `test_roles_runtime_skill_loader_fence.py` to block path/import/export reintroduction. |
| LS-17A | LS-17 | P2 | Accel verify / orchestrator facade | Closed: `infrastructure.accel.verify.orchestrator` no longer exists as a backward-compat re-export over `verify.core`. | Closed by migrating all production imports to the canonical verify core module, keeping the package root as the stable public import surface, deleting the retired facade, and adding `test_accel_verify_orchestrator_fence.py` to block path/import reintroduction. |
| LS-18A | LS-18 | P2 | Dependency injection / dead factory stubs | Closed: DI no longer exposes removed singleton factories or no-op test reset shims. | Closed by deleting the `NotImplementedError` factory stubs and no-op resets, removing lazy export mappings, updating the global pytest reset fixture, and adding `test_di_dead_factory_exports_fence.py` to block dead export/function/import reintroduction. |
| LS-19A | LS-19 | P2 | LLM evaluation / deterministic judge re-export | Closed: the deprecated `internal.deterministic_judge` re-export shim is removed. | Closed by migrating production and test imports to `internal.judge` canonical modules, deleting the retired shim file, and adding `test_llm_evaluation_judge_shim_fence.py` to block path/import reintroduction. |
| LS-20A | LS-20 | P2 | Bootstrap / runtime health re-export | Closed: `bootstrap.runtime_health` no longer exists as a deprecated re-export beside the application health owner. | Closed by migrating remaining test imports to `polaris.application.health`, deleting the retired shim file, and adding `test_bootstrap_runtime_health_shim_fence.py` to block path/import reintroduction. |
| LS-21A | LS-21 | P2 | Bootstrap ports / KernelOne error re-export | Closed: `bootstrap.ports.backend_bootstrap` no longer re-exports `BackendBootstrapError`. | Closed by keeping the port module focused on protocol contracts and the `BootstrapPort` alias, while fencing production imports so error authority stays in `polaris.kernelone.errors`. |
| LS-22A | LS-22 | P2 | Bootstrap ports / launch contract type fallback | Closed: bootstrap port annotations no longer fall back to `Any` when canonical launch contracts fail to import. | Closed by directly importing `BackendLaunchRequest` / `BackendLaunchResult` from the contract owner and extending port tests to assert concrete annotation resolution. |
| LS-23A | LS-23 | P2 | Domain export artifacts / invalid type fallback | Closed: `parse_export_type` no longer maps invalid values to `PM_TASK_DRAFT`. | Closed by making invalid or empty export type input raise `ValueError` with supported values and updating duplicated domain test suites to assert fail-fast behavior. |
| LS-24A | LS-24 | P2 | QA audit verdict / review record identity fallback | Closed: `ReviewRecord.from_dict` no longer creates empty-identity review records from malformed snapshots. | Closed by adding required field validation for `review_id`, `task_id`, and `title`, while preserving optional defaults for priority/status and updating integration tests to assert fail-fast behavior. |
| LS-25A | LS-25 | P2 | FinOps budget guard / numeric fact fallback | Closed: `BudgetRecord.from_dict` and `UsageRecord.from_dict` no longer synthesize missing budget limits or usage amounts as zero. | Closed by introducing required integer field validation for `limit` and `amount`, preserving optional metadata defaults, and adding negative tests for missing and invalid numeric facts. |
| LS-26A | LS-26 | P2 | TaskMarket / work item restore fallback | Closed: `TaskWorkItemRecord.from_dict` no longer synthesizes empty identity or state fields from malformed rows, and JSON store loading skips corrupt records with warning evidence. | Closed by adding required field/status validation in the model boundary, JSON store corrupt-row handling, model-level negative tests, and a JSON restore mixed-record regression test. |
| LS-27A | LS-27 | P2 | Task runtime / execution session restore fallback | Closed: `TaskExecutionSession.from_dict` no longer synthesizes missing persisted task/session identity or status fields into active sessions. | Closed by adding required field/status validation in the session model, preserving optional `run_id` compatibility, and adding model plus service recovery tests for corrupt session snapshots. |
| LS-28A | LS-28 | P2 | Factory pipeline / run status restore fallback | Closed: `FactoryRun.from_dict` no longer synthesizes a missing persisted lifecycle status into `pending`. | Closed by adding required lifecycle status validation in the run model and a store-level recovery test proving corrupt run snapshots are skipped with warning evidence. |
| LS-29A | LS-29 | P2 | Runtime messaging / event envelope wire fallback | Closed: `RuntimeEventEnvelope.from_dict` no longer synthesizes missing runtime.v2 wire identity, routing, or timestamp facts. | Closed by adding strict envelope field validation and consumer coverage proving malformed JetStream messages are ACKed, dropped, and not queued as valid runtime events. |
| LS-30A | LS-30 | P2 | KernelOne audit / persisted event restore fallback | Closed: `KernelAuditEvent.from_dict` no longer synthesizes missing identity/hash-chain facts, and `AuditStore._load_last_hash` ignores malformed tail events. | Closed by adding strict persisted-event validation plus a store recovery test proving next append chains to the last valid event rather than corrupt tail JSON. |
| LS-31A | LS-31 | P2 | KernelOne LLM / tool-contract re-export shim | Closed: `kernelone/llm/tools/contracts.py` no longer exists as a deprecated re-export of canonical tool contracts. | Closed by moving `tools.normalizer` and `tools.runtime` to canonical `polaris.kernelone.llm.contracts` imports, deleting the retired shim, and adding an architecture fence that blocks file recreation and production imports through the retired module path. |
| LS-32A | LS-32 | P2 | KernelOne LLM / native-function-calling facade | Closed: `kernelone/llm/toolkit/native_function_calling.py` no longer exists as a deprecated facade over native parser/runtime behavior. | Closed by deleting the facade and its self-preserving tests, removing retired symbols from `toolkit.__all__`, preserving malformed-argument evidence in `NativeFunctionCallingParser` / parser adapter, blocking parse-error calls in `KernelToolCallingRuntime`, and adding an architecture fence for the retired path/export surface. |
| LS-33A | LS-33 | P2 | Migration ledger / retired compatibility cell evidence | Closed: `mig-compatibility-cell-removal` no longer uses a vague directory-level note for the retired empty compatibility cell. | Closed by changing the source ref note to an explicit empty file list plus verification basis; `legacy_file_coverage_audit` passes without suppressing the directory granularity rule. |
| LS-34A | LS-34 | P1 | Director task consumer / stale deprecation marker | Closed: `director.task_consumer` no longer marks the active TaskMarket `pending_exec` consumer as deprecated or as a compatibility adapter for `DirectorPool`. | Closed by removing the stale `DeprecationWarning`, `__deprecated__` map, package/cell metadata deprecation text, and README deprecation claim while preserving the canonical claim -> execute -> task-boundary behavior; added construction coverage proving the active consumer no longer emits deprecation warnings. Verified with targeted task-consumer tests, ruff, negative grep for deprecated task-consumer markers, and legacy/shim governance scripts. |
| LS-35A | LS-35 | P1 | PM dispatch / duplicate TaskMarket lazy import owners | Closed: `dispatch_pipeline.py` no longer carries duplicate TaskMarket lazy-import implementations beside canonical `internal.dispatch._lazy_imports`. | Closed by routing `_get_task_market_services`, `_get_task_market_revision_services`, and `_get_task_market_consumers` through the extracted lazy-import owner while preserving `dispatch_pipeline` module attributes for existing patch seams. Verified with dispatch pipeline/contract tests, ruff, mypy, py_compile, and negative grep proving those function definitions now exist only in `_lazy_imports.py`. |
| LS-36A | LS-36 | P2 | KernelOne LLM tools / package-root re-export shim | Closed: `polaris.kernelone.llm.tools` no longer re-exports tool contracts/toolkit runtime symbols or emits a package-import `DeprecationWarning`. | Closed by making the package root namespace-only while keeping explicit submodule owners (`message_normalizer`, `normalizer`, `schema_validator`) intact; architecture fence now blocks reintroducing root re-export behavior. Verified with LLM tool submodule tests, architecture fence tests, ruff, mypy, py_compile, and negative import scans. |
| LS-37A | LS-37 | P2 | Director CLI / retired stream-audit decorator | Closed: `polaris.delivery.cli.director.audit_decorator` no longer exists as a deprecated no-op wrapper beside UEP v2 stream audit sinks. | Closed by deleting the unused CLI decorator module, shrinking the delivery-internal-import baseline, and adding an architecture fence that blocks file/import reintroduction. Verified with director audit fence, delivery internal import fence, stream audit tests, ruff, mypy, py_compile, and active-marker scan. |
| LS-38A | LS-38 | P2 | Roles runtime / internal package-root compatibility facade | Closed: `polaris.cells.roles.runtime.internal` is no longer a package-root deprecation facade or dynamic re-export surface for retired `RoleExecution*` names. | Closed by making the package root namespace-only, preserving active internal submodules such as `process_service` and `session_orchestrator`, updating stale tech-debt verification notes, and adding an architecture fence that blocks package-root warning/re-export reintroduction. |
| LS-39A | LS-39 | P2 | Roles kernel / LLM caller removed-module warning marker | Closed: `polaris.cells.roles.kernel.internal.llm_caller` no longer emits `DeprecationWarning` for already-removed module names such as `call_sync`; removed module access fails closed with a clear `ModuleNotFoundError`. | Closed by replacing the warning helper with a direct removed-module error path while preserving active `LLMInvoker` package-root exports; added an architecture fence that blocks warning marker reintroduction. |
| LS-40A | LS-40 | P2 | Roles kernel / retry error-category facade | Closed: `polaris.cells.roles.kernel.internal.error_category` no longer exists as a deprecation facade beside retry policy and KernelOne LLM error mapping. | Closed by moving retry-policy categories and helpers into `retry_policy_engine.py`, migrating public exports and tests to the retry-policy owner, fixing auto-retry sets to use the same enum as `RetryPolicyEngine.should_retry`, removing the retired module from cell metadata and descriptor projection, and adding an architecture fence against reintroduction. |
| LS-41A | LS-41 | P2 | Roles kernel / tool executor error-category compatibility alias | Closed: `polaris.cells.roles.kernel.internal.services.tool_executor` no longer exposes a module-level `ErrorCategory` compatibility alias, warning path, or `__all__` export. | Closed by using the canonical `polaris.kernelone.errors.ErrorCategory` directly in `ToolResult`, `ToolError`, `ToolExecutor`, and tests; refreshed the roles.kernel descriptor source hash; added an architecture fence that blocks alias, warning, and module-level `__getattr__` reintroduction. |
| LS-42A | LS-42 | P2 | KernelOne tool execution / retired executor stubs | Closed: `polaris.kernelone.tool_execution.executor` and `polaris.kernelone.tool_execution.executor_core` no longer exist as deprecated empty execution stubs beside `AgentAccelToolExecutor`. | Closed by deleting both retired modules, cleaning package-root documentation, moving the remaining error-code test to the canonical `validate_tool_step` contract, and adding an architecture fence that blocks retired module paths and imports from reappearing. |
| LS-43A | LS-43 | P2 | Director HTTP v2 / merge status test re-export shim | Closed: `polaris.delivery.http.v2.director` no longer exposes `_merge_director_status` as a warning-emitting test re-export beside `runtime.projection.public.service.merge_director_status`. | Closed by migrating merge-status tests to the runtime projection public service, deleting the v2 director shim and warning import, removing the stale descriptor capability, refreshing the descriptor source hash, and adding an architecture fence that blocks the re-export and descriptor projection from reappearing. |
| LS-44A | LS-44 | P2 | KernelOne tool execution / contracts normalization and spec compatibility wrappers | Closed: `tool_execution.contracts` no longer exposes warning-backed normalization/cache/spec compatibility wrappers. | Closed by migrating production chain and plan parsing to canonical `normalize_tool_arguments`, reading tool category and validation specs through `ToolSpecRegistry`, migrating active governance scripts and standards to the registry, removing `normalize_tool_args`, `_TOOL_SPECS`, `_has_value`, `reset_tool_spec_registry_cache`, and warning decorators from `contracts.py`, and adding an architecture fence that blocks reintroduction. |
| LS-45A | LS-45 | P2 | KernelOne storage / artifact lifecycle policy stub | Closed: `kernelone.storage.policy` no longer exposes empty Polaris artifact policy stubs or warning-backed helper wrappers beside `audit.verdict.artifact_service`. | Closed by deleting `ARTIFACT_POLICY_METADATA`, `get_artifact_policy_metadata`, `should_compress_artifact`, and `should_archive_artifact` from KernelOne storage policy/package exports, converting storage tests to assert the retired surface is absent, and adding an architecture fence that keeps artifact lifecycle policy ownership in `polaris.cells.audit.verdict.internal.artifact_service`. |
| LS-46A | LS-46 | P2 | KernelOne runtime / legacy Result and ErrorCodes module | Closed: `polaris.kernelone.runtime.result` no longer exists as a legacy Result/ErrorCodes compatibility module beside `polaris.kernelone.contracts.technical`. | Closed by deleting the runtime result module and its legacy-only test suite, removing `ErrorCodes` from the runtime package root, keeping `runtime.Result` as the canonical contracts-layer type, and adding an architecture fence that blocks legacy module/import/export reintroduction. |
| LS-47A | LS-47 | P2 | Context engine / search gateway ad-hoc ingest no-op | Closed: `SearchService.add_documents` no longer exists as a deprecated warning-backed no-op beside graph/catalog-backed search. | Closed by deleting the `add_documents` method and warning import from `context.engine` search gateway, updating behavior/governance tests to assert the ingest API is absent, and adding an architecture fence that blocks `add_documents`, `DeprecationWarning`, and `warnings.warn` from reappearing in the search gateway. |
| LS-48A | LS-48 | P2 | KernelOne policy / deprecated package-root facade | Closed: `polaris.kernelone.policy` no longer exists as a warning-emitting package facade that points callers to upper-layer policy cells. | Closed by deleting the retired package root, removing it from the KernelOne reverse-dependency baseline, extending legacy cleanup coverage to assert `polaris.kernelone.policy` is not importable, updating active governance docs/gates to the current `roles.kernel` policy layer and `kernelone.security` owners, and adding an architecture fence that blocks policy package source files and baseline budget from reappearing. |
| LS-49A | LS-49 | P2 | KernelOne LLM / error category re-export shim | Closed: `polaris.kernelone.llm.error_categories` no longer exists as a warning-backed `ErrorCategory` re-export and parallel classifier beside `polaris.kernelone.errors`. | Closed by migrating LLM toolkit contracts and engine resilience to import `ErrorCategory` / `classify_error` from `polaris.kernelone.errors`, deleting the retired LLM-local module and its self-preserving tests, and adding an architecture fence that blocks retired module imports while proving the canonical owner remains present. |

| LS-50A | LS-50 | P2 | KernelOne LLM toolkit / retired tool definitions bridge | Closed: `polaris.kernelone.llm.toolkit.definitions` no longer exists as a deprecated `ToolDefinition` / `ToolRegistry` / `create_default_registry` bridge beside canonical `ToolSpecRegistry`. | Closed by moving alias-aware provider schema projection into `polaris.kernelone.tool_execution.tool_spec_registry.ToolSpecRegistry`, migrating Director quality repair, materialization retry, roles-kernel native tool schema assembly, and legacy role integration schema output to that owner, deleting the retired module and package-root exports, and adding an architecture fence against file/import reintroduction. |

| LS-51A | LS-51 | P2 | Director adapter tests / retired text-file fallback success path | Closed: Director materialization retry tests no longer expect Markdown/text file blocks to write files through the retired patch fallback path. | Closed by converting the stale retry tests to assert native-tool retry schema injection followed by fail-closed `incomplete_materialization` evidence when the model returns only text file blocks; existing patch/text fallback tests continue to assert `legacy_patch_file_protocol_disabled` and no write authority. |

| LS-52A | LS-52 | P2 | Director interface / root and PM import shims | Closed: the old root `director_interface.py` shim and `polaris.delivery.cli.pm.director_interface` re-export shim are removed; internal callers already use `director_interface_core`. | Closed by deleting both shim files and adding an architecture fence that blocks file recreation or production imports through the retired root/PM paths while proving `director_interface_core` remains the canonical owner. |

| LS-53A | LS-53 | P2 | Delivery CLI / terminal_console import shim | Closed: `polaris.delivery.cli.terminal_console` no longer exists as a backward-compat re-export beside canonical `polaris.delivery.cli.terminal`. | Closed by migrating active tests to the canonical terminal package, keeping `Super*` task data types sourced from `polaris.delivery.cli.super_mode`, deleting the retired shim file, shrinking the delivery internal-import baseline, and upgrading the architecture fence to block file/import reintroduction. |

| LS-54A | LS-54 | P2 | Director CLI / terminal_console re-export shim | Closed: `polaris.delivery.cli.director.terminal_console` no longer exists as a Director-local re-export beside canonical `polaris.delivery.cli.director` and `polaris.delivery.cli.terminal`. | Closed by deleting the unused Director submodule shim and adding an architecture fence that blocks file/import reintroduction while proving the canonical Director package root still exposes `run_director_console`. |

| LS-55A | LS-55 | P2 | PM dispatch / internal error-classification re-export surface | Closed: `pm_dispatch.internal.error_classifier` no longer re-exports shared error taxonomy types beside canonical `orchestration.shared_types` / `kernelone.errors`. | Closed by limiting the internal module to pm_dispatch-local retry utilities, using the shared classifier privately inside `RetryExecutor`, migrating classification tests to canonical owners, and updating orchestration import fences to prove the internal module exposes retry helpers only. |
| LS-56A | LS-56 | P2 | Orchestration / ErrorCategory re-export surfaces | Closed: `orchestration.shared_types` and `pm_dispatch` public/package exports no longer re-export `ErrorCategory`; the enum authority is only `polaris.kernelone.errors`. | Closed by removing the `shared_types.__getattr__` deprecation shim, moving workflow imports to KernelOne, keeping `shared_types` focused on `ErrorClassifier` / `ErrorRecord` / `RecoveryRecommendation`, migrating pm_dispatch public exports and tests, and updating import fences to prove the old re-export path is absent. |
| LS-57A | LS-57 | P2 | KernelOne technical contracts / error type re-export surfaces | Closed: `kernelone.contracts.technical`, `master_types`, `kernelone.runtime`, and `application` no longer re-export `ErrorCategory` or deprecated `KernelOneError`; error taxonomy and base exception authority is only `polaris.kernelone.errors`. | Closed by removing the `master_types` deprecation shim and subclass, keeping `KernelError` / `TaggedError` / `Result` in technical contracts, migrating tests to canonical error imports, and adding fences that prove runtime and master_types no longer expose the retired error names. |
| LS-58A | LS-58 | P2 | PM dispatch / host argparse-engine compatibility parameters | Closed: `pm_dispatch.internal.dispatch_pipeline.run_dispatch_pipeline` and `run_engine_dispatch` no longer accept `args=` / `engine=` host-layer parameters or emit deprecation warnings; `DispatchCallbacks` now carries the explicit Chief Engineer analysis callback, and CE preflight no longer carries an unused `args` field. | Closed by routing factory market-chain and delivery CE node callers through typed callback/explicit runner injection, updating tests away from the engine bridge, and aligning shim-marker governance tests with the fully converged no-`shim_only` target state. Verified with focused dispatch/preflight/CE consumer tests, import-fence tests, ruff, mypy, compileall, negative legacy-parameter scans, and `check_shim_markers.py --json`. |
| LS-59A | LS-59 | P1 | PM dispatch / iteration-state host argparse boundary | Closed: `pm_dispatch.internal.iteration_state.finalize_iteration` no longer accepts unused host CLI `args`, and `handle_spin_guard` / `_handle_spin_guard` consume explicit `max_spin_rounds: int` instead of `argparse.Namespace`. | Closed by extracting `max_spin_rounds` at the delivery runner boundary, removing `argparse.Namespace` from iteration-state helper tests, and verifying with focused iteration-state/import-fence tests, ruff, mypy, compileall, and negative scans for `argparse.Namespace` / `args=args` in the Cell iteration-state surface. |
| LS-60A | LS-60 | P1 | Director adapter / legacy role-response shim | Closed: `roles.adapters.internal.director.adapter` no longer defines or references a Director-specific `generate_role_response` wrapper, and the `_call_role_llm` / `_call_role_llm_with_timeout` compatibility methods are retired in favor of `_invoke_role_dialogue` / `_invoke_role_dialogue_with_timeout`. | Closed by removing the Director-specific role-response shim from `adapter.py`, dropping `generate_role_response` from `director.__init__` and the `internal.director_adapter` facade, retargeting Director tests to canonical `_invoke_role_dialogue*` seams, and verifying the architecture guard, focused runtime-dialogue tests, ruff, mypy, compileall, shim-marker gate, and negative scans. |
| LS-61A | LS-61 | P1 | Director adapter / tool execution proxy | Closed: `DirectorAdapter._execute_tools` no longer exists as a compatibility proxy over `DirectorPatchExecutor.execute_tools`. | Closed by routing production fallback tool execution in `execute_method.py` directly through `adapter._execution.execute_tools(..., adapter._update_task_progress, ...)`, retargeting tests to patch/call the execution component owner, removing the test-local `_Adapter._execute_tools` shadow proxy, and verifying focused Director retry/tool tests, ruff, mypy, compileall, shim-marker gate, and negative scans. |
| LS-62A | LS-62 | P1 | Director tasking / patch apply compatibility module | Closed: `director.tasking.internal.patch_apply_engine` no longer exists as a compatibility layer over KernelOne protocol parsing and application. | Closed by moving the Director tasking projection contract to `director.tasking.internal.patch_protocol`, removing deprecated compatibility parameters, routing `FileApplyService` and tasking public/internal exports through the canonical adapter, renaming focused tests to `test_patch_protocol.py`, adding a fence that blocks `patch_apply_engine.py` reintroduction, and verifying focused patch protocol/task lifecycle/toolchain tests, ruff, mypy, compileall, shim-marker gate, and targeted residual scans. |
| LS-63A | LS-63 | P1 | Roles kernel / pathless SEARCH/REPLACE regex fallback | Closed: `OutputParser.extract_search_replace` no longer maintains a second legacy regex parser for pathless SEARCH/REPLACE blocks. | Closed by making roles-kernel structured patch extraction delegate only to the canonical Director/KernelOne protocol parser, preserving explicit-path standalone and `PATCH_FILE` parsing while rejecting pathless SEARCH/REPLACE text as non-structural output. Focused OutputParser and KernelOne protocol parser tests, ruff, mypy, compileall, shim-marker gate, and residual fallback scans pass. |
| LS-64A | LS-64 | P1 | Director post-execution repair / direct writer fallback | Closed: `post_execution_repair_bridge` no longer exposes `_direct_runtime_writer` or permits C++ post-execution repair to write workspace files when no policy-gated Director adapter is present. | Closed by requiring explicit adapter binding for `run_cpp_post_repairs_as_tool_results`, returning structured `director_adapter_required_for_policy_gated_repair` fail-closed evidence for explicit `adapter=None`, preserving the normal `DirectorToolExecutor` path, refreshing the roles.adapters descriptor pack, and verifying focused C++ repair tests, architecture fences, ruff, mypy, compileall, and negative scans for the retired writer/test symbols. |
| LS-65A | LS-65 | P2 | Infrastructure accel / token-estimator metadata shim | Closed: `polaris.infrastructure.accel.token_estimator` no longer exists as a backward-compat metadata shim beside canonical KernelOne token estimation and domain token budget services. | Closed by deleting the retired shim, removing it from the FinOps Budget Guard catalog current modules/owned paths and generated packs, and adding `test_accel_token_estimator_shim_fence.py` to block file/import reintroduction. Verified with focused token-estimator tests, the architecture fence, catalog governance gate, ruff, mypy, py_compile, JSON/YAML validation, and active-path negative import scans. |
| LS-65B | LS-65 | P1 | Infrastructure accel / package-root stale token-estimator import | Closed: `polaris.infrastructure.accel.__init__` no longer imports or re-exports the retired `token_estimator` module. | Closed by removing package-root token-estimator exports and hardening `test_accel_token_estimator_shim_fence.py` to detect relative imports from `infrastructure/accel/__init__.py`. Verified with the accel fence, `test_kernelone_security_hardening.py`, KernelOne release gate, catalog governance gate, ruff, mypy, py_compile, and active-path negative import scans. |
| LS-66A | LS-66 | P2 | Roles engine / sequential strategy stale shim wording | Closed: `roles.engine.internal.sequential_adapter` remains the active single-shot sequential strategy, but no longer describes itself or its cell metadata as a compatibility shim. | Closed by rewording the implementation, cell gap, and descriptor capability descriptions around the canonical roles.engine fallback strategy, then adding `test_roles_engine_sequential_strategy_fence.py` to block stale shim wording from returning. Verified with focused roles.engine tests, the architecture fence, descriptor hash validation, catalog governance gate, ruff, mypy, py_compile, and active-path negative marker scans. |
| LS-67A | LS-67 | P2 | KernelOne context / budget allocator alias | Closed: `polaris.kernelone.context.contracts` no longer exports the unused `ContextAllocatorBudgetPort` backward-compat alias beside canonical `ContextBudgetAllocatorPort`. | Closed by removing the alias from the public contract and `__all__`, then adding `test_kernelone_context_budget_alias_fence.py` to block reintroduction. Verified with focused context-budget tests, the architecture fence, canonical import smoke, KernelOne release gate, catalog governance gate, ruff, mypy, py_compile, and active-path negative scans. |

## Closure Order

No open closure-order items remain. New legacy/shim findings must be added as a new
ledger item with concrete evidence, owner layer, and exit criteria before code changes
begin.

## Not Counted As Debt

- Provider protocol labels such as "OpenAI compatible" or "Anthropic compatible".
- React `Suspense fallback` and ordinary UI error fallback messages.
- Historical governance/archive documents that describe already-closed incidents.
- Tests and fixtures intentionally containing "legacy" to assert retired behavior stays blocked.

## Operating Rule

Close one item at a time. Each closure must include:

1. A scoped code change that reduces or removes a compatibility surface.
2. A negative search proving the old symbol/path/field cannot reappear silently.
3. A targeted test or type check proving the canonical path still works.
4. A ledger update that moves the item to `Closed` with the verification command.
