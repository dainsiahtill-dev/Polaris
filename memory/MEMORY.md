# Polaris Bench Memory

Reset timestamp: 2026-07-09T21:11:56+08:00

Scope:
- Past L1-01 bench outputs were deleted from `/tmp/factory-bench-L1-01-*`.
- Past L1-01 runtime cache was deleted from `~/.cache/kernelone/.polaris/projects/l1-01-*`.
- Previous bench conclusions are not used as evidence for the restarted sequence.

Current protocol:
- Restart from `L1-01`.
- Use `--launcher-instance-mode isolated`.
- Use Execution Ledger / task runtime facts, final provider request snapshots, tool lifecycle receipts, task boundary verdicts, and QA verdicts as evidence.
- Do not use raw TaskBoard rows, old session JSON, prompt summaries, or old memory as the source of truth.
- Do not modify generated target project code to pass bench.
- Do not add repair logic to legacy deterministic repair paths, `execute_method.py`, Factory, QA, or bench gates.

Runs:
- `L1-01 r01`: FAIL after 331.9s.
  - requested_project_id: `L1-01`
  - canonical_project_id: `L1-01`
  - instance_id: `factory-bench-l1-01-r01-l1-01`
  - workspace: `/tmp/factory-bench-L1-01-r01/L1-01`
  - backend_port: `49978`
  - frontend_port: `5174`
  - factory_run_id: `factory_09161e1146f3`
  - director_run_id: `director-e97c44b1cd9d`
  - runtime_dir: `/home/dains/.cache/kernelone/.polaris/projects/l1-01-0835c37d0f91/runtime`
  - pass rate: `0/1`; runnable rate: `0%`; source files: `0`; QA ran: `false`.
  - final provider request refs:
    - primary Director: `runtime/contexts/d7/d753c44ff0d4ae797e0e3ead`
    - required-tool text fallback: `runtime/contexts/ef/efb58f9d54fdfb6cf203c6fa`
    - final error snapshot: `runtime/contexts/22/22e0f1310b1e5365f8f42919`
  - provider request audit:
    - role identity: Director ok.
    - primary tools: `write_file`; `tool_choice=write_file`.
    - fallback text wrapper: `tools=[]`; `tool_choice=none`; expected for required-tool text fallback.
    - evidence coverage passed with PM contract, CE blueprint, target files, language guidance, execution envelope.
    - context utilization: primary about `0.0154`; fallback about `0.053`; no truncation/overload evidence.
  - root causes:
    - `runtime_environment:director_provider_rate_limit`: MiniMax quota/rate limit occurred before tool dispatch.
    - `fallback_failure_projection_gap`: Kimi required-tool text fallback failure was not preserved as the final error; the final event projected the original MiniMax error.
    - `backup_candidate_gap`: config had `_director_backup`, but role failover candidates only included `director.bindings`.
    - `task_boundary_misclassification`: project-level task boundary projected the no-artifact outcome as `IMPLEMENTATION_DEFECT` even though dispatch evidence classified provider quota as model_provider/resource exhaustion.
  - legacy regression: none found in repair path; no target project code edited.
  - OpenCode audit: attempted read-only audit, but `/tmp/polaris-opencode-l1-01-r01-audit.log` contained only startup text and no usable conclusion.
  - Polaris fixes made:
    - `src/backend/polaris/kernelone/llm/runtime_config.py`: append explicit `_<role>_backup` / `<role>_backup` role bindings to failover candidates without changing worker slots.
    - `src/backend/polaris/cells/roles/kernel/internal/llm_caller/invoker.py`: preserve latest role-binding fallback failure evidence and report it when all fallbacks fail.
    - `src/backend/polaris/kernelone/llm/tests/test_runtime_config_provider_pool.py`: coverage for backup role candidates.
    - `src/backend/polaris/cells/roles/kernel/tests/test_llm_invoker_role_binding_fallback.py`: coverage for fallback failure attribution.
  - validation:
    - `ruff check ... --fix`: pass.
    - `ruff format ...`: pass.
    - `mypy ...`: pass.
    - `pytest test_runtime_config_provider_pool.py test_llm_invoker_role_binding_fallback.py -q`: `33 passed`.
    - `pytest test_llm_caller_helpers.py -q`: `98 passed`.
    - `pytest test_llm_caller_components.py -k "required_tool or role_binding" -q`: `5 passed`.
- `L1-01 r02`: FAIL after 407.4s.
  - requested_project_id: `L1-01`
  - canonical_project_id: `L1-01`
  - instance_id: `factory-bench-l1-01-r02-l1-01`
  - workspace: `/tmp/factory-bench-L1-01-r02/L1-01`
  - backend_port: `49978`
  - frontend_port: `5174`
  - factory_run_id: `factory_31b887af7e34`
  - director_run_id: `director-5e8330d80c19`
  - runtime_dir: `/home/dains/.cache/kernelone/.polaris/projects/l1-01-5fe82f1518d9/runtime`
  - pass rate: `0/1`; runnable rate: `0%`; source files: `0`; QA ran: `false`.
  - final provider request refs:
    - primary Director: `runtime/contexts/37/37c788017a3735e0f023ec8c`
    - Kimi required-tool text fallback: `runtime/contexts/a7/a74f7f862b054da0ba34301b`
    - final error snapshot: `runtime/contexts/75/7530d4a4d00220343df33150`
  - provider route evidence:
    - MiniMax primary failed with rate limit before tool dispatch.
    - Kimi required-tool text fallback was attempted.
    - `_director_backup` Gemma was attempted: provider `openai_compat-1780683130410`, model `gemma-4-12B-it-Q8_0`.
  - root causes:
    - `fallback_request_provider_override_gap`: fallback profile could select Gemma, but `AIRequest.provider_id/model` could still carry stale primary values into `AIExecutor.resolve_provider_model`.
    - `native_fallback_context_snapshot_gap`: native role-binding fallback request emitted retry metadata without storing its own final provider request snapshot, so Gemma request evidence was incomplete.
    - `error_projection_still_primary`: final LLM error event had model Gemma but MiniMax 429 error text, consistent with stale provider/model propagation or original TransactionKernel error projection.
    - `task_boundary_misclassification`: project task boundary still reported `IMPLEMENTATION_DEFECT` for provider-route failure with zero tools/effects.
  - Polaris fixes made:
    - `src/backend/polaris/cells/roles/kernel/internal/llm_caller/invoker.py`: fallback invokes now use `_invoke_with_profile_binding` for both required-tool text fallback and native role-binding fallback.
    - `src/backend/polaris/cells/roles/kernel/internal/llm_caller/invoker.py`: `_invoke_with_profile_binding` now temporarily overrides `AIRequest.provider_id/model` and restores them after invoke.
    - `src/backend/polaris/cells/roles/kernel/internal/llm_caller/invoker.py`: native role-binding fallback now stores final provider request snapshot before emitting retry audit.
    - `src/backend/polaris/cells/roles/kernel/tests/test_llm_invoker_role_binding_fallback.py`: added stale request provider override regression.
  - validation:
    - `ruff check ... --fix`: pass.
    - `ruff format ...`: pass.
    - `mypy invoker.py test_llm_invoker_role_binding_fallback.py`: pass.
    - `pytest test_llm_invoker_role_binding_fallback.py -q`: `10 passed`.
    - `pytest test_llm_caller_helpers.py test_llm_caller_components.py -k "required_tool or role_binding" -q`: `9 passed`.
    - `pytest test_runtime_config_provider_pool.py -q`: `24 passed`.
- `L1-01 r03`: FAIL after 327.8s.
  - requested_project_id: `L1-01`
  - canonical_project_id: `L1-01`
  - instance_id: `factory-bench-l1-01-r03-l1-01`
  - workspace: `/tmp/factory-bench-L1-01-r03/L1-01`
  - backend_port: `49978`
  - frontend_port: `5174`
  - factory_run_id: `factory_6df53aff96ee`
  - director_run_id: `director-d2d3f11c866a`
  - runtime_dir: `/home/dains/.cache/kernelone/.polaris/projects/l1-01-98bff925811e/runtime`
  - pass rate: `0/1`; cumulative pass rate after reset: `0/3`; runnable rate: `0%`; source files: `0`; QA ran: `false`.
  - final provider request refs:
    - primary Director: `runtime/contexts/78/78df36151ced262cf81bc73d`
    - Kimi required-tool text fallback: `runtime/contexts/76/76f9a8c36e6e5b9c7620a890`
    - Gemma native role-binding fallback: `runtime/contexts/bb/bb51ad3667b9f229cd4bf818`
    - final error snapshot: `runtime/contexts/22/229907c71fe388400b4559e4`
  - provider request audit:
    - role identity: Director ok.
    - primary tools: `write_file`; `tool_choice=write_file`.
    - Kimi fallback tools: none with text fallback wrapper; expected for models without reliable forced native tool choice.
    - Gemma fallback tools: `write_file`; `tool_choice=write_file`; provider `openai_compat-1780683130410`; model `gemma-4-12B-it-Q8_0`.
    - Gemma fallback context utilization: about `0.9417` of a 16k window; available headroom about `955` tokens.
    - final error snapshot incorrectly projected MiniMax provider/error text while the event model said Gemma.
  - runtime/task evidence:
    - tool lifecycle receipt: absent because all providers failed before tool dispatch.
    - task boundary verdict: still no materialized effects; classified as `IMPLEMENTATION_DEFECT` instead of a provider-route/platform failure.
    - route audit: `llm_route_audit` failed for Director and only had reliable MiniMax no-dispatch evidence.
  - root causes:
    - `fallback_error_evidence_mutability_gap`: `_invoke_with_profile_binding` correctly pinned fallback provider/model during invoke, but restored the same mutable `AIRequest` afterward; the later final error evidence reused that restored object and projected primary MiniMax.
    - `call_error_provider_metadata_gap`: call_error metadata did not explicitly include provider/provider_id/model for the active fallback binding, making route audit easier to misproject.
    - `fallback_context_pressure`: Gemma fallback got a valid final request but used about 94% of its configured window; this is a separate capacity risk to watch on rerun.
  - legacy regression: none found; no repair path or target project code edited.
  - Polaris fixes made:
    - `src/backend/polaris/cells/roles/kernel/internal/llm_caller/invoker.py`: added evidence-only profile-bound request cloning before call_error audit generation, so fallback failure evidence cannot be polluted by restored primary provider/model fields.
    - `src/backend/polaris/cells/roles/kernel/internal/llm_caller/invoker.py`: call_error metadata now includes explicit `provider`, `provider_id`, and `model` for the active provider binding.
    - `src/backend/polaris/cells/roles/kernel/tests/test_llm_invoker_role_binding_fallback.py`: added regression coverage for evidence request pinning and fallback failure metadata attribution.
  - validation:
    - `ruff check invoker.py test_llm_invoker_role_binding_fallback.py --fix`: pass.
    - `ruff format invoker.py test_llm_invoker_role_binding_fallback.py`: pass.
    - `mypy invoker.py test_llm_invoker_role_binding_fallback.py`: pass.
    - `pytest test_llm_invoker_role_binding_fallback.py -q`: `11 passed`.
    - `pytest test_llm_caller_helpers.py test_llm_caller_components.py -k "required_tool or role_binding" -q`: `9 passed`.
    - `pytest test_runtime_config_provider_pool.py -q`: `24 passed`.
- `L1-01 r04`: FAIL after 347.0s.
  - requested_project_id: `L1-01`
  - canonical_project_id: `L1-01`
  - instance_id: `factory-bench-l1-01-r04-l1-01` (Launcher run_identity projected an empty instance_id; bench command instance name was used for cleanup)
  - workspace: `/tmp/factory-bench-L1-01-r04/L1-01`
  - backend_port: `49978`
  - frontend_port: `5174`
  - factory_run_id: `factory_a1a7b5cdf966`
  - director_run_id: `director-762d3832fcaa`
  - runtime_dir: `/home/dains/.cache/kernelone/.polaris/projects/l1-01-6cdef265d31a/runtime`
  - pass rate: `0/1`; cumulative pass rate after reset: `0/4`; runnable rate: `0%`; source files: `0`; QA ran: `false`.
  - final provider request refs:
    - primary Director MiniMax: `runtime/contexts/12/1273f098a829b2d5adb055a1`
    - Kimi required-tool text fallback: `runtime/contexts/05/051956317a7613bcaacf7a1a`
    - Gemma native role-binding fallback: `runtime/contexts/69/69f572dcdc7635263e51d7df`
    - final error snapshot: `runtime/contexts/fd/fd217fde5d4b4305c1d14221`
  - provider request audit:
    - role identity: Director ok.
    - primary tools: `write_file`; `tool_choice=write_file`; context utilization about `0.0154`.
    - Kimi fallback tools: none with text fallback wrapper; context utilization about `0.053`.
    - Gemma fallback tools: `write_file`; `tool_choice=write_file`; context utilization about `0.9417`, headroom about `955` tokens.
    - r03 provider projection bug is fixed: final `llm_error` event provider/model are `openai_compat-1780683130410` / `gemma-4-12B-it-Q8_0`.
    - remaining mismatch: final `llm_error.error_message` still carried MiniMax 429 text even though provider/model pointed to Gemma.
  - runtime/task evidence:
    - `director.result.json` task 1 classified `INCOMPLETE_MATERIALIZATION`; tools_executed=0; changed_files=[].
    - `primary_llm.metadata.provider_request_snapshot_degraded=true`, reason `transaction_kernel_failed_without_llm_metadata`.
    - `transaction_kernel_error_audit_available=false` in Director adapter result even though `.llm.events.jsonl` had final request evidence.
    - `task_runtime.execution.jsonl` reached failed with no tool dispatch; no tool lifecycle receipt because LLM failed before tool dispatch.
    - `run_identity.instance_id` was empty in factory audit bundle, which is an observability projection gap to keep tracking.
  - root causes:
    - `decision_error_metadata_drop`: `DecisionCaller` raised a plain `RuntimeError` on failed `LLMResponse`, discarding response metadata containing final request audit, context snapshot, provider/model and error category.
    - `turn_ledger_decision_exception_gap`: `TurnTransactionController._call_llm_for_decision` only recorded `TurnLedger.llm_calls` after a successful provider response; provider exceptions during `DECISION_REQUESTED` left TransactionKernel error projection degraded.
    - `transaction_failure_provider_overwrite`: `_profile_error_metadata` overwrote ledger/fallback provider/model with the primary profile provider/model.
    - `error_message_origin_gap`: provider/model projection is now correct, but the remaining error text is still the primary MiniMax quota message; r05 must verify whether DecisionCaller metadata preservation fixes this or whether lower AIExecutor/TransactionKernel still reuses the primary exception string.
  - legacy regression: none found; no repair path, bench gate, or target project code edited.
  - Polaris fixes made:
    - `src/backend/polaris/cells/roles/kernel/internal/llm_caller/decision_caller.py`: failed decision calls now attach `llm_response_metadata`, `llm_response_model`, and error category to the raised exception.
    - `src/backend/polaris/cells/roles/kernel/internal/turn_transaction_controller.py`: decision provider exceptions with LLM metadata now record a `decision_error` LLM call in `TurnLedger` before re-raising.
    - `src/backend/polaris/cells/roles/kernel/internal/kernel/tool_dispatch_projection.py`: transaction error metadata projection now preserves provider/provider_id/model/error fields from ledger LLM calls.
    - `src/backend/polaris/cells/roles/kernel/internal/kernel/transaction_failure_projection.py`: profile metadata now only fills missing provider/model instead of overwriting ledger evidence.
    - Added regression tests in `test_llm_caller_components.py`, `test_role_kernel_transaction_wiring.py`, `test_tool_dispatch_projection.py`, and `test_transaction_failure_projection.py`.
  - validation:
    - `ruff check ... --fix` and `ruff format ...`: pass.
    - `mypy` over 8 touched production/test files: pass.
    - `pytest test_llm_caller_components.py -k "DecisionCaller" -q`: `6 passed`.
    - `pytest test_tool_dispatch_projection.py test_transaction_failure_projection.py -q`: `10 passed`.
    - `pytest test_role_kernel_transaction_wiring.py -k "decision_exception or provider_preserves_explicit_empty_forced_tools_override or provider_tool_calls_alias_reaches_transaction_dispatch" -q`: `3 passed`.
- Pending: run `L1-01 r05` on the TransactionKernel error-evidence fix and audit whether `provider_request_snapshot_degraded` is gone.
- `L1-01 r05`: FAIL after 457.7s.
  - requested_project_id: `L1-01`
  - canonical_project_id: `L1-01`
  - instance_id: `factory-bench-l1-01-r05-l1-01` (deleted after run; Launcher returned 404 on repeated delete, so no lingering owned instance)
  - workspace: `/tmp/factory-bench-L1-01-r05/L1-01`
  - backend_port: `49978`
  - frontend_port: `5174`
  - factory_run_id: `factory_394f6dc3868a`
  - director_run_id: `director-0e6b7dd2f9e6`
  - runtime_dir: `/home/dains/.cache/kernelone/.polaris/projects/l1-01-4f505e3a2f5b/runtime`
  - pass rate: `0/1`; cumulative pass rate after reset: `0/5`; runnable rate: `0%`; source files: `0`; QA ran: `false`.
  - final provider request refs:
    - primary Director MiniMax: `runtime/contexts/96/961500b2ba4f03fbc64bc128`
    - Kimi required-tool text fallback: `runtime/contexts/2f/2fafbe4e0bc2d5da398bd670`
    - Gemma native role-binding fallback: `runtime/contexts/43/43bb9610a7c6cdfe5515ca0f`
    - final error snapshot: `runtime/contexts/dc/dcb257cbbc61a27d88ce817b`
  - provider request audit:
    - role identity: Director ok.
    - primary tools: `write_file`; `tool_choice=write_file`; context utilization about `0.0155`.
    - Kimi fallback tools: none with text fallback wrapper; context utilization about `0.0532`.
    - Gemma fallback tools: `write_file`; `tool_choice=write_file`; context utilization about `0.9441`, about `916` tokens headroom.
    - r04 fix worked: `director.result.json` no longer has `provider_request_snapshot_degraded`; `transaction_kernel_error_audit_available=true`.
    - remaining inconsistency: final `llm_error` provider/model are Gemma, but error text still says MiniMax 429.
  - runtime/task evidence:
    - `task_runtime.execution.jsonl`: task 1 claimed, heartbeats, then failed with `director_no_materialized_changes`; no tool dispatch/effect receipt because LLM failed before dispatch.
    - `director.result.json`: task 1 classified `INCOMPLETE_MATERIALIZATION`; tools_executed=0; changed_files=[]; tasks 2 and 3 blocked by task 1.
    - `goal_audit.root_cause_category`: `runtime_environment:director_provider_rate_limit`.
    - local direct `http://127.0.0.1:8000/v1/models` returned HTTP 502 with empty body, so the Gemma route was not healthy during the run.
    - `run_identity.instance_id` was empty in the factory audit bundle; this remains an observability projection gap.
  - root causes:
    - `runtime_environment:director_provider_exhaustion`: MiniMax quota/rate limit, Kimi fallback failure, Gemma fallback route unavailable/unhealthy before tool dispatch.
    - `fallback_error_message_origin_gap`: provider/model/context evidence now points to Gemma, but the final error string still carries the primary MiniMax 429 text; need distinguish external gateway behavior from platform stale-error reuse.
    - `task_boundary_failure_class_gap`: zero-effect provider-route failure still surfaces as incomplete materialization/implementation-class failure in task boundary evidence.
    - `observability_projection_gap`: factory run identity omits `instance_id` even though requested/canonical project and workspace/ports are available.
  - legacy regression: none found; no deterministic repair path, bench gate, `execute_method.py`, or target project code edited.
  - next action:
    - Audit fallback error origin with codegraph and focused tests before rerun.
    - If platform metadata is correct and only provider availability fails, classify as environment/provider routing, not JS implementation or repair failure.
- `L1-01 r05 post-audit fix`: Runtime backup binding override root cause identified and fixed.
  - evidence:
    - Actual Director candidates from runtime config: primary `anthropic_compat-1782212251463` / `MiniMax-M3`, fallback `anthropic_compat-1771249789301` / `kimi-for-coding`, backup `openai_compat-1780683130410` / `gemma-4-12B-it-Q8_0`.
    - Gemma config base_url is `http://127.0.0.1:8000/v1`; a true Gemma transport failure should not format as MiniMax anthropic URL.
    - r05 LLM events showed Gemma final request context was present but final error text still used MiniMax 429.
  - root cause:
    - `backup_binding_override_resolution_gap`: `_director_backup` was appended to failover candidates, but `RuntimeConfigManager.get_role_model("director")` only honored binding overrides that matched the primary `director` role bindings/pool.
    - If primary/Kimi cooldown expired near the 120s fallback boundary, `AIExecutor.resolve_provider_model()` could see a context role override but resolve it back to primary MiniMax because the backup binding was not acknowledged by the role-model resolver.
  - Polaris fixes made:
    - `src/backend/polaris/kernelone/llm/runtime_config.py`: binding override resolution now also matches `get_role_binding_candidates(role)` so explicit backup role candidates are authoritative for the current execution context.
    - `src/backend/polaris/kernelone/llm/tests/test_runtime_config_provider_pool.py`: added coverage that a `director` binding override to `_director_backup` Gemma wins in both `get_role_model` and `resolve_provider_model`.
    - `src/backend/polaris/cells/roles/kernel/tests/test_llm_invoker_role_binding_fallback.py`: added coverage for primary failure -> Kimi text fallback failure -> Gemma native fallback failure; final error/event metadata must report Gemma failure, not primary MiniMax or Kimi.
  - validation:
    - `ruff check runtime_config.py test_runtime_config_provider_pool.py test_llm_invoker_role_binding_fallback.py --fix`: pass.
    - `ruff format runtime_config.py test_runtime_config_provider_pool.py test_llm_invoker_role_binding_fallback.py`: pass.
    - `mypy runtime_config.py test_runtime_config_provider_pool.py test_llm_invoker_role_binding_fallback.py`: pass.
    - `pytest test_runtime_config_provider_pool.py -q`: `25 passed`.
    - `pytest test_llm_invoker_role_binding_fallback.py -q`: `12 passed`.
  - remaining risks:
    - Local Gemma endpoint was unhealthy during r05 (`/v1/models` returned HTTP 502), so r06 may still fail for provider availability, but it should no longer misroute final Gemma fallback to MiniMax.
    - Task boundary still needs better classification for provider-route failures with zero materialized effects.
- `L1-01 r06`: FAIL after 404.5s, but provider/tool dispatch materially improved.
  - requested_project_id: `L1-01`
  - canonical_project_id: `L1-01`
  - instance_id: `factory-bench-l1-01-r06-l1-01` (deleted after audit via Launcher, DELETE 200)
  - workspace: `/tmp/factory-bench-L1-01-r06/L1-01`
  - backend_port: `49978`
  - frontend_port: `5174`
  - factory_run_id: `factory_8dda75528e3b`
  - director_run_id: `director-193afb8afdff`
  - runtime_dir: `/home/dains/.cache/kernelone/.polaris/projects/l1-01-d499f2c80428/runtime`
  - pass rate: `0/1`; cumulative pass rate after reset: `0/6`; runnable/materialization rate: source files `7`, TS syntax `ok`, package scripts `ok`; QA ran `true`, QA passed `false`.
  - wall clock: `404.5s`.
  - final outcome:
    - code_file_count: `11`; source_file_count: `7`; prod_lines: `566`; behavior_symbols: `58`; branches: `60`.
    - failed gates: `implementation_depth`, `chain_clean`, `integration_qa_passed`, `real_run_gate`, `run_ledger_projection`, `delivery_depth_gate`.
    - immediate compiler diagnostic: `src/main.ts(44,47)` and `src/main.ts(63,47)` `TS2304 Cannot find name 'dayOfYear'.`
    - downstream symptom: `test_files=0`, because TASK-1 failed before TASK-2/TASK-3, where CE had target test files.
  - execution evidence:
    - Execution Ledger / truthlog showed `tool_batch_started` with `tool_count=9`, `tool_batch_completed` with `receipt_count=9`, and a successful `tool_batch_with_receipt`; no `tool_dispatch_dropped`.
    - Director wrote `package.json`, `tsconfig.json`, `src/index.ts`, `src/main.ts`, `src/models/Firefly.ts`, `src/models/Flower.ts`, `src/models/MoonPhase.ts`, `src/models/Humidity.ts`, and `src/web.ts`.
    - PM contract contained level minimums and required `tests/` directory.
    - CE blueprints included test targets in TASK-2/TASK-3, but TASK-1 target_files did not include tests.
    - TASK-1 failed task boundary/materialization quality before downstream test tasks could run.
  - final provider/context audit:
    - Director primary route used `anthropic_compat-1782212251463` / `MiniMax-M3`.
    - LLM route audit passed.
    - Tool schema included `write_file` and dispatch produced receipts.
    - Quality-gate LLM repair final request coverage failed, missing refs including `pm_contract`, `ce_blueprint`, `failed_gate_evidence`, `architecture_or_file_plan`, `module_interface_contract`, and `framework_best_practices`; this is a separate post-Director repair-context gap.
  - coverage / plan probe:
    - `query_director_repair_coverage`: `TS2304 dayOfYear` was `known_rule_matched=true`, `executable_runtime_plan_matched=true`.
    - `query_director_repair_plan_probe` before fix: `coverage_matched_but_unplannable`; `deterministic_typescript_reexported_type_binding_repair` and `deterministic_typescript_unresolved_identifier_repair` both produced `patch_count=0`.
  - root causes:
    - `typescript_runtime_planner_gap`: TS2304 unresolved identifier coverage existed, but runtime planner could not produce a patch for the common case where the missing value is already exported by an existing local barrel import.
    - `downstream_tests_blocked`: test targets were in downstream tasks, but TASK-1 build failure blocked them.
    - `quality_gate_repair_context_gap`: failed-gate LLM repair final request evidence coverage failed before it could assist.
  - Polaris fixes made:
    - `src/backend/polaris/cells/director/runtime/internal/repair_kernel/typescript_syntax.py`: extended `typescript.unresolved_identifier` planner to add a missing identifier to an existing named import when that imported local module actually exports the symbol. The rule only patches existing import declarations and fails closed if no current imported module exports the symbol.
    - `src/backend/polaris/cells/director/runtime/tests/test_repair_kernel_contract.py`: added public-boundary coverage that `TS2304 Cannot find name 'dayOfYear'` from an existing barrel import is `covered_plannable`.
  - validation:
    - `ruff check typescript_syntax.py test_repair_kernel_contract.py --fix`: pass.
    - `ruff format typescript_syntax.py test_repair_kernel_contract.py`: pass.
    - `mypy typescript_syntax.py test_repair_kernel_contract.py`: pass.
    - `pytest test_repair_kernel_contract.py -k "unresolved_identifier_import_from_existing_barrel or typescript_ts2304_dom_html_type_diagnostic_is_covered_plannable or reexported_type_binding" -q`: `3 passed`.
    - `pytest test_repair_kernel_coverage_gate.py test_repair_kernel_javascript_python_runtime.py -q`: `47 passed`.
    - Re-running r06 diagnostic through public plan probe now returns `covered_plannable` with plannable source tool `deterministic_typescript_unresolved_identifier_repair` and one patch to `src/main.ts`.
  - legacy regression: none found; no Factory/QA/bench gate/legacy deterministic repair path edited; no target project code edited as a fix.
