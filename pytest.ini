[pytest]
addopts = --import-mode=importlib
norecursedirs = tmp_pytest_agentic_eval_local .polaris .git .tox dist build *.egg-info
markers =
    integration: integration/e2e style tests
    slow: long-running tests
